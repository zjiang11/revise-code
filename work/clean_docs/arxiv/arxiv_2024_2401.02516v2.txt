Moving-Horizon Estimators
for Hyperbolic and Parabolic PDEs in 1-D
Luke Bhan, Yuanyuan Shi, Iasson Karafyllis, Miroslav Krstic, and James B. Rawlings
Abstract— Observers for PDEs are themselves PDEs. There-
fore, producing real time estimates with such observers is
computationally burdensome. For both finite-dimensional and
ODE systems, moving-horizon estimators (MHE) are operators
whose output is the state estimate, while their inputs are the
initial state estimate at the beginning of the horizon as well
as the measured output and input signals over the moving
time horizon. In this paper we introduce MHEs for PDEs
which remove the need for a numerical solution of an observer
PDE in real time. We accomplish this using the PDE back-
stepping method which, for certain classes of both hyperbolic
and parabolic PDEs, produces moving-horizon state estimates
explicitly. Precisely, to explicitly produce the state estimates,
we employ a backstepping transformation of a hard-to-solve
observer PDE into a target observer PDE, which is explicitly
solvable. The MHEs we propose are not new observer designs
but simply the explicit MHE realizations, over a moving horizon
of arbitrary length, of the existing backstepping observers. Our
PDE MHEs lack the optimality of the MHEs that arose as
duals of MPC, but they are given explicitly, even for PDEs.
In the paper we provide explicit formulae for MHEs for
both hyperbolic and parabolic PDEs, as well as simulation
results that illustrate theoretically guaranteed convergence of
the MHEs.
I. INTRODUCTION
a) Motivation: moving-horizon estimators for PDEs
In this paper, we develop moving-horizon estimators for
two distinct classes of PDEs: first-order hyperbolic partial
integro-differential equation (PIDE) systems and parabolic
PDE systems governing reaction-diffusion processes. Our
motivation stems from a well-established challenge in the
field that PDE observers are expensive to compute in real
time due to being PDEs themselves. Instead, we craft explicit
representations of the PDE observer systems, bypassing
the challenges of PDE discretization schemes like finite
differences, which, when mishandled, can lead to numerical
instability as in the Gibbs phenomenon. To achieve explicit
representations of PDE observers, we employ the power
of PDE backstepping transformations. By designing these
transformations, we convert complex observer PDE systems,
which cannot be analytically solved, into target systems that
are analytically solvable. We then go a step further and,
rather then producing state estimates using the measured
signal history from the initial time, we employ the observer
PDE’s explicit solution over a moving time horizon, requir-
ing only a measurement history over a constant, most recent
time interval. This represents the notion of a moving-horizon
estimator (MHE), extended from [22] to PDE systems. The
MHEs we produce are the equivalent representations of the
PDE observers and maintain their guarantees of exponential
convergence towards the state of the plant. Thus, in this
paper, we present a blueprint for a new direction in PDE
observer design—PDE MHEs—on two separate PDE bench-
mark systems from distinct PDE classes.
b) Advances in PDE observers
The first results in PDE observers began with [29]
presenting a series of observers for 1-D parabolic PDEs
using backstepping transformations. Following this result,
observers were developed across a variety of PDE classes
and even coupled PDE-ODE systems. For example, for
hyperbolic PDEs, references [2], [5], [34] present results
for both complex coupled hyperbolic PDE-ODE systems as
well as adaptive observer designs. Reference [18] develops
observers for coupled parabolic PDEs with different diffu-
sion coefficients which represent simplified chemical tubular
reactor models.
Additionally, observers have been developed for targeted
applications such as the Burgers equation and more challeng-
ing Navier-Stokes equations in [14], [32], [35], [36]. State-
of-charge (SOC) estimation for electrochemical models is
studied in [19] and [33] studies a boundary observer for 2-D
thermal fluid convection. Lastly, a prescribed-time observer
for the linear Schr¨odinger equation is presented in [31].
c) Moving horizon estimators
For many years, MHEs have been well studied for finite
dimensional systems [20], [23], [21], [25, Ch. 4], [22], [24],
[1], [26]. Particularly, MHEs are robustly globally exponen-
tially stable (RGES) under mild exponential assumptions [25,
S. 4.3.3]. Additionally, reference [8] explores and provides
conditions for convergence of MHEs in hybrid systems. From
an application perspective, MHEs have found success over
traditional approaches such as the extended Kalman filter
in networked chemical reaction systems [9] [16]. Moreover,
reference [38] develops a MHE algorithm for distributed net-
working systems and lastly, MHEs have played an important
role in developing robots for various applications ranging
from underwater localization to agricultural machinery [37]
[17] [12].
However, to the author’s knowledge, very little work
has been explored in MHEs for PDEs without first dis-
cretizing the PDE (early lumping) and then performing well
known finite dimensional MHE [10]. Recently, reference [6]
proposed a late lumping approach—but only for reaction
diffusion PDEs and builds their method from gradient-based
estimation rather than explicitly solving the observer PDE as
done in this work.
d) Contributions
The paper’s contributions are for two separate PDE
classes. First, we present a novel hyperbolic PIDE observer.
arXiv:2401.02516v2  [eess.SY]  28 Nov 2024

We then prove that the observer exponentially converges to
the true system state via Lyapunov analysis. We then convert
the PDE observer into an explicit MHE, retaining all the
convergence guarantees of the PDE observer. We repeat the
process for parabolic PDEs, leveraging some of our results
from [29]. Lastly, in the case of parabolic PDEs we conclude
with illustrations of our result via a simulation of the explicit
MHE for a slowly unstable yet sufficiently excited parabolic
PDE.
e) Notation
R+ is the interval [0, +∞). Let u : [0, 1] × R+ →R
be given. We use u[t] to denote the profile of u at t, i.e.,
(u[t])(x) = u(x, t) for all x ∈[0, 1]. L2(0, 1) denotes the
space of equivalent classes of square-integrable, measurable
functions defined on (0, 1). For every u in L2(0, 1), ∥u∥
denotes the standard norm of u in L2(0, 1). H2(0, 1) is the
Sobolev space of functions in L2(0, 1) with weak first and
second derivatives in L2(0, 1). Let ux(x, t) denote the partial
derivative of the function u with respect to spatial variable
x and ut(x, t) denote similarly for t.
II. 1D HYPERBOLIC PIDE
We consider hyperbolic PIDE systems commonly found
in explaining physical phenomena such as traffic flows and
chemical reactors [15] as
ut(x, t)
=
ux(x, t) + g(x)u(0, t)
+
Z x
0
f(x, y)u(y, t)dy ,
(1)
u(1, t)
=
U(t) ,
(2)
Y (t)
=
u(0, t) ,
(3)
where t ≥0 is the time, x ∈[0, 1] is the spatial coordinate,
U is the control and Y is the measured output. Furthermore,
we require the following assumption.
Assumption 1: f ∈C1(T ) and g ∈C1([0, 1]), where
T = {(x, y) ∈R2; 0 ≤y ≤x ≤1}.
For every u0 ∈C([0, 1]) and U ∈C1(R+) that satisfy
the compatibility conditions
u0(1)
=
U(0) ,
(4)
˙U(0)
=
u′
0(1) + g(1)u0(0) +
Z 1
0
f(1, y)u0(y)dy ,(5)
using Corollary 2.3 in [11], we guarantee that the PDE
system (1), (2) with initial condition
u[0] = u0
(6)
has a unique classical solution defined for all t ≥0.
Moreover, in relation to Assumption 1, we define
Mg
=
sup
x∈[0,1]
|g(x)| ,
(7)
Mf
=
sup
(x,y)∈T
|f(x, y)| .
(8)
Next, we postulate the observer
ˆut(x, t)
=
ˆux(x, t) + g(x)ˆu(0, t)
+
Z x
0
f(x, y)ˆu(y, t)dy
+p1(x)[u(0, t) −ˆu(0, t)] ,
(9)
ˆu(1, t)
=
U(t) ,
(10)
with a (Dirichlet) boundary measurement (3) at x = 0, and
with the observer gain function p1(x) to be derived. As
above, we require that ˆu0 ∈C1([0, 1]) and that the following
compatibility conditions
ˆu0(1)
=
U(0) ,
(11)
˙U(0)
=
ˆu′
0(1) + g(1)ˆu0(0)
+
Z 1
0
f(1, y)ˆu0(y)dy
+p1(1)

u0(0) −ˆu0(0)

,
(12)
are satisfied to ensure the system (1), (2), (9), (10) with initial
conditions (6) and
ˆu[0] = ˆu0
(13)
has a unique classical solution defined for all t ≥0.
Further, let the state estimation error be denoted by
˜u(x, t) = u(x, t) −ˆu(x, t) ,
(14)
resulting in the error system
˜ut(x, t)
=
˜ux(x, t) + g(x)˜u(0, t)
+
Z x
0
f(x, y)˜u(y, t)dy
−p1(x)˜u(0, t) ,
(15)
˜u(1, t)
=
0 .
(16)
Now, consider applying the backstepping transformation
˜u(x, t) = ˜w(x, t) −
Z x
0
k(x, y) ˜w(y, t)dy ,
(17)
with the goal of ensuring that the state ˜w of the observer
error target system be governed by the plain homogeneous
transport PDE
˜wt(x, t)
=
˜wx(x, t) ,
(18)
˜w(1, t)
=
0 .
(19)
To transform (15), (16) into this target system, k(x, y) must
satisfiy the Goursat PDE
ky(x, y) + kx(x, y)
=
f(x, y)
−
Z x
y
f(x, η)k(η, y)dη ,
∀(x, y) ∈T ,
(20)
k(1, y)
=
0, ∀y ∈[0, 1] ,
(21)
and the observer gain p1(x) must be given by
p1(x) = g(x) −k(x, 0) .
(22)
The following result certifies that the kernel PDE k(x, y) is
bounded.
Theorem 1: (proven in [2]) For every f ∈C1(T ) and
g ∈C1([0, 1]) the PDE problem (20), (21), has a unique
C1(T ) solution with the bound
|k(x, y)| ≤Mf(1 −x)eMf (x−y)(1−x) ,
∀(x, y) ∈T . (23)
The result is proven in Sec 6.1 of [2] using the method
of successive approximations coupled with a change of

variables. Following Theorem (1), we define
¯k =
sup
(x,y)∈T
|k(x, y)|
(24)
Furthermore, recall the inverse backstepping kernel
l(x, y) ∈C1(T ) and the inverse backstepping transformation
as
˜w(x, t) = ˜u(x, t) +
Z x
0
l(x, y)˜u(y, t)dy .
(25)
From Section 4.5 of [15], the backstepping and inverse
backstepping kernels satisfy
l(x, y)
=
k(x, y) +
Z x
y
k(x, η)l(η, y)dy .
(26)
leading to the following bound on the inverse kernel
|l(x, y)| ≤¯ke
¯k .
(27)
Then, applying (23) to k(x, y) in (27) yields the following
conservative bound
|l(x, y)| ≤
 MfeMf 
eMf eMf .
(28)
Similar to above, we then define
¯l = sup
T
|l(x, y)|
(29)
We now have the tools to present the first result for the
observer in (9), (10).
Theorem 2: Let Assumption 1 hold. Then for every U ∈
C1(R+), u0, ˆu0 ∈C1([0, 1]) for which the compatibility
conditions in (4), (5), (11), (12) hold, the observer (9), (10)
ensures that the error between its state with initial condition
(13) and the state of the plant (1), (2), (3) with initial
condition (6) satisfies for all t ≥0 and c > 0 the exponential
stability bound
∥u[t] −ˆu[t]∥≤Me−ct∥u0 −ˆu0∥,
(30)
where
M = ec(1 +
 MfeMf 
eMf eMf )(1 + MfeMf ) ,
(31)
Moreover, ˆu(t) ≡u(t) for t ≥1, namely, the observer error
is equal to 0 for t ≥1 and remains zero thereafter.
Proof: First, note that the observer can be transformed
into the target system ˜w as in (18), (19) using the back-
stepping transformation in (17). We propose the Lyapunov
functional for the ˜w system as
V (t) = 1
2
Z 1
0
ecx ˜w2(x, t)dx,
c > 0 ,
(32)
with derivative
˙V
=
−˜w2(0, t) −c
Z 1
0
ecx ˜w2(x, t)dx
(33)
=
−˜w2(0, t) −2cV .
(34)
Note that V satisfies
1
 1 + ¯k
2 ∥˜u∥2 ≤V ≤ec  1 + ¯l
2 ∥˜u∥2 ,
(35)
and since V (t) ≤V (0)e−2ct, plugging in for (35) yields
∥˜u[t]∥≤ec(1−t)(1 + ¯l)(1 + ¯k)∥˜u[0]∥.
(36)
Lastly, note that by explicit calculation of (18), (19), we
show that ˜w is 0 for t ≥1.
We have now established that the observer in (9), (10)
converges to the true system exponentially. However, in
practice, it can be challenging to implement the PDE form
of the observer and so we provide the observer’s explicit
moving-horizon form in the next result. We assume that the
horizon T is not shorter than one, i.e., T ≥1. We do so
for the sake of simplifying the expressions of the MHE and
for avoiding the need to store the past values of the estimate
ˆu(x, t −T).
Theorem 3: Let Assumption 1 hold. Then for every U ∈
C1(R+), u0, ˆu0 ∈C1([0, 1]) for which the compatibility
conditions in (4), (5), (11), (12) hold, the solution of (1),
(2), (3), (9), (10) with initial conditions (6), (13) satisfies for
all t ≥1
ˆu(x, t) = ˆw(x, t) −
Z x
0
k(x, y) ˆw(y, t)dy ,
∀x ∈[0, 1] , (37)
where ˆw is given by the formula
ˆw(x, t)
=
Z t
t+x−1
θ(t + x −τ)Y (τ)dτ
+U(t + x −1) , ∀x ∈[0, 1] ,
(38)
with θ defined as
θ(x)
=
p1(x) +
Z x
0
l(x, y)p1(y)dy ,
(39)
where p1(x) is defined by (22).
Note that Theorem 3 allows us to use (37) as an ex-
plicit moving-horizon estimator. Furthermore, in (39), the
functions θ(x) and p1(x) are in an inverse backstepping
relationship, similar to the states of the ˆw and ˆu systems.
Proof: First, note that the observer system in (9), (10)
under the inverse backstepping transform
ˆw(x, t) = ˆu(x, t) +
Z x
0
l(x, y)ˆu(y, t)dy
(40)
yields
ˆwt(x, t)
=
ˆwx(x, t) + θ(x)u(0, t)
(41)
ˆw(1, t)
=
U(t)
(42)
which is a transport PDE with a source term. The resulting
PDE is solved explicitly as in [2] yielding the desired result.
Note that the moving-horizon estimator in (37), (38) does
not depend on past state estimates ˆu(x, t−T), where T > 0,
at the start t −T of the horizon, but just on the input
and output signals for the PDE over the window [t −1, t].
This is due to the property of the transport PDE with a
unity propagation speed that the effect of its initial condition
”washes out” of the domain in the time interval [0, 1] and is
zero for t ≥1.
III. 1D PARABOLIC PDE
We now consider parabolic PDE systems of the form of
ut(x, t)
=
uxx(x, t) + λ(x)u(x, t),
x ∈(0, 1) ,
(43)
u(0, t)
=
0 ,
(44)
u(1, t)
=
U(t) .
(45)

under the following assumption.
Assumption 2: λ ∈C1([0, 1]).
Moreover, in relation to Assumption 2, we define
¯λ = sup
x∈[0,1]
|λ(x)| .
(46)
Assumption 2 guarantees that for every u0 ∈H2(0, 1)
and every U ∈C2(R+) with
u0(0)
=
0 ,
(47)
u0(1)
=
U(0) ,
(48)
there exists a unique solution of the initial-boundary value
problem (43), (44), (45) with initial condition given by (6),
which is defined for all t ≥0 (see results for parabolic PDEs
in the book [7]).
We study (43), (44), (45) with (Neumann) boundary
measurement
Y (t) = ux(1, t) ,
(49)
at x = 1.
Next we continue again by postulating an observer of the
form
ˆut(x, t)
=
ˆuxx(x, t) + λ(x)ˆu(x, t)
+p1(x)

ux(1, t) −ˆux(1, t)] ,
(50)
ˆu(0, t)
=
0 ,
(51)
ˆu(1, t)
=
U(t) ,
(52)
and with the observer gain function p1(x) to be derived. As
above, we require that ˆu0 ∈H2(0, 1) and that the following
compatibility conditions
ˆu0(0)
=
0 ,
(53)
ˆu0(1)
=
U(0) ,
(54)
are satisfied to ensure the system (43), (44), (45), (50), (51),
(52) with initial conditions (6) and (13) has a unique classical
solution defined for all t ≥0 (here a backstepping argument
is first used in order to eliminate the nonlocal terms that
appear on the right hand side of (50)).
We need to choose p1(x) such that the error system is
stable. As above, the error system can be written as
˜ut(x, t)
=
˜uxx(x, t) + λ(x)˜u(x, t)
−p1(x)˜ux(1, t) ,
(55)
˜u(0, t)
=
0 ,
(56)
˜u(1, t)
=
0 .
(57)
As the hyperbolic PDE system, our goal is to transform the
system (55), (56), (57) into the following target system
˜wt(x, t)
=
˜wxx(x, t) ,
(58)
˜w(0, t)
=
0 ,
(59)
˜w(1, t)
=
0 ,
(60)
through the backstepping transform
˜u(x, t) = ˜w(x, t) −
Z 1
x
k(x, y) ˜w(y, t)dy.
(61)
To achieve the transformation of (55), (56), (57) into (58),
(59), (60), the kernel PDE k(x, y) must satisfy the Goursat
PDE
kxx(x, y) −kyy(x, y)
=
λ(y)k(x, y),
∀(x, y) ∈T ,
(62)
k(x, 0)
=
0,
∀x ∈[0, 1] ,
(63)
k(x, x)
=
−1
2
Z x
0
λ(y)dy,
∀x ∈[0, 1] ,
(64)
where T = {(x, y) ∈R2; 0 ≤y ≤x ≤1} is the same as
above. Furthermore, the observer gain p1(x) must be
p1(x) = k(1, x) .
(65)
As above, the resulting kernel PDE k(x, y) is bounded.
Theorem 4: (proven in [28], [30]) For every λ
∈
C1([0, 1]), the PDE problem (62), (63), (64) has a unique
C2(T ) solution with the bound
|k(x, y)| ≤¯λe2¯λx ,
(66)
for all x ∈[0, 1].
Additionally, recall the inverse backstepping kernel
l(x, y) ∈C2(T ) and inverse backstepping transformation
˜w(x, t)
=
˜u(x, t) +
Z 1
x
l(x, y)˜u(y, t)dy ,
(67)
which similarly satisfies (26). Then, the inverse kernel l(x, y)
is bounded as
|l(x, y)| ≤(¯λe2¯λ)e
¯λe2¯λ .
(68)
Now, we provide the error system convergence result for the
observer in (50), (51), (52).
Theorem 5: (proven in [13]) Let Assumption 2 hold.
Then for every U ∈C2(R+), u0, ˆu0 ∈H2(0, 1) for which
the compatibility conditions (47), (48), (53), (54) hold, the
observer (50), (51), (52) ensures that the error between its
state with initial condition (13) and the state of the plant (43),
(44), (45) with initial condition (6) satisfies for all t ≥0 the
exponential stability bound
∥u[t] −ˆu[t]∥≤Me−π2t∥u0 −ˆu0∥, ∀t ≥0 ,
(69)
where
M =

1 + ¯λe2¯λ 
1 + ¯λe2¯λ
e
¯λe2¯λ.
(70)
Furthermore, over a horizon of length T, the estimation error
obeys the relation for all t ≥T
∥u[t] −ˆu[t]∥
≤
Me−π2T ∥u[t −T] −ˆu[t −T]∥.(71)
Note, that the relation (71), which quantifies the progress
in the state estimation over the horizon [t −T, t], becomes a
contraction when
T > ln(M)
π2
,
(72)
for the overshoot coefficient M given by (70) and dependent
on the degree of plant’s instability ¯λ, i.e., when the horizon
is long enough in relation to the plant’s level of instability.
The next result is arguably the paper’s main result: the
explicit MHE for the reaction-diffusion PDE. We consider
the following explicit moving-horizon estimator
ˆu(x, t) = ˆw(x, t) −
Z 1
x
k(y, x) ˆw(y, t)dy
(73)

ˆw(x, t) =
∞
X
n=1
ϕn(x) exp(−n2π2T)
×
Z 1
0
ˆw(y, t −T)ϕn(y)dy
+
∞
X
n=1
Z 1
0
l(1, y)ϕn(y)dy

ϕn(x)
×
Z t
t−T
exp
 −n2π2(t −τ)

×
 Y (τ) −k(1, 1)U(τ)

dτ
−π
√
2
∞
X
n=1
n
 −1
nϕn(x)
×
Z t
t−T
exp
 −n2π2(t −τ)

U(τ)dτ
(74)
ˆw(x, t −T) =ˆu(x, t −T) +
Z 1
x
l(y, x)ˆu(y, t −T)dy (75)
where ϕn(x) =
√
2 sin(nπx) is the nth eigenfunction of
the underlying Sturm-Liouville operator for the heat equa-
tion with homogeneous Dirichlet boundary conditions. The
moving-horizon estimator is well defined for t ≥T and its
properties are given by the following theorem.
Theorem 6: Let Assumption 2 hold. Then for every U ∈
C2(R+), u0, ˆu0 ∈H2(0, 1) for which the compatibility
conditions in (47), (48), (53), (54) hold, the solution of (43),
(44), (45), (50), (51) with initial conditions (6), (13) satisfies
for all t ≥T (73) where ˆw is given by (74) and (75).
Proof: Note, from [11], the explicit form of (74), (75)
is equivalent to the system
ˆwt
=
ˆwxx + l(1, x)

ux(1, t) −k(1, 1)U(t)

(76)
ˆw(0, t)
=
0
(77)
ˆw(1, t)
=
U(t)
(78)
on the interval [t −T, t]. The system (76), (77), (78) is
transformed into the following observer system after the
application of the inverse backstepping transformation as
ˆw(x, t) = ˆu(x, t) +
Z 1
x
l(y, x)ˆu(y, t)dy
(79)
resulting in the observer (50), (51), (52) and obtaining the
asserted result.
IV. SIMULATION OF PARABOLIC MHE
We consider the system in (43), (44), (45) with λ(x) =
21 cos (5 cos−1(x)) as a Chebyshev polynomial. Addition-
ally, we have an input controller U(t) = 10 cos(2πt) +
7 sin(16t) which creates a slowly unstable system as shown
in the top row of Figure 1. For computation, we choose
a horizon window of T = 0.1 over 2 total seconds of
simulation time and set dt = 1e −6 and dx = 0.001 as
the temporal and spatial step sizes respectively. To simulate
the true PDE system. we employ a traditional forward Euler
finite difference scheme as in [13]. We truncate the infinite
series in (74) 4 terms which can be precomputed offline.
The only online computation are the discrete integrals in (74)
x
0.0
0.5
1.0
Time
0.0
0.5
1.0
1.5
2.0
u(x,t)
−10
0
10
20
x
0.0
0.5
1.0
Time
0.0
0.5
1.0
1.5
2.0
ˆu(x,t)
−10
0
10
20
0.0
0.5
1.0
1.5
2.0
Time
0
5
10
15
∥˜u[t]∥
Fig. 1.
The top row shows system (43), (44), (45) with u0(x) ≡10 and
input U(t) = 10 cos (2πt) + 7 sin (16t), which creates an unstable and
input-excited system. The second row represents the observer in the explicit
MHE form (73), (74), (75), with the sliding window as T = 0.1 and the
infinite series in (74) truncated to N = 4 terms. We add Gaussian measure-
ment noise on the boundary measurement in the form of Normal(0, 500).
The large noise is handled due the frequency of measurements available
when using a time step of 1e −6. In practice, one may not be able to
handle such a large deviation with a less rapid measurement frequency. The
initial condition for the observer for t ≤T is ˆu(x, θ) = 20, θ ∈[0, T].
The bottom row showcases the L2 error between the observer system and
the true system, ∥˜u[t]∥L2, which decreases precipitously at t = 0.1 (shown
by the grey dotted line), when the observer estimate begins to engage.
which depend on the horizon interval T and can be computed
recursively from the previous timestep.
In the second row of Figure 1, we chose for the observer
to have an initial condition of ˆu(x, θ) = 20, θ ∈[0, T]
for the initial horizon window and add Gaussian noise on
the measurement of ux(1, t). Despite the noise, the bottom
of Figure 1 shows the L2 error quickly decreases once the
estimate engages at t = 0.1 and that the observer is able to
emulate the true state with good accuracy. We note, without
noise, the observer would exponentially converge to the true
state immediately after engaging.
V. CONCLUSION
In this work, we present the first set of moving hori-
zon estimators for both first-order hyperbolic and reaction-
diffusion PDEs. The MHEs replace the need to employ

burdensome numerical schemes for solving PDE observers
with explicit forms. It is through the power of a backstepping
transformation that establishes the equivalence between a
complicated unsolvable PDE and a simple, explicitly solvable
(target) PDE. To the authors’ knowledge, this is the first work
in designing, for PDE systems, moving-horizon estimators,
which are well developed for discrete-time models, in the
context of MPC [22], [25]. It is still unclear what the full
potential of the PDE backstepping-based MHEs is, but one
suggestion, along the lines of [3], [4], [13], [27], is to
approximate the MHE operators with neural networks. The
finite-horizon nature of the MHEs makes them suitable for
a deep learning representation in which the input functions
(the measured output and control input signals, as well as
the state estimate from the start of the horizon) are functions
on constant intervals whose lengths are constant (and, in
particular, do not grow unbounded). The authors leave this
topic as an exciting possibility for future work.
