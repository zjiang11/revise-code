Threshold Constraints with Guarantees for Parity
Objectives in Markov Decision Processes∗
Raphaël Berthon1,2, Mickael Randour2, and Jean-François Raskin2
1
ENS Rennes, France
2
Computer Science Department, ULB - Université libre de Bruxelles, Belgium
Abstract
The beyond worst-case synthesis problem was introduced recently by Bruyère et al. [10]: it aims
at building system controllers that provide strict worst-case performance guarantees against an
antagonistic environment while ensuring higher expected performance against a stochastic model
of the environment. Our work extends the framework of [10] and follow-up papers, which focused
on quantitative objectives, by addressing the case of ω-regular conditions encoded as parity
objectives, a natural way to represent functional requirements of systems.
We focus on building strategies that satisfy a main parity objective on all plays, while ensuring
a secondary one with suﬃcient probability. This setting raises new challenges in comparison to
quantitative objectives, as one cannot easily mix diﬀerent strategies without endangering the
functional properties of the system. Interestingly, we establish that, for all variants of this pro-
blem, deciding the existence of a strategy lies in NP∩coNP, the same complexity class as classical
parity games. Hence, our framework provides additional modeling power while staying in the
same complexity class.
1
Introduction
Beyond worst-case synthesis. Two-player zero-sum games [18, 21] and Markov decision
processes (MDPs) [17, 4] are two popular frameworks to model decision making in adversarial
and uncertain environments respectively. In the former, a system controller (player 1) and its
environment (player 2) compete antagonistically, and synthesis aims at building strategies for
the controller that ensure a speciﬁed behavior against all possible strategies of the environment.
In the latter, the system is faced with a given stochastic model of its environment, and the
focus is on satisfying a given level of expected performance, or a speciﬁed behavior with a
suﬃcient probability.
The beyond worst-case synthesis framework was introduced by Bruyère et al. [10] to unite
both views: in this setting, we look for strategies that provide both strict worst-case guarantees
and a good level of performance against the stochastic model. Such requirements are natural
in many practical situations (e.g., see [9, 23] for applications to the shortest path problem).
The original paper [10] dealt with mean-payoﬀand shortest path quantitative settings.
Substantial follow-up work include, e.g., multi-dimensional extensions [14], optimization of
the expected mean-payoﬀunder hard Boolean constraints [1] or under energy constraints [7],
or integration of beyond worst-case concepts in the tool Uppaal [15].
Parity objectives. In this paper, we study the beyond worst-case problem for ω-regular
conditions encoded as parity objectives. Parity games have been under close scrutiny for a long
time both due to their importance (e.g., they subsume modal µ-calculus model checking [16])
∗Work partially supported by the ERC Starting grant 279499 (inVEST) and the ARC project “Non-Zero
Sum Game Graphs: Applications to Reactive Synthesis and Beyond” (Fédération Wallonie-Bruxelles).
J.-F. Raskin is Professeur Francqui de Recherche, M. Randour is an F.R.S.-FNRS postdoctoral researcher.
© Raphaël Berthon, Mickael Randour, and Jean-François Raskin;
licensed under Creative Commons License CC-BY
Leibniz International Proceedings in Informatics
Schloss Dagstuhl – Leibniz-Zentrum für Informatik, Dagstuhl Publishing, Germany
arXiv:1702.05472v2  [cs.LO]  27 Apr 2017

XX:2
Threshold Constraints with Guarantees for Parity Objectives in MDPs
and their intriguing complexity: they belong to the class of problems in NP ∩coNP [19]
and despite many eﬀorts (see [11] for pointers), whether they belong to P is still an open
question.
In the aforementioned papers dealing with beyond worst-case problems, the focus was
on quantitative objectives (e.g., mean-payoﬀ). While it is usually the case that qualitative
objectives, such as parity, are easier to deal with than quantitative ones, this is not true
in the setting considered in this paper. Indeed, in the context of quantitative objectives, it
is conceivable to alternate between two strategies along a play, such that one – eﬃcient –
strategy balances the performance loss due to playing the other – less eﬃcient – strategy
for a limited stretch of play inﬁnitely often. In the context of qualitative objectives, this is
no more possible in general, as one strategy may induce behaviors (such as invalidating the
parity condition inﬁnitely often) that can never be counteracted by the other one. Hence, in
comparison, we need to deﬁne more elaborate analysis techniques to detect when satisfying
both the worst-case and the probabilistic constraints with a single strategy is actually possible.
1, 0
1, 0
2, 0
2, 1
a
b
c
d
Figure 1 An MDP where player 1 can ensure p1 surely and p2 almost-surely.
▶Example 1. Consider the MDP of Figure 1. Circle states are owned by player 1 (system)
and square states are owned by player 2 (environment). In the stochastic model of the
environment, square states are probabilistic, and, when not speciﬁed, we consider the uniform
distribution over their successors. Each state is labelled with a name and two integers x, y
representing priorities deﬁned by two functions, p1 and p2. An inﬁnite path in the graph is
winning for player 1 and parity objective pi, i ∈{1, 2}, if the maximal priority seen inﬁnitely
often along the path for function pi is even. We claim that player 1 has a strategy λ to
ensure that (i) all plays consistent with λ satisfy p1 (i.e., p1 is surely satisﬁed) and (ii) the
probability measure induced by λ on this MDP ensures that p2 is satisﬁed with probability
one (i.e., almost-surely).
One such λ is as follows. It plays an inﬁnite sequence of rounds of ni steps, i ∈N. In
round i, in state a, the strategy chooses b for ni steps, such that the probability to reach c
during round i is larger than 1 −2−i (this is possible as at each step c is reached from b
with probability 1
2). If during round i, c is not reached (which can happen with a small
probability) then λ goes to d once. Then the next round i+1 is started. This inﬁnite-memory
strategy ensures both (i) and (ii). Indeed, it can be shown that the probability that λ plays d
inﬁnitely often is zero. Also, during each round, the maximal priority for p1 is guaranteed to
be even because if c is not visited, d is systematically played.
Finally, we can prove that player 1 needs inﬁnite memory to ensure p1 surely and p2
almost-surely, and also, that this is the best that player 1 can do here: he has no strategy to
enforce surely both p1 and p2 at the same time.
Outline and contributions. This paper extends a preceding conference version [5]. We
consider MDPs with two parity objectives (i.e., using diﬀerent priority functions). We study
the problem of deciding the existence of a strategy that ensures the ﬁrst parity objective
surely (i.e., on all plays) while yielding a probability at least equal to (resp. greater than)

R. Berthon, M. Randour, and J.-F. Raskin
XX:3
a given rational threshold to satisfy the second parity objective. In Section 2, we formally
deﬁne the framework and recall important results from the literature. In Section 3, as an
intermediate step, we solve the problem of ensuring the ﬁrst parity objective surely while
visiting a target set of states with suﬃcient probability: this tool will help us several times
later. We prove that the corresponding decision problem is in NP ∩coNP and at least as
hard as parity games, and that ﬁnite-memory strategies are suﬃcient. In Section 4, we solve
the problem for the two parity objectives, where the second one must hold almost-surely (i.e.,
with probability one). Our main tools are the novel notion of ultra-good end-components,
as well as the reachability problem solved in Section 3. We generalize our approach to
arbitrary probability thresholds in Section 5, in which we introduce the notion of very-good
end-components. In both the almost-sure and the arbitrary threshold cases, we prove that the
decision problem belongs to NP ∩coNP and is at least as hard as parity games. In contrast
to the reachability case, we prove that inﬁnite memory is in general necessary.
Additional related work. The beyond worst-case synthesis framework is an example of
the usefulness of non-zero-sum games for reactive synthesis. More can be found in these
expository papers [8, 22]. Other types of multi-objective speciﬁcations in stochastic models
have been considered in the literature: e.g., percentile queries generalize the classical threshold
probability problem to several dimensions [24].
In [3], Baier et al. study the quantitative analysis of MDPs under weak and strong
fairness constraints. They provide algorithms for computing the probability for ω-regular
properties in worst and best-case scenarios, when considering strategies that in addition
satisfy weak or strong fairness constraints almost-surely. In contrast, we are able to consider
similar objectives but for strategies that satisfy weak or strong fairness constraints surely,
i.e., with certainty and not only with probability one.
In [1], Almagor et al. consider the optimization of the expected mean-payoﬀunder
hard Boolean constraints in weighted MDPs. Our concept of ultra-good end-component
builds upon their notion of super-good end-component. A reduction to mean-payoﬀparity
games [12] is part of the identiﬁcation process of both types of end-components.
2
Preliminaries
2.1
Core concepts
Directed graphs. A directed graph is a pair G = (S, E) with S a set of vertices, called
states, and E ⊆S × S a set of directed edges. We focus here on ﬁnite graphs (i.e., |S| < ∞).
Given a state s ∈S, we denote by Succ(s) = {s′ ∈S | (s, s′) ∈E} the set of successors of s
by edges in E. We assume that graphs are non-blocking, i.e., for all s ∈S, Succ(s) ̸= ∅.
A play in G from an initial state s ∈S is an inﬁnite sequence of states π = s0s1s2 . . .
such that s0 = s and (si, si+1) ∈E for all i ≥0. The preﬁx up to the (n + 1)-th state of
π is the ﬁnite sequence π(0, n) = s0s1 . . . sn. We resp. denote the ﬁrst and last states of a
preﬁx ρ = s0s1 . . . sn by First(ρ) = s0 and Last(ρ) = sn. For a play π, we naturally extend
the notation to First(π). Finally, for i ∈N, π(i) = si, and for j > i, π(i, j) = si . . . sj.
The set of plays of G is denoted by Plays(G). The corresponding set of preﬁxes is denoted
by Pref(G). For a set of plays Π, we will also denote by Pref(Π) the set of all preﬁxes of
these plays. Given two preﬁxes ρ = s0 . . . sm and ρ′ = s′
0 . . . s′
n in Pref(G), we denote their
concatenation as ρ · ρ′ = s0 . . . sms′
0 . . . s′
n. This concatenation is not necessarily a valid preﬁx
of G. The same holds for a preﬁx concatenated with a play.
Probability distributions. Given a countable set A, a (rational) probability distribution

XX:4
Threshold Constraints with Guarantees for Parity Objectives in MDPs
on A is a function p: A →[0, 1] ∩Q such that P
a∈A p(a) = 1. We denote the set of
probability distributions on A by D(A). The support of the probability distribution p on A
is Supp(p) = {a ∈A | p(a) > 0}.
Markov decision processes. A (ﬁnite) Markov decision process (MDP) is a tuple M =
(G, S1, S2, δ) where (i) G = (S, E) is a directed graph; (ii) (S1, S2) is a partition of S into
states of player 1 (denoted by P1 and representing the system) and states of player 2 (denoted
by P2 and representing the stochastic environment); (iii) δ: S2 →D(S) is the transition
function that, given a stochastic state s ∈S2, deﬁnes the probability distribution δ(s) over
the successors of s, such that for all s ∈S2, Supp(δ(s)) = Succ(s) (i.e., all outgoing edges of
have non-zero probability to be taken). An MDP where for all s ∈S1, |Succ(s)| = 1 is a
fully-stochastic process called a Markov chain (MC).
A preﬁx ρ ∈Pref(M) belongs to Pi, i ∈{1, 2}, if Last(ρ) ∈Si. The set of preﬁxes that
belong to Pi is denoted by Prefi(M).
Strategies.
A strategy for P1 is a function λ: Pref1(M) →D(S), such that for all
ρ ∈Pref1(M), we have Supp(λ(ρ)) ⊆Succ(Last(ρ)). The set of all strategies in M is
denoted by Λ. Pure strategies have their support equal to a singleton for all preﬁxes. When
a strategy λ is pure, we simplify its notation and write λ(ρ) = s instead of λ(ρ)(s) = 1,
for any ρ ∈Pref1(M) and the unique state s ∈Supp(λ(ρ)). We sometimes mention that
a strategy is randomized to stress on the need for randomness in general (i.e., when pure
strategies do not suﬃce).
A strategy λ for P1 can be encoded by a stochastic state machine with outputs, called
stochastic Moore machine, M = (M, m0, αu, αn) where (i) M is a ﬁnite or inﬁnite set of
memory elements, (ii) m0 ∈M is the initial memory element, (iii) αu : M × S →M is the
update function, and (iv) αn : M × S1 →D(S) is the next-action function. If the MDP is in
s ∈S1 and m ∈M is the current memory element, then the strategy chooses s′, the next
state of the MDP, according to the probability distribution αn(m, s). When the MDP leaves
a state s ∈S, the memory is updated to αu(m, s). Hence updates are deterministic and
outputs are potentially stochastic. Formally, (M, m0, αu, αn) deﬁnes the strategy λ such
that λ(ρ · s) = αn(bαu(m0, ρ), s) for all ρ ∈Pref(M) and s ∈S1, where bαu extends αu to
sequences of states starting from m0 as expected. Note that pure strategies have deterministic
next-action functions. A strategy λ is ﬁnite-memory if |M| < ∞and memoryless if |M| = 1.
That is, it does not depend on the history but only on the current state of the MDP: in this
case, we have that λ: S1 →D(S). Finally, if the same strategy can be used regardless of the
initial state, we say that a uniform strategy exists.
A play π is said to be consistent with a strategy λ if for all n ≥0 such that π(n) ∈S1,
we have that π(n + 1) ∈Supp(λ(π(0, n)). This notion is deﬁned similarly for preﬁxes. We
denote by OutM(λ) ⊆Plays(G) the set of plays consistent with λ. We use OutM
s (λ) when
ﬁxing an initial state s.
Given a strategy λ in M and a preﬁx ρ ∈Pref1(M), we deﬁne the initialized strategy
λ[ρ] as follows:
∀ρ′ ∈Pref1(M), λ[ρ](ρ′) =
(
λ(ρ · ρ′) if ρ · ρ′ ∈Pref1(M),
λ(ρ′) otherwise.
Markov chain induced by a strategy. An MDP M = (G = (S, E), S1, S2, δ) and a
strategy λ for P1 encoded by the stochastic Moore machine M = (M, m0, αu, αn) determine
an MC C = (G′, δ′) on the state space S′ = S × M as follows. For any pair of states
s′
1 = (s1, m1) and s′
2 = (s2, m2) in S′, δ′(s′
1)(s′
2) = αn(s1, m1)(s2) if m2 = αu(s1, m1) and 0

R. Berthon, M. Randour, and J.-F. Raskin
XX:5
otherwise. Observe that given a ﬁnite MDP, a ﬁnite-memory (resp. inﬁnite-memory) strategy
induces a ﬁnite (resp. inﬁnite) MC. We deﬁne plays and preﬁxes of an induced MC as before,
considering only transitions with non-zero probability.
Let M = (G, S1, S2, δ) be an MDP, s ∈S an initial state and λ a strategy of P1 encoded
by M = (M, m0, αu, αn). Let A ⊆Plays(G). We denote by Pλ
M,s[A] the probability of the
plays of the induced MC C (starting in (s, m0)) whose projection (deﬁned by removing
the memory elements) to M is in A, i.e., the probability of event A when M is executed
with initial state s and strategy λ. Note that every Borel set A has a uniquely deﬁned
probability [25] (Carathéodory’s extension theorem induces a unique probability measure on
the Borel σ-algebra over Plays(G)). Throughout the paper, we may also write Pλ
M,s[A] for
A a set of preﬁxes: this is an abuse of notation meaning that we consider the probability of
the set of plays A′ containing all consistent continuations of preﬁxes in A (i.e., we consider
the probability of the cylinders deﬁned by those preﬁxes).
Two-player games. Observe that in MDPs, we call both P1 and P2 players, though P2
does not have controllable choices. Indeed, an MDP can be seen as a game where P1 plays
against a stochastic adversary P2 using the ﬁxed memoryless randomized strategy δ: they
are sometimes called 1 1
2-player games. Throughout this paper, we also need to consider real
two-player games, denoted G = (G = (S, E), S1, S2). In those games, both players control
their actions using strategies: we extend all previous notions (e.g., strategies) to P2 and
games. In particular, any MDP M = (G, S1, S2, δ) can be seen as a two-player game by
forgetting about the transition function δ and letting P2 pick the strategy of its choice.
Mixing both interpretations (stochastic and antagonistic versions of P2) is crucial in the
beyond worst-case framework that we study here.
Objectives. Given an MDP M = (G, S1, S1, δ), an objective is a set of plays A ⊆Plays(G).
We consider two classical objectives from the literature. Both deﬁne measurable events.
To deﬁne them, we introduce the following notation: given a play π ∈Plays(G), let
inf(π) = {s ∈S | ∀i ≥0, ∃j ≥i, π(j) = s} be the set of states seen inﬁnitely often along π.
Reachability. Given a target T ⊆S, the reachability objective asks for plays that visit T:
Reach(T) = {π ∈Plays(G) | ∃n ≥0, π(n) ∈T}. We later use the LTL notation ♦T to
denote the event Reach(T).
Parity. Let p: S →{1, 2, . . . , d} be a priority function that maps each state to an
integer priority, where d ≤|S| + 1 (w.l.o.g.). The parity objective asks that, among the
priorities seen inﬁnitely often, the maximal one be even: Parity(p) = {π ∈Plays(G) |
max
s∈inf(π) p(s) is even}. We later simply use p to denote the event Parity(p).
2.2
Technical tools
For the following deﬁnitions, let M = (G = (S, E), S1, S2, δ) be an MDP.
Attractors. The attractor for Pi, i ∈{1, 2}, of a target set of states T, denoted Attri(T) is
computed as the ﬁxed point of the sequence Attrn+1
i
(T) = Attrn
i (T) ∪{s ∈Si | Succ(s) ∩
Attrn
i (T) ̸= ∅} ∪{s ∈S3−i | Succ(s) ⊆Attrn
i (T)} with Attr0
i (T) = T. It contains all states
from which Pi can force a visit of T. Observe that this notion interprets P2 as an antagonistic
adversary, i.e., having the choice of his strategy.
Traps, end-components and sub-MDPs. A trap for Pi, i ∈{1, 2}, is a set R ⊆S that
Pi cannot leave: ∀s ∈R ∩Si, Succ(s) ⊆R and ∀s ∈R ∩S3−i, Succ(s) ∩R ̸= ∅.
An end-component (EC) of M is a trap C for P2 that is strongly connected, i.e., for
any two states s, s′ ∈C, there exists a path from s to s′ that stays in C. It is well-known

XX:6
Threshold Constraints with Guarantees for Parity Objectives in MDPs
that inside an EC C, P1 can force the visit of any state s ∈C with probability 1 (that is,
when P2 is seen as stochastic and obeys the strategy δ), see e.g., [4]. The union of two
ECs with non-empty intersection is an EC. An EC C is thus maximal if, for every EC C′,
C′ ⊆C ∨C′ ∩C = ∅.
Given an EC C ⊆S of M, we denote by M⇂C the sub-MDP that is naturally deﬁned
by M⇂C = (G′ = (C, E ∩C × C), S′
1 = S1 ∩C, S′
2 = S2 ∩C, δ′), where δ′ : S′
2 →D(C) is
simply the restriction of δ to the domain C. Note that M⇂C is a well-deﬁned MDP: it has
no deadlock since C is strongly connected and in all stochastic states s, the support of δ′(s)
is included in C (as C was an EC in M).
Technical lemmas. Before turning to the beyond worst-case problem, we recall some
classical results about MDPs that will be useful later on.
▶Lemma 2 (Optimal reachability [4]). Given an MDP M = (G = (S, E), S1, S2, δ) and
a target set T ⊆S, we can compute for each state s ∈S the maximal probability v∗
s =
supλ∈Λ Pλ
M,s[♦T] to reach T, in polynomial time. There is an optimal uniform pure memo-
ryless strategy λ∗that enforces v∗
s from all s ∈S.
Fix s ∈S and c ∈Q such that c < v∗
s. Then there exists k ∈N such that by playing λ∗
from s for k steps, we reach T with probability larger than c.
We say that λ∗is optimal as no strategy can achieve a probability strictly higher than
v∗
s, for any s ∈S.
▶Lemma 3 (Long-run appearance of ECs [4]). Given an MDP M = (G = (S, E), S1, S2, δ)
and E = {C ⊆S | C is an EC in M} the set of all its end-components, for any strategy λ
of P1 and any state s ∈S, the following holds:
Pλ
M,s

{π ∈OutM(λ) | inf(π) ∈E}

= 1.
2.3
The beyond worst-case framework
Events and probabilistic operators. Consider an MDP M = (G = (S, E), S1, S2, δ).
Recall that we have deﬁned two types of measurable events (speciﬁc subsets of Plays(G))
with respective notations ♦T for T ⊆S (reachability), and p for p: S →{1, . . . , d} a priority
function (parity). We deﬁne three operators to reason about the probabilities of these events:
S, P∼c, and AS. Given an event A and a state s, they are used as follows:
A is sure from s, denoted s |= S(A), if there exists a strategy λ of P1 such that
OutM
s (λ) ⊆A. Here probabilities are ignored and we consider P2 as antagonistic.
A holds with probability at least equal to (resp. greater than) c ∈Q from s, denoted
s |= P≥c(A) (resp. s |= P>c(A)) if there exists λ such that Pλ
M,s[A] ≥c (resp. > c).
A is almost-sure from s, denoted s |= AS(A), if there exists λ such that Pλ
M,s[A] = 1.
For any operator O, we say that such a λ is a witness strategy for s |= O(A) and we write
s, λ |= O(A) to denote it. We will also consider combinations of the type s |= O1(A1) ∧O2(A2)
for two operators and events: in this case, we require that the same strategy be a witness for
both conjuncts, i.e., that there exists λ such that s, λ |= O1(A1) and s, λ |= O2(A2). Finally,
we will sometimes use diﬀerent MDPs, in which case we add the considered MDP M as a
subscript on |=, e.g., s |=M O(A). We drop this subscript when the context is clear.
Beyond worst-case problems. Let M = (G = (S, E), S1, S2, δ) be an MDP, s ∈S be
an initial state, and p1, p2 be two priority functions on M. Throughout this paper, we
provide algorithms to decide the existence of a witness strategy — and synthesize it — for

R. Berthon, M. Randour, and J.-F. Raskin
XX:7
the following formulae combining worst-case and probabilistic guarantees regarding parity
objectives:
1. s |= S(p1) ∧AS(p2) — this is the canonical beyond worst-case (BWC) problem;
2. s |= S(p1) ∧P∼c(p2) for ∼∈{>, ≥} and c ∈Q ∩[0, 1).
3
Reachability under parity constraints
In this section, we consider the synthesis of strategies that enforce to reach a target set
of states T ⊆S with high probability while enforcing a parity condition surely. Using the
previously deﬁned notations, we consider two variants of the problem, given a state s ∈S
and a priority function p: S →{1, . . . d}:
1. s |= S(p) ∧AS(♦T),
2. s |= S(p) ∧P∼c(♦T) for ∼∈{>, ≥} and c ∈Q ∩[0, 1).
3.1
Almost-sure reachability
▶Theorem 4. Given an MDP M = (G = (S, E), S1, S2, δ), a state s0 ∈S, a priority
function p: S →{1, . . . , d}, and a target set of states T ⊆S, it can be decided in NP ∩coNP
if s0 |= S(p) ∧AS(♦T). If the answer is Yes, then there exists a ﬁnite-memory witness
strategy. This decision problem is at least as hard as solving parity games.
Proof. A slight variant of this result has been established inside the proof of [2, Lemma 3]
(extended version of [1]), where it is asked that the parity condition holds not necessarily on
all consistent plays but only on all plays that do not reach the target set T. We reduce our
problem to the latter as follows. First, we compute all states in S from which P1 can enforce
p surely. This boils down to solving a classical parity game [19]. We remove all states that
are not surely winning for p in M as entering one of those states implies that the parity
condition will be violated on some consistent play. Then we apply the approach described in
Theorem 27 (Appendix A) to the resulting MDP: it is based on a reduction to a particular
parity-Büchi game based on the MDP. If the answer is Yes for s0, we answer Yes to our
problem, otherwise we answer No. We claim that this reduction is correct. First, it is clear
that satisfying their constraint is a necessary condition to satisfy ours. Now, if there is a
strategy λ that ensures to reach T almost-surely in the reduced MDP and enforces p on all
the consistent plays that never reach T, then we construct a strategy λ′ that plays as λ up
to reaching T (if ever) and then switches to the strategy λp that enforces p surely. This new
strategy is a witness for our problem since the parity objective is preﬁx-independent. That
is, s0, λ′ |= S(p) ∧AS(♦T). Since both solving the parity game and solving the parity-Büchi
game from Theorem 27 are in NP ∩coNP, we conclude that our decision problem lies in
PNP∩coNP = NP ∩coNP [6]. Furthermore, our problem clearly generalizes parity games: it
suﬃces to ﬁx T = S to trivially ensure the second conjunct and obtain a classical parity
game. Hence, the hardness follows.
Finally, since λp is memoryless w.l.o.g. and the strategy λ obtained via Theorem 27 is
also ﬁnite-memory (due to the reduction to a parity-Büchi game), we have that our witness
strategy λ′ is also ﬁnite-memory.
◀
3.2
Reachability with threshold probability
Optimal reachability strategies. We ﬁrst study strategies that maximize the probability
of reaching a target set of states T ⊆S in an MDP M. Recall that Lemma 2 states the

XX:8
Threshold Constraints with Guarantees for Parity Objectives in MDPs
existence of an optimal uniform pure memoryless strategy λ∗that enforces v∗
s from all s ∈S,
where v∗
s is the maximal probability to reach T that can be achieved by P1 in s. We deﬁne
the set E¬opt = {(s, s′) ∈E | s ∈S1 ∧v∗
s > v∗
s′} that contains all edges that are non-optimal
choices for P1 in the sense that they result in a strict decrease of the probability to reach T.
We show that playing, for a ﬁnite number of steps, edges that are optimal for reachability
(i.e., edges in Eopt = E \ E¬opt), and then switching to an optimal strategy to reach T, like
λ∗, produces an optimal strategy too.
▶Lemma 5. Let λ∗be an optimal uniform pure memoryless strategy in M to reach T, from
all states in S. If λ is a strategy that plays only edges in Eopt for m steps, for m ∈N, and
then switches to λ∗, then λ is also optimal to reach T from all states in S.
Proof. We prove this lemma by induction. For m = 0, we only play λ∗, thus λ is trivially
optimal. Now, assume the property holds up to m−1 steps, for m > 0, i.e., that all strategies
playing in Eopt for m −1 steps, and then playing λ∗, are optimal. Consider a strategy λm
that does this for m steps now. Strategy λm thus chooses an edge in Eopt, and then switches
to a strategy λm−1 that satisﬁes the induction hypothesis. So for any state s0 ∈S1 \ T (for
s0 ∈T, we have probability one whatever the strategy), we have that:
Pλm
M,s0 [♦T] =
X
s∈S
h
Pλm
M,s0 [{π | π(1) = s}] · Pλm−1
M,s [♦T]
i
=
X
s∈S
h
Pλm
M,s0 [{π | π(1) = s}] · v∗
s
i
(by induction hypothesis)
≥
X
s∈S
h
Pλm
M,s0 [{π | π(1) = s}] · v∗
s0
i
= v∗
s0.
(by deﬁnition of Eopt)
Hence, we obtain that λm is also optimal from s0, which concludes our proof.
◀
Solving the problem. We now turn to the problem s0 |= S(p) ∧P∼c(♦T).
▶Theorem 6. Given an MDP M = (G = (S, E), S1, S2, δ), a state s0 ∈S, a priority
function p: S →{1, . . . , d}, a target set of states T ⊆S, and a probability threshold
c ∈[0, 1) ∩Q, it can be decided in NP ∩coNP if s0 |= S(p) ∧P∼c(♦T) for ∼∈{>, ≥}. If the
answer is Yes, then there exists a ﬁnite-memory witness strategy. This decision problem is
at least as hard as solving parity games.
Proof. First, we remove from M the states from which P1 cannot enforce S(p). In particular
if s0 ̸|= S(p), the answer is No. Remember that it can be done by computing the winning
states in M seen as a parity game: a problem that belongs to NP∩coNP [19]. We now reason
on the MDP Mw where those losing states have been removed: it is well-deﬁned as the set of
winning states for P1 in M is a trap for P2. We note λp a uniform pure memoryless strategy
that ensures S(p) from any state of Mw. The proof of this theorem is easy for the case >,
and more involved for ≥. We start with the easy case.
The algorithm for the case > c is as follows. First, we compute the maximal probability
v∗
s0 to reach T, as in Lemma 2. This takes polynomial time. If v∗
s0 ≤c, then the answer is
clearly No. Otherwise, we claim that the answer is Yes. Let λ∗be an optimal uniform pure
memoryless strategy enforcing probability v∗
s0 from s0. We construct a witness strategy λ for
s0 |= S(p) ∧P>c(♦T) from λ∗and λp as follows. First, starting in s0, the strategy λ plays
as λ∗for k steps where k is taken as in Lemma 2: hence the probability to reach T after k
steps is strictly greater than c, which implies that s0, λ |= P>c(♦T). Then, λ switches to λp.
Since the parity objective is preﬁx-independent, we have that s0, λ |= S(p), and we are done.

R. Berthon, M. Randour, and J.-F. Raskin
XX:9
Overall, our procedure lies in PNP∩coNP = NP ∩coNP [6]. Also observe that our strategy λ is
ﬁnite-memory since λ∗and λp are memoryless and k is ﬁnite thanks to Lemma 2.
We now turn to the case ≥c. The algorithm, as in the previous case, ﬁrst computes the
maximal probability v∗
s0 to reach T, in polynomial time. If v∗
s0 > c, then we answer Yes as
we can apply the same reasoning as in the previous case. If v∗
s0 < c, then we trivially answer
No. The more involved case is when v∗
s0 = c. In this case, we must verify that probability c
is still achievable if, in addition, it is required to enforce S(p). To answer this question, we
modify Mw and we reduce our problem to the almost-sure case of Theorem 4. We present
the details in the following. Intuitively, we construct the MDP M′ as follows:
we enrich states with one bit that records if the set T has been visited or not;
while T has not been visited, we suppress all edges controlled by P1 that are not optimal
for reachability, i.e., all edges in E¬opt;
while T has not been visited, we delete all states that cannot reach T (i.e., all states that
do not have a path to T in the underlying graph) and normalize the probability of the
edges that survive this deletion: we note the set of states to be removed by Sw
□¬T .
Formally, the new MDP M′ is deﬁned as follows:
S′ = (Sw × {0, 1}) \ (Sw
□¬T × {0}), S′
1 = S′ ∩(Sw
1 × {0, 1}), and S′
2 = S′ ∩(Sw
2 × {0, 1});
s′
0 is equal to (s0, 0) if s0 ̸∈T, and to (s0, 1) if s0 ∈T;
E′ = {((s, 0), (s′, 0)) | (s, s′) ∈Eopt ∧s′ ̸∈T} ∪{((s, 0), (s′, 1)) | (s, s′) ∈Eopt ∧s′ ∈T}
∪{((s, 1), (s′, 1)) | (s, s′) ∈E};
the probability distributions are then deﬁned as:
for all (s, 0) ∈S′
2, and ((s, 0), (s′, b)) ∈E′, δ′((s, 0))((s′, b)) = δ(s)(s′)
Xs
where we deﬁne
Xs =
X
t∈Sw|((s,0),(t,b))∈E′
δ(s)(t),
for all (s, 1) ∈S′
2, and ((s, 1), (s′, 1)) ∈E′, δ′((s, 1))((s′, 1)) = δ(s)(s′);
the priority function p′ : S′ →{1, . . . , d} is such that for all (s, b) ∈S′, p′((s, b)) = p(s).
Note that each preﬁx (resp. play) ρ′ in M′ is naturally mapped to a unique preﬁx γ(ρ′)
in Mw, obtained by projecting away the additional bit of information (about visiting the
target T). Conversely, each preﬁx (resp. play) ρ in Mw that does not use edges in E¬opt
and states in Sw
□¬T is naturally mapped to a unique preﬁx α(ρ) in M′. Those mappings are
such that for all ρ′ in M′, ρ′ = α(γ(ρ′)).
Finally, we claim that s0 |= S(p) ∧P≥c(♦T) in Mw if, and only if, s′
0 |= S(p) ∧AS(♦T ′) in
M′, where T ′ = T × {1}. For the sake of readability, we postpone the correctness of this
reduction to Lemma 7. Given that the latter problem was shown to be in NP ∩coNP in
Theorem 4, and that M′ is only polynomially larger than Mw (and thus M), we obtain the
claimed complexity for the problem, using PNP∩coNP = NP ∩coNP [6]. The proof of Lemma 7
also implies that the witness strategy can be ﬁnite-memory.
Again, problem s0 |= S(p) ∧P∼c(♦T) trivially generalizes parity games, by taking T = S,
which concludes our proof.
◀
▶Lemma 7 (Correctness of the reduction used for Theorem 6). The following equivalence
holds:
s′
0 |=M′ S(p) ∧AS(♦T ′) ⇐⇒s0 |=Mw S(p) ∧P≥c(♦T).

XX:10
Threshold Constraints with Guarantees for Parity Objectives in MDPs
Proof. We start with the left-to-right implication. Assume that there exists a witness strategy
λ′ for P1 in M′ such that s′
0, λ′ |=M′ S(p) ∧AS(♦T ′). By Theorem 4, we may assume λ′ to
be ﬁnite-memory. From λ′, we build a strategy λ in Mw as follows:
for all ρ ∈Pref(G) that does not visit T, only uses edges in Eopt and does not contain
states in Sw
□¬T , λ(ρ) = γ(λ′(α(ρ))), i.e., λ mimics λ′,
for all other ρ ∈Pref(G), the strategy plays as λp, the memoryless strategy ensuring S(p)
in Mw.
Observe that λ is a ﬁnite-memory strategy: the conditions to check can be encoded using a
ﬁnite number of memory elements in addition to the ﬁnite Moore machine representing λ′.
We now claim that s0, λ |=Mw S(p) ∧P≥c(♦T).
First, consider the conjunct S(p). Along any play π ∈Plays(G) starting in s0, either
(i) λ always plays as λ′, or (ii) it eventually switches to λp. In case (i), π has a unique
corresponding play in M′, π′ = α(π), and π′ is consistent with λ′. Since λ′ ensures S(p)
by hypothesis and the priority function is the same in M′ and Mw, π also satisﬁes the
parity objective. In case (ii), π also satisﬁes the parity objective by deﬁnition of λp and
preﬁx-independence of parity. Hence, we have that s0, λ |=Mw S(p).
It remains to prove that s0, λ |=Mw P≥c(♦T). We prove it by contradiction. Assume that
Pλ
Mw,s0 [♦T] < c, or equivalently, that Pλ
Mw,s0 [□¬T] > 1 −c, using the classical always LTL
operator. This means that, because of the strict inequality > 1 −c, there exists m in N such
that the set
BadPref =
n
ρ ∈Pref(G) | ρ consistent with λ ∧|ρ| = m + 1
∧∀continuation π of ρ consistent with λ, π ̸|= ♦T
o
satisﬁes P
ρ∈BadPref Pλ
Mw,s0 [ρ] > 1 −c. Note that this sum is indeed a lower bound on the
probability of □¬T as all preﬁxes of BadPref deﬁne disjoint cylinder sets.
Now, we claim that for all ρ ∈BadPref, we have that Last(ρ) ∈Sw
□¬T . To prove it, recall
the deﬁnition of λ. If it were not true, then λ would have mimicked λ′ all along ρ (i.e., not
switching to λp). But since all continuations of ρ consistent with λ in Mw never reach T, this
would also hold for continuations of α(ρ) consistent with λ′ in M′. Hence preﬁx α(ρ) would
prove that Pλ′
M′,s′
0 [♦T ′] < 1, which contradicts the hypothesis stating that s′
0, λ′ |= AS(♦T ′).
Hence, we can now conﬁrm that for all ρ ∈BadPref, we have that Last(ρ) ∈Sw
□¬T .
To pursue, we deﬁne a new strategy on Mw, λr: it plays as λ, which only using edges in
Eopt, for m steps (m as deﬁned above), then switches to λ∗, the optimal strategy to reach T
in Mw, which ensures probability v∗
s0 = c of reaching T from s0 (see Proof of Theorem 6).
By Lemma 5, we have that λr is still optimal, hence that Pλr
Mw,s0 [♦T] = c. But by the
previous arguments, we also have that
Pλr
Mw,s0 [□¬T] ≥
X
ρ∈BadPref
Pλ
Mw,s0 [ρ] > 1 −c,
hence that Pλr
Mw,s0 [♦T] < c.
We have obtained the contradiction, hence proving that
s0, λ |=Mw P≥c(♦T) and concluding the proof of the left-to-right implication.
We now prove the right-to-left implication. Assume that there exists a witness strategy
λ for P1 in Mw such that s0, λ |=Mw S(p) ∧P≥c(♦T). Recall that the maximal achievable
probability to reach T in Mw is v∗
s0 = c. Hence, it can be shown that λ must restrict its
choices to Eopt before reaching T, otherwise it would not be possible to ensure P≥c(♦T).
Similarly, we can assume w.l.o.g. that λ switches to λp, the strategy ensuring S(p), after T

R. Berthon, M. Randour, and J.-F. Raskin
XX:11
or Sw
□¬T is reached: if not the case, we can build a strategy with those properties based on λ
that is still a witness for the considered property. We ﬁrst study λ′, which is simply λ played
on M′, and show that it is a witness for s′
0 |=M′ S(p).
To prove that s′
0, λ′ |=M′ S(p), observe that on any play π′ consistent with λ′, we either
(i) reach T ′ and switch to λp (more precisely, its translation over M′), (ii) do not reach T ′ but
neither reach Sw
□¬T (by construction), hence keep playing like λ. In case (i), and since all edges
from the initial MDP Mw are present after reaching T ′, λp can be implemented faithfully
and any consistent play π′ will satisfy the parity objective thanks to preﬁx-independence.
In case (ii), any consistent play π′ of λ′ has a unique corresponding play π = γ(π′) in Mw
that is consistent with λ, hence satisﬁes the parity objective since λ ensures S(p). We thus
conclude that λ′ is a witness for s′
0 |=M′ S(p).
We now show that T ′ is dense in λ′, i.e., that any preﬁx ρ′ consistent with λ′ has a
continuation π′ such that π′ |= ♦T ′. In other words, seeing strategy λ′ as an execution tree,
there is no subtree such that all branches in this subtree never visit T ′. Formally, this is
saying that the following set must be empty:
BadPref′ =
n
ρ′ ∈Pref(G′) | ρ′ consistent with λ′
∧∀continuation π′ of ρ′ consistent with λ′, π′ ̸|= ♦T ′o
.
We prove its emptiness by contradiction. Assume there exists a preﬁx ρ′ in BadPref′. Since
it is a preﬁx in M′, it implies by construction that Last(γ(ρ′)) ̸∈Sw
□¬T in Mw, hence that
some paths reach T ′ from Last(γ(ρ′)) in the underlying graph. Hence, we can deﬁne a new
strategy λr in Mw that plays exactly as λ except after preﬁx γ(ρ′), where it switches to
the optimal reachability strategy λ∗: the probability to reach T after γ(ρ′) under λr is thus
strictly positive, say equal to q > 0. We know have that
Pλr
Mw,s0 [♦T] = c + Pλr
Mw,s0 [γ(ρ′)] · q > c
which is in contradiction with c = v∗
s0 being the maximal achievable probability. Hence, we
deduce that BadPref′ is indeed empty and that T ′ is dense for λ′. Now, using the reduction
to a parity-Büchi game which underlies Theorem 4, we deduce from the density argument
presented in Appendix A that we can build λ′′ from λ′ such that s′
0, λ′′ |=M′ S(p) ∧AS(♦T ′).
This concludes the proof of the right-to-left implication and of this lemma.
◀
4
Almost-sure parity under parity constraints
Overview and key lemma. Here, we study strategies that enforce a parity objective p1
surely (i.e., on all consistent plays) and a parity objective p2 almost-surely (i.e., with
probability one). Formally, we look at the problem s |= S(p1) ∧AS(p2). The cornerstone
of our approach is the notion of ultra-good end-component. Throughout this section, we
consider an MDP M = (G = (S, E), S1, S1, δ) with two priority functions p1 and p2.
▶Deﬁnition 8. An end-component C of M is ultra-good (UGEC) if in the sub-MDP M⇂C,
the following two properties hold:
(1U) ∀s ∈C, s |=M⇂C S(p1) ∧AS(♦Cmax
even(p1)), where
Cmax
even(pi) =

s ∈C | (pi(s) is even) ∧(∀s′ ∈C, pi(s′) is odd
=⇒pi(s′) < pi(s))
	
contains the states with even priorities that are larger than any odd priority in C (this
set can be empty for arbitrary ECs but needs to be non-empty for UGECs);

XX:12
Threshold Constraints with Guarantees for Parity Objectives in MDPs
(2U) ∀s ∈C, s |=M⇂C AS(p1) ∧AS(p2), or equivalently, s |=M⇂C AS(p1 ∩p2).
We introduce the following notations:
UGEC(M) is the set of all UGECs of M,
U = ∪U∈UGEC(M)U is the set of states that belong to a UGEC in M.
Intuitively, within a UGEC, P1 has a strategy to almost-surely visit Cmax
even(p1) while
guaranteeing S(p1), and he also has a (generally diﬀerent) strategy that almost-surely ensures
both parity objectives. Figure 2 gives an example of UGEC: here, the strategy ensuring (1U)
is to go to d, and the strategy ensuring (2U) is to go to b.
1, 0
1, 0
2, 0
4, 1
3, 1
a
b
c
d
e
Figure 2 This MDP is a UGEC: going to d satisﬁes (1U), whereas going to b satisﬁes (2U).
The notion of UGEC strengthens the concept of super-good EC that was introduced in [1]:
essentially, the super-good ECs are exactly the ECs satisfying (1U). Thus, every UGEC is a
super-good EC, but the converse is false.
The central lemma underpinning our approach is the following.
▶Lemma 9. The following equivalence holds:
s0 |= S(p1) ∧AS(♦U) ⇐⇒s0 |= S(p1) ∧AS(p2).
Essentially, this lemma permits to reduce the problem under study to the one treated in
Theorem 4, provided that we are able to compute U, the set of states appearing in a UGEC.
The rest of this section is dedicated to the proof of this lemma and its consequences: in
Sect. 4.1, we prove the left-to-right implication, in Sect. 4.2, we prove its converse, and ﬁnally,
in Sect. 4.3, we establish an algorithm to solve problem s0 |= S(p1) ∧AS(p2) based on this
reduction and we study its complexity.
4.1
Left-to-right implication (suﬃcient condition)
Available strategies in UGECs. We ﬁrst focus on witness strategies for conditions (1U)
and (2U) of Deﬁnition 8. For (1U), it was shown in the proof of [2, Lemma 3] (extended
version of [1]) that deciding if the condition holds is in NP ∩coNP and that uniform ﬁnite-
memory witness strategies exist. For (2U), we establish the following lemma.
▶Lemma 10. Let C be an EC of M. The following assertions hold.
1. It can be decided in polynomial time if condition (2U) holds.
2. If the answer is Yes, then there exists a (uniform randomized) memoryless witness
strategy λ2,C and a sub-EC D ⊆C such that Dmax
even(p1) ̸= ∅, Dmax
even(p2) ̸= ∅, and for all
s ∈C,
Pλ2,C
M⇂C,s

{π ∈OutM⇂C(λ2,C) | inf(π) = D}

= 1.
3. Furthermore, λ2,C satisﬁes the following property: ∀s ∈C, ∀ε > 0, ∃n ∈N such that
Pλ2,C
M⇂C,s

π ∈OutM⇂C(λ2,C) | ∃i, 0 ≤i ≤n, π(i) ∈Dmax
even(p1)
	
≥1 −ε.

R. Berthon, M. Randour, and J.-F. Raskin
XX:13
Proof. We ﬁrst prove Point 2.
Let C be an EC that satisﬁes condition (2U), and a
corresponding witness strategy λ (that could use memory in full generality). By Lemma 3,
we know that for any state s ∈C,
Pλ
M⇂C,s

{π ∈OutM⇂C(λ) | inf(π) ∈E}

= 1,
where E is the set of ECs of M⇂C. We claim that for any D ∈E for which
Pλ
M⇂C,s

{π ∈OutM⇂C(λ) | inf(π) = D}

> 0,
we necessarily have that Dmax
even(p1) ̸= ∅and Dmax
even(p2) ̸= ∅. Indeed, assume this is false
for pi.
Then, with probability strictly greater than zero, λ induces plays π such that
maxs′∈inf(π) pi(s′) is odd (as the maximal priority in inf(π) = D is odd). This contradicts
the fact that λ is a witness strategy for s |=M⇂C AS(p1) ∧AS(p2) (condition (2U)).
Assume that λ is a witness strategy, potentially leading to several ECs Dj satisfying
the above property with non-zero probability, and potentially using memory. Since each
Dj is included in the EC C, it is easy to conceive a witness strategy λ2,C that reaches and
stays in only one of those ECs with probability one (we call it D from now on) and without
using any memory: indeed, each state of an EC can be almost-surely visited using a uniform
randomized memoryless strategy [4].
Second, to prove Point 3, it suﬃces to use Lemma 2. See that λ2,C reaches Dmax
even(p1)
almost-surely from any state s ∈C. By taking n = k for the k given in Lemma 2 for c = 1−ε,
we obtain the desired property.
Finally, consider Point 1. We claim that (i) the existence of a sub-EC D such that
Dmax
even(p1) ̸= ∅and Dmax
even(p2) ̸= ∅is not only necessary but also suﬃcient to satisfy condition
(2U), and (ii) the existence of such a set can be decided in polynomial time.
For (i), as discussed above, for any sub-EC of C, in particular for D, we can build a
uniform randomized memoryless strategy λ such that
Pλ
M⇂C,s

{π ∈OutM⇂C(λ) | inf(π) = D}

= 1.
By hypothesis on D, we thus have that p1 and p2 are almost-surely satisﬁed by λ, hence that
λ is a witness for s |=M⇂C AS(p1) ∧AS(p2) and that condition (2U) holds in C.
It remains to check the existence of such a set D ⊆C in polynomial time. First, we check
if Cmax
even(p1) ̸= ∅and Cmax
even(p2) ̸= ∅. If this holds, then D = C and the answer is Yes (it
takes linear time obviously). If it does not hold, then we compute the sets
Cmax
odd (pi) =

s ∈C | (pi(s) is odd) ∧(∀s′ ∈C, pi(s′) is even
=⇒pi(s′) < pi(s))
	
and we iterate this procedure in the sub-EC C′ ⊂C deﬁned as
C′ = C \ Attr2
 Cmax
odd (p1) ∪Cmax
odd (p2)

.
It is easy to see that a suitable D exists if and only if this procedure stops before C′ = ∅. In
addition, this procedure takes at most |C| iterations (as we remove at least one state at each
step) and each iteration takes linear time. This implies Point 3 and concludes our proof.
◀
Inside UGECs. We will now prove that inside any UGEC, there is a strategy for the
beyond worst-case problem S(p1) ∧AS(p2). From now on, let C be a UGEC of M, λ1,C
be a uniform ﬁnite-memory witness strategy for condition (1U) of Deﬁnition 8, and λ2,C
be a uniform randomized memoryless one for condition (2U), additionally satisfying the
properties of Lemma 10. We build a strategy λC based on λ1,C and λ2,C.

XX:14
Threshold Constraints with Guarantees for Parity Objectives in MDPs
▶Deﬁnition 11. Let C ∈UGEC(M). Let (ni)i∈N be a sequence of naturals ni such that
Pλ2,C
M⇂C,s

π ∈OutM⇂C(λ2,C) | ∃i, 0 ≤i ≤ni, π(i) ∈Dmax
even(p1)
	
≥1 −2−i,
whose existence is guaranteed by Lemma 10. We build the beyond worst-case strategy λC as
follows, starting with i = 0.
a) Play λ2,C for ni steps. Then i = i + 1 and go to b).
b) If Dmax
even(p1) was visited in phase a), then go to a).
Else, play λ1,C until Cmax
even(p1) is reached and then go to a).
Observe that λC requires inﬁnite memory. In the next lemma, we prove that λC is a
proper witness for S(p1) ∧AS(p2) in the UGEC C.
▶Lemma 12. Let C ∈UGEC(M). For all s ∈C, it holds that s, λC |= S(p1) ∧AS(p2).
Proof. Consider the strategy λC as described in Deﬁnition 11 and any state s ∈C. We ﬁrst
prove that s, λC |= S(p1). We want to show that for any π ∈OutM⇂C
s
(λC), maxs′∈inf(π) p1(s′)
is even. Fix such a play π. Three cases are possible: (i) λC switches inﬁnitely often between
λ1,C and λ2,C, (ii) it eventually plays λ1,C forever, and (iii) it eventually plays λ2,C forever.
In case (i), we have that Cmax
even(p1) is visited inﬁnitely often along π. Since any state in this set
has an even priority higher than any odd priority in C by deﬁnition, we have that π satisﬁes
the parity objective p1. In case (ii), we eventually always play as λ1,C and by hypothesis, we
know that s, λ1,C |= S(p1). By preﬁx-independence, we thus have that π satisﬁes the parity
objective p1. Finally, in case (iii), we have that Dmax
even(p1) is visited inﬁnitely often, and that
eventually, play π never leaves the sub-EC D associated to strategy λ2,C (by deﬁnition of
λ2,C, the fact that Dmax
even(p1) ⊆D is reached, and that we never switch to λ1,C from some
point on). Since any state in Dmax
even(p1) has an even priority higher than any odd priority in D,
we have that π satisﬁes the parity objective p1. Therefore, we conclude that s, λC |= S(p1).
It remains to show that s, λC |= AS(p2). To do so, we will prove that λC almost-surely
ends up in playing only λ2,C (which ensures AS(p2)). First, observe that λ1,C almost-surely
reaches Cmax
even(p1), hence each phase b) of strategy λC terminates with probability one. Now,
consider the probability xi that λ1,C is never played again after round i (in which λ2,C is
played for ni steps). For any round i ≥1, it is equal to xi = Q∞
j=i(1 −2−j) thanks to the
choice of ni. Since (1 −2−j) > 0 for any j > 1, we have that log(xi) = P∞
j=i log(1 −2−j).
Now, for any z ∈[0, 1
2], we have the following inequalities:
0 ≥log(1 −z) = −(log(1) −log(1 −z)) = −
Z 1
1−z
1
y dy ≥−2
Z 1
1−z
dy = −2z.
Using these, we can bound log(xi) as follows:
∞
X
j=i
−2 · 2−j ≤log(xi) ≤0 ⇐⇒−2 · 2−i
∞
X
j=0
2−j ≤log(xi) ≤0
⇐⇒−2−(i−2) ≤log(xi) ≤0.
Hence, log(xi)
i→+∞
−−−−→0, and by continuity of the exponential function, we ﬁnally obtain
that xi
i→+∞
−−−−→1. This proves that λC almost-surely consists in playing as λ2,C forever from
some point on, which combined with the preﬁx-independence of the parity objective, implies
that λC ensures AS(p2), and concludes our proof.
◀

R. Berthon, M. Randour, and J.-F. Raskin
XX:15
Global strategy. We can now prove the left-to-right implication of Lemma 9. For this,
assume that for s0 ∈S, we have that λU is a witness for s0 |= S(p1)∧AS(♦U), where we recall
that U represents the union of all UGECs of the MDP M. Note that such a strategy can be
ﬁnite-memory w.l.o.g. as proved in Theorem 4. We build a global strategy λ as follows.
▶Deﬁnition 13. Based on strategies λU and λC for all C ∈UGEC(M), we build the global
strategy λ as follows.
a) Play λU until a UGEC C is reached, then go to b).
b) Play λC forever.
This strategy requires inﬁnite memory because it is needed for the strategies λC. We prove
that λ is a witness for the beyond worst-case problem S(p1) ∧AS(p2).
▶Lemma 14. It holds that s0, λ |= S(p1) ∧AS(p2).
Proof. First, we consider S(p1). Along any play π consistent with λ, either (i) a UGEC C is
eventually reached and λ switches to λC, or (ii) λ behaves as λU forever. Since all strategies
λC and strategy λU ensure S(p1) and the parity objective is preﬁx-independent, we have that
s0, λ |= S(p1).
Second, with probability one, λU reaches some UGEC C, in which λC ensures AS(p2).
Again invoking preﬁx-independence, we conclude that λ is also a witness for s0 |= AS(p2),
which ends our proof.
◀
4.2
Right-to-left implication (necessary condition)
We now turn to the converse implication of Lemma 9, i.e., that s0 |= S(p1) ∧AS(p2) implies
s0 |= S(p1) ∧AS(♦U). This in particular requires U to be non-empty, i.e., the existence of a
UGEC in M.
Initialized strategies. We start by an intermediate lemma regarding witness strategies for
the beyond worst-case problem. It establishes that all states reachable via such a strategy
also satisfy the property.
▶Lemma 15. For every state s ∈S, every strategy λ such that s, λ |= S(p1) ∧AS(p2), and
every preﬁx ρ ∈Pref(OutM
s (λ)), we have that Last(ρ) |= S(p1) ∧AS(p2).
Proof. Fix s ∈S, λ a witness for s |= S(p1) ∧AS(p2), and a preﬁx ρ ∈Pref(OutM
s (λ)). We
claim that the initialized strategy λ[ρ], that behaves as λ behaves after preﬁx ρ, is a witness
for Last(ρ) |= S(p1) ∧AS(p2).
We ﬁrst consider S(p1). For all π ∈OutM
Last(ρ)(λ[ρ]), we have that ρ · π ∈OutM
s (λ).
Furthermore, maxs′∈inf(ρ·π) p1(s′) = maxs′∈inf(π) p1(s′) is even since s, λ |= S(p1). Hence, we
conclude that Last(ρ), λ[ρ] |= S(p1).
Now, consider AS(p2). By contradiction, assume that Last(ρ), λ[ρ] ̸|= AS(p2), i.e., that
q = Pλ[ρ]
M,Last(ρ)[p2] < 1. It implies that Pλ
M,s[p2] ≤1 −Pλ
M,s[ρ] · (1 −q) < 1 (since any preﬁx
ρ ∈Pref(OutM
s (λ)) has strictly positive probability). This contradicts the hypothesis that
s, λ |= AS(p2). Hence it holds that Last(ρ), λ[ρ] |= AS(p2) and we are done.
◀
Existence of UGECs. The next lemma establishes that at least one UGEC must exist
in M since s0 |= S(p1) ∧AS(p2).
▶Lemma 16. The following holds: s0 |= S(p1) ∧AS(p2) =⇒UGEC(M) ̸= ∅.

XX:16
Threshold Constraints with Guarantees for Parity Objectives in MDPs
Proof. Given a set of plays Π ⊆Plays(G), we deﬁne
States(Π) = {s ∈S | ∃π ∈Π, ∃n ∈N, π(n) = s}.
To prove this lemma, we ﬁrst study the following set S of subsets of S:
S =

R ⊆S | ∃s ∈S, ∃λ ∈Λ, (s, λ |= S(p1) ∧AS(p2)) ∧(R = States(OutM
s (λ)))
	
.
Intuitively, this set contains any subset of S that captures all states reachable by some
witness strategy λ, from some state s ∈S. First note that s0 |= S(p1) ∧AS(p2) implies that
S is non-empty, as for a witness strategy λ, R = States(OutM
s0 (λ)) ∈S, by deﬁnition.
Second, we show that all minimal elements of S for set inclusion ⊆are UGECs, i.e.,
that for all R ∈min⊆(S), it holds that R ∈UGEC(M). This will establish our lemma. By
Deﬁnition 8, we have to prove that for each R ∈min⊆(S), the following properties hold:
a) R is an EC in M,
b) ∀s ∈R, s |=M⇂R S(p1) ∧AS(♦Rmax
even(p1)),
c) ∀s ∈R, s |=M⇂R AS(p1) ∧AS(p2).
Before proving those three items, we claim that for all R ∈min⊆(S), and s ∈R, there
exists a strategy λR such that s, λR |=M⇂R S(p1) ∧AS(p2), i.e., λR satisﬁes the property
without leaving R. This is a direct consequence of Lemma 15 and the minimality of R in S
for the ⊆order. We use strategy λR in the rest of the proof.
Item a). We ﬁrst prove that R is strongly connected. By contradiction, assume it is not
the case, i.e., that there exist s, s′ ∈R such that there is no path in R from s to s′. Then, let
R′ be the set of states reachable with strategy λR from a preﬁx ρ ending in s. By Lemma 15,
we have that R′ ∈S. But as there is no path from s to s′ in R, we have that s′ ̸∈R′
and R′ ⊊R. This contradicts the minimality of R, hence we conclude that R is strongly
connected. We now have to prove that R is a trap for P2. Clearly, for any state s ∈R ∩S1,
we have Succ(s) ∩R ̸= ∅, as R is strongly connected. Hence, it remains to show that for all
s ∈R ∩S2, we have Succ(s) ⊆R. By contradiction, ﬁx some s ∈R ∩S2 and assume there
exists s′ ̸∈R such that (s, s′) ∈E. As R belongs to S, recall that R = States(OutM
s′′ (λ)) for
some strategy λ and state s′′. Since s ∈R, there exists a preﬁx ρ ∈Pref(OutM
s′′ (λ)) such
that Last(ρ) = s. But then, preﬁx ρ′ = ρ · s′ also belongs to Pref(OutM
s′′ (λ)), and s′ ∈R.
Thus, we conclude that R is indeed a trap for P2.
Item b). Fix any s ∈R and let us prove that s |=M⇂R S(p1) ∧AS(♦Rmax
even(p1)). As seen
above, from the minimality of R and Lemma 15, we know that s, λR |=M⇂R S(p1) ∧AS(p2).
Now, again from the minimality of R in S, we know that in the subtree induced by OutM
s (λR),
any non-empty subset R′ ⊆R is dense. That is, for every preﬁx ρ, the subtree deﬁned by λR
from ρ reaches a state of R′, and this holds in all subsequent subtrees. Using the reduction
to a parity-Büchi game which underlies Theorem 4, we deduce from the density argument
presented in Appendix A that we can build λ from λR such that s, λ |=M⇂R S(p1) ∧AS(♦R′).
It remains to argue that Rmax
even(p1) is non-empty to prove this item. This is necessarily true,
otherwise Rmax
odd (p1) would be non-empty and λR would not ensure S(p1) in R (as Rmax
odd (p1)
would be a dense subset).
Item c). This is trivial as, for s ∈R, λR enforces s, λR |=M⇂R S(p1) ∧AS(p2), a stronger
property.
◀
Reaching UGECs. Let us now collect in Umin = ∪R∈min⊆(S)R all states that belong to
minimal sets R of S, and establish that Umin can be reached almost-surely if it holds that
s0 |= S(p1) ∧AS(p2).

R. Berthon, M. Randour, and J.-F. Raskin
XX:17
▶Lemma 17. The following holds: s0 |= S(p1) ∧AS(p2) =⇒s0 |= S(p1) ∧AS(♦Umin).
Proof. Let λ be a witness for s0 |= S(p1) ∧AS(p2). By Lemma 16, we know that UGEC(M)
is non-empty, and so is Umin. Furthermore, we claim that Umin is dense in the tree induced
by OutM
s0 (λ). Indeed, by Lemma 15, after any preﬁx ρ ∈Pref(OutM
s0 (λ)), the following
property holds: Last(ρ), λ[ρ] |= S(p1) ∧AS(p2) (hence Umin is reached, repeating the previous
arguments). Since this holds for all consistent preﬁxes, we have that Umin is indeed dense in
the tree of λ. Hence, again using the density argument presented in Appendix A, we can
build λ′ from λ such that s0, λ′ |= S(p1) ∧AS(♦Umin).
◀
4.3
Algorithm
Lemma 14 and Lemma 17 prove the correctness of the reduction presented in Lemma 9. It is
the cornerstone of our algorithm deciding whether the property s0 |= S(p1) ∧AS(p2) holds.
▶Theorem 18. Given an MDP M = (G = (S, E), S1, S2, δ), a state s0 ∈S, and two priority
functions pi : S →{1, . . . , d}, i ∈{1, 2}, it can be decided in NP∩coNP if s0 |= S(p1)∧AS(p2).
If the answer is Yes, then there exists an inﬁnite-memory witness strategy, and inﬁnite
memory is in general necessary. This decision problem is at least as hard as solving parity
games.
Proof. The algorithm can be sketched as follows:
1. Compute the set max⊆(SGEC(M)) of maximal super-good ECs, using [1]. Those are
the maximal ECs satisfying condition (1U) in Deﬁnition 8. There are only polynomially
many of them, and their computation is in NP ∩coNP.
2. For each of them, check if condition (2U) holds using Lemma 10. This can be done in
polynomial time, and if a super-good EC does not satisfy (2U), then it is also the case of
all its sub-ECs (as seen in the proof of Lemma 10). Hence, we have that
U =

C ∈max
⊆(SGEC(M)) | C satisﬁes (2U)

.
3. Decide if s0 |= S(p1) ∧AS(♦U) using Theorem 4. This is in NP ∩coNP. If it holds, then
answer Yes, otherwise answer No.
The correctness of this algorithm was established in Lemma 9. It belongs to PNP∩coNP =
NP ∩coNP [6], and it trivially generalizes classical parity games (e.g., by taking p2 : s 7→0
for all s ∈S).
Finally, let us discuss strategies. A witness strategy λ plays as follows: (i) it plays as the
ﬁnite-memory strategy witness for s0 |= S(p1) ∧AS(♦U) given by Theorem 4 until a UGEC C
is reached, (ii) then it switches to the inﬁnite-memory strategy λC described in Deﬁnition 11.
It is clear that such a strategy is a witness for s0 |= S(p1) ∧AS(p2), as expected.
Inﬁnite memory is required in general, as shown in the UGEC C depicted in Figure 2:
there exists no ﬁnite-memory witness strategy in C. Indeed, assume P1 is restricted to a
ﬁnite-memory strategy λ. To be able to ensure p1 on the play in which P2 always goes to c
from b, P1 must visit d inﬁnitely often, and because of the ﬁnite memory of λ, he must do
it after a bounded number of steps along which a is not visited: say n steps. Hence, the
probability to do it will be bounded from below by a strictly positive constant, here 2−n
2 (the
probability that P2 chooses c for n
2 times in a row), all along a consistent play. Therefore,
P1 will almost-surely visit d inﬁnitely often, and p2 will actually be satisﬁed with probability
zero.
◀

XX:18
Threshold Constraints with Guarantees for Parity Objectives in MDPs
5
Parity with threshold probability under parity constraints
We now turn to the problem s0 |= S(p1) ∧P∼c(p2) for ∼∈{>, ≥} and c ∈Q ∩[0, 1).
Very-good end-components. In addition to UGECs, we need the new notion of very-good
end-component.
▶Deﬁnition 19. An end-component C of M is very-good (VGEC) if the following two
properties hold:
(1V) ∀s ∈C, s |=M S(p1);
(2V) ∀s ∈C, s |=M⇂C AS(p1) ∧AS(p2), or equivalently, s |=M⇂C AS(p1 ∩p2).
We introduce the following notations:
VGEC(M) is the set of all VGECs of M,
V = ∪V ∈VGEC(M)V is the set of states that belong to a VGEC in M.
Note that in condition (1V), P1 is allowed to leave C to ensure S(p1): this is in contrast
to condition (1U) for UGECs, in Deﬁnition 8. On the contrary, condition (2V) is exactly
the same as (2U).
From these deﬁnitions, it is trivial to see that any UGEC is also a VGEC, but the converse
is false. Consider Figure 3 (δ is the uniform distribution): {a, b, c} is a VGEC. The strategy
ensuring (2V) from a is to go to b, and the strategy ensuring (1V) from a is to go to d.
As we will prove in Lemma 21 and as in all VGECs, P1 can ensure a |= S(p1) ∧P>1−ε(p2)
for any ε > 0. Still, {a, b, c} is not a UGEC: no strategy ensures S(p1) on M⇂{a,b,c}, as P2
can enforce the play (ab)ω that has odd maximal priority. This illustrates why the notion
of UGEC is too strong when reasoning about threshold probability instead of almost-sure
satisfaction, hence why we need to introduce VGECs.
1, 1
1, 1
2, 2
0, 1
a
b
c
d
Figure 3 The EC {a, b, c} is very-good but not ultra-good, as P1 has to leave it to ensure S(p1).
Available strategies in VGECs.
As for UGECs, we will use witness strategies for
conditions (1V) and (2V). Deciding if condition (1V) holds is solving a classical parity game,
which lies in NP ∩coNP [19]. Uniform pure memoryless witness strategies exist. We denote
by λ1 such a witness. For simplicity of presentation, we assume in the following that all states
of M satisfy (1V), as otherwise they will trivially not satisfy the properties we consider (as
S(p1) will not be ensured). For condition (2V), we established in Lemma 10 that deciding if
it holds can be done in polynomial time and that uniform randomized memoryless witness
strategies exist. We denote by λ2,C such a witness.
Reaching VGECs. We prove a strong relationship between the measure of paths that
satisfy the two parity objectives p1 and p2, and the measure of paths that reach VGECs,
under any strategy.
▶Lemma 20. For all s ∈S, and all λ ∈Λ, the following holds: Pλ
M,s[♦V] ≥Pλ
M,s[p1 ∩p2].

R. Berthon, M. Randour, and J.-F. Raskin
XX:19
Proof. Let Π = {π ∈OutM
s (λ) | maxs∈inf(π) p1(s) and maxs∈inf(π) p2(s) are even} be the
set of consistent plays from s that are winning for both p1 and p2. Let q ∈[0, 1] be the
measure of Π under λ. Now, let us deﬁne the set Π′ as Π from which we remove the plays π
such that inf(π) is not an EC. By Lemma 3, the measure of Π \ Π′ is equal to zero, and so
the measure of Π′ is also q. Now, let us note that any remaining play π in Π′ visits (even
inﬁnitely many times) the EC inf(π), and by deﬁnition of Π, there exists a witness for (2V):
it suﬃces to play uniformly at random in inf(π). Since (1V) is satisﬁed everywhere, we have
that inf(π) is a VGEC. All the states of such VGECs belong to V by deﬁnition, and so we
are done as we have proved that Pλ
M,s[♦V] ≥q.
◀
Limit-sure satisfaction in VGECs. For each state in a VGEC, we claim that the parity
objective p2 can be satisﬁed with probability arbitrarily close to one, while ensuring p1 surely.
▶Lemma 21. Let C ∈VGEC(M). For all s ∈C and ε ∈(0, 1], the following property
holds: s |= S(p1) ∧P>1−ε(p2).
Proof. Our goal is to deﬁne a witness strategy λε. First, for ε ∈(0, 1], we ﬁx an inﬁnite
sequence of strictly positive rational probabilities f : N →Q ∩(0, 1] such that the product of
all those probabilities is larger than 1 −ε, i.e., Q
i∈N f(i) > 1 −ε. Such a sequence always
exists. In turn, we use Lemma 10 to associate to f a sequence of natural numbers g: N →N
such that if λ2,C (the witness strategy for (2V)) is played for g(i) steps from any s′ ∈C,
then the set Dmax
even(p1) associated to λ2,C is visited during those g(i) steps with probability
larger than f(i).
Now, we are in position to deﬁne for all ε ∈(0, 1], a strategy λε,C that enforces, from
s ∈C, the property S(p1) ∧P>1−ε(p2). The strategy λε,C uses a counter i whose value is
initially equal to 0. At round i, the strategy plays as λ2,C for g(i) steps. During those last
g(i) steps, if the set Dmax
even(p1) is visited, then the counter i is incremented and the next
round is executed. Otherwise, the strategy switches to λ1 forever.
It is easy to see that, because of the deﬁnition that we have used for the sequence g, the
probability that we ever switch to the strategy λ1 is less than ε. So with probability larger
than 1 −ε, we always play λ2,C, which implies that λε,C ensures P>1−ε(p2). Also, on all
consistent plays in which strategy λ2,C is played forever, we know that the maximal priority
seen inﬁnitely often for p1 is even (by deﬁnition of Dmax
even(p1)). On the other plays, as we
play λ1, we also have that the maximal priority seen inﬁnitely often for p1 is even. So, we
can also conclude that λε,C ensures S(p1), which concludes our proof.
◀
5.1
The strict threshold case
We ﬁrst solve the strict threshold case with an approach that relies on VGECs. Similarly
to what we did in Section 4, we will establish a reduction of the decision problem for
s0 |= S(p1) ∧P>c(p2) to a reachability problem toward the set V, i.e., the union of VGECs.
The actual algorithm will be detailed in Section 5.3.
The ﬁrst lemma gives a suﬃcient condition under which the property is satisﬁed. Note
that its proof is constructive and tells us how to construct witness strategies.
▶Lemma 22. The following holds: s0 |= S(p1) ∧P>c(♦V) =⇒s0 |= S(p1) ∧P>c(p2).
Proof. Let λ♦V be a uniform pure memoryless strategy that ensures to reach V with
probability q > c from s0 (it exists by Lemma 2).
Recall that λ1 is a uniform pure
memoryless strategy that enforces S(p1) from any state in M. Let ε be such that q >
c
1−ε,

XX:20
Threshold Constraints with Guarantees for Parity Objectives in MDPs
and for all C ∈VGEC(M), let λε,C be a strategy that ensures S(p1) ∧P>1−ε(p2) when
played from any state in C, as deﬁned in the proof of Lemma 21.
We construct the strategy λ that witnesses the desired property starting from the elements
deﬁned above. Let r be a number of steps suﬃcient to ensure that V is reached with a
probability larger than
c
1−ε when playing λ♦V from s0: it exists by Lemma 2. Strategy λ plays
as λ♦V for r steps. If a VGEC C of M is reached during those r steps, λ immediately switches
to λε,C. Otherwise, after the r steps, λ switches to λ1 forever. It is easy to verify that, when
λ is played, the probability that p2 holds is larger than
c
1−ε · (1 −ε), and so it is larger than c.
Furthermore, p1 holds on all consistent plays, which proves that s0, λ |= S(p1) ∧P>c(p2)
holds.
◀
This second lemma gives a necessary condition for the property to hold.
▶Lemma 23. The following holds: s0 |= S(p1) ∧P>c(p2) =⇒s0 |= S(p1) ∧P>c(♦V).
Proof. First, recall that λ1 is a uniform pure memoryless strategy that enforces S(p1) from
any state in M. Second, as s0 |= S(p1) ∧P>c(p2), by Lemma 20 and Lemma 2, there exists a
uniform pure memoryless strategy λ♦V that reaches V with probability q > c from s0.
We construct a strategy λ that witnesses s0 |= S(p1) ∧P>c(♦V) starting from λ1 and
λ♦V above. Let r be a number of steps such that, if λ♦V is played for r steps, then the
probability to reach V from s0 is larger than c: it exists by Lemma 2. Strategy λ starts
from s0 by playing as λ♦V. It stops as soon as V is reached or if r steps have been played
and V has not been reached: in both cases, it switches to λ1 forever. It is easy to check
that λ reaches V with probability larger than c and it enforces S(p1) by deﬁnition of λ1 and
preﬁx-independence of the parity objective. Hence, it holds that s0, λ |= S(p1)∧P>c(♦V).
◀
5.2
The non-strict threshold case
First, we note that, as we have solved the strict case above, the only interesting remaining
case is when P1, while surely forcing p1, can force p2 with probability c, but no more. The
following two lemmas present a solution to this case. The main conceptual tool here is
UGECs. As for the strict case, the corresponding algorithm will be detailed in Section 5.3.
The ﬁrst lemma gives a suﬃcient condition. Its proof is constructive: it explains how to
build witness strategies. Recall that U = ∪U∈UGEC(M)U is the set of states that belong to a
UGEC in M.
▶Lemma 24. The following holds: s0 |= S(p1) ∧P≥c(♦U) =⇒s0 |= S(p1) ∧P≥c(p2).
Proof. Let λ♦U be a ﬁnite-memory strategy that witnesses property s0 |= S(p1) ∧P≥c(♦U).
It exists by Theorem 6. For all C ∈UGEC(M), let λC be an inﬁnite-memory witness for
s |= S(p1) ∧AS(p2), for all s ∈C: it exists by Lemma 12. From those strategies, we deﬁne the
strategy λ as follows. From s0, λ plays as λ♦U up to reaching a UGEC C of M, or forever if
U is never reached. If some UGEC C is reached, then it switches to λC forever.
We claim that λ is a witness for s0 |= S(p1)∧P≥c(p2). First, S(p1) clearly holds as strategy
λ♦U and all strategies λC ensure it. Second, λ♦U ensures to reach U with probability at
least c, and when some UGEC C ⊆U is reached, the strategy λC ensures p2 with probability
one. Thus, λ ensures p2 with probability at least c from s0, and we are done.
◀
We now turn to a lemma that gives a necessary condition, keeping in mind that we are
interested in the case where P1 cannot ensure probability strictly larger than c.

R. Berthon, M. Randour, and J.-F. Raskin
XX:21
▶Lemma 25. The following holds:
(s0 |= S(p1) ∧P≥c(p2)) ∧(s0 ̸|= S(p1) ∧P>c(p2)) =⇒s0 |= S(p1) ∧P≥c(♦U).
Proof. Let λ be a strategy that witnesses s0 |= S(p1)∧P≥c(p2). As s0 ̸|= S(p1)∧P>c(p2), the
measure of Π = {π ∈OutM
s0 (λ) | maxs∈inf(π) p1(s) and maxs∈inf(π) p2(s) are even} is exactly
equal to c. We deﬁne Π′ as Π from which we remove all play π such that inf(π) is not an
EC. By Lemma 3, the measure of Π′ is equal to the one of Π. As all plays in Π′ satisfy both
p1 and p2 and are such that inf(π) is an EC, we conclude that they all reach a VGEC (as
conditions (1V) and (2V) are satisﬁed in inf(π)).
Let π ∈Π′ and let ρ be a preﬁx of π such that Last(ρ) ∈C for some C ∈VGEC(M). We
claim that Last(ρ), λ[ρ] |= S(p1) ∧AS(p2). We prove it by contradiction. Assume that λ[ρ]
only enforces p2 with probability 1 −ε for some ε > 0. As Last(ρ) belongs to the VGEC C,
we invoke Lemma 21 to build a strategy λρ such that Last(ρ), λρ |= S(p1) ∧P>1−ε′(p2) for
ε′ < ε. Now, to obtain the contradiction, we construct a new strategy λ′ from s0 that plays as
λ but switches to λρ after preﬁx ρ. We have that s0, λ′ |= S(p1) ∧P>c(p2), which contradicts
our hypothesis. Hence, we conclude that Last(ρ), λ[ρ] |= S(p1) ∧AS(p2) holds.
Finally, applying Lemma 17 to Last(ρ), we know that Last(ρ) |= S(p1) ∧AS(♦U). As
the measure of Π′ is equal to c and this reasoning holds for any π ∈Π′, we conclude that
s0, λ |= S(p1) ∧P≥c(♦U) and we are done.
◀
5.3
Algorithm
Based on the reductions shown above, we can now establish an algorithm and complexity
results for the threshold problem.
▶Theorem 26. Given an MDP M = (G = (S, E), S1, S2, δ), a state s0 ∈S, and two priority
functions pi : S →{1, . . . , d}, i ∈{1, 2}, it can be decided in NP∩coNP if s0 |= S(p1)∧P∼c(p2)
for ∼∈{>, ≥} and c ∈Q ∩[0, 1). If the answer is Yes, then there exists an inﬁnite-memory
witness strategy, and inﬁnite memory is in general necessary. This decision problem is at
least as hard as solving parity games.
Proof. The algorithm can be sketched as follows:
1. Remove from M all states where S(p1) does not hold, as well as their attractor for P2:
if s0 is removed, then answer No. Let M′ be the remaining MDP. This operation is in
NP ∩coNP as it consists in solving a classical parity game [19].
2. Compute the set V representing the union of VGECs in M′.
This can be done in
polynomial time by computing the maximal ECs of M′ and applying Lemma 10 to check
condition (2V) for each of them (condition (1V) holds thanks to the previous step).
3. Decide if s0 |= S(p1) ∧P>c(♦V) using Theorem 6. This is in NP ∩coNP. If it holds, then
answer Yes. If it does not hold and ∼is >, then answer No, otherwise, i.e., if ∼is ≥,
continue with the next step.
4. Use the sub-algorithm described in Theorem 18 to compute the set U representing the
union of UGECs in M′. This is in NP ∩coNP.
5. Decide if s0 |= S(p1) ∧P≥c(♦U) using Theorem 6. This is in NP ∩coNP. If it holds,
answer Yes, otherwise answer No.
The correctness of this algorithm follows from Lemma 22, Lemma 23, Lemma 24, and
Lemma 25. It belongs to PNP∩coNP = NP ∩coNP [6], and it trivially generalizes classical
parity games (e.g., by taking p2 : s 7→0 for all s ∈S).

XX:22
Threshold Constraints with Guarantees for Parity Objectives in MDPs
Finally, let us discuss strategies. Witness strategies for the case > (resp. ≥) were described
in Lemma 22 (resp. Lemma 24). In both cases, inﬁnite memory is in general required, because
it is in general necessary to play optimally in both VGECs and UGECs. For UGECs, see
Theorem 18 for an example. For VGECs, consider the VGEC {a, b, c} in the MDP of Figure 3.
We claim that for every ﬁnite-memory strategy λ ensuring S(p1), the probability to ensure p2
is zero, hence there is no ﬁnite-memory witness for a |= S(p1) ∧P>1−ε(p2). As argued for the
UGEC case, in order to ensure p1 on the play in which P2 always goes to a from b, P1 must
go to d at some point, and because of the ﬁnite memory of λ, he must do it after a bounded
number of steps along which c is not visited: say n steps. Again, the probability to do it will
be bounded from below by a strictly positive constant, here 2−n
2 (the probability that P2
chooses a for n
2 times in a row), all along a consistent play. Therefore, P1 will almost-surely
go to d, and p2 will actually be satisﬁed with probability zero.
◀
6
Conclusion
We further extended the beyond worst-case synthesis framework by providing tools to reason
about ω-regular conditions, representing functional requirements of systems. We studied the
case of two parity objectives and proved NP ∩coNP membership for all considered variants.
It is interesting to note that our algorithms can easily be generalized to more than two
parity objectives as long as we consider only the S and AS operators. Indeed, we have that
for any MDP M, any state s in M, and any number of priority functions p1, . . . pn, it holds
that s |= V
i S(pi) V
j AS(pj) ⇐⇒s |= S
  V
i pi

∧AS
  V
j pj

, and it is easy to reduce the
latter problem to s′ |= S(p′) ∧AS(p′′) on a (larger) MDP M′, using classical techniques (e.g.,
any conjunction of parity objectives can be expressed as a Muller condition [13], that in
turn can be transformed into a single parity condition on a larger graph [20]). Extending
this generalization to the operator P∼c is more challenging and would require to mix our
techniques to methods for percentile queries [24]: an interesting direction for future work.
Another question is the limits of ﬁnite-memory strategies. We saw that in general, inﬁnite
memory is needed to satisfy problems involving two parity objectives. We would like to
investigate under which additional conditions ﬁnite-memory strategies suﬃce, and to develop
corresponding algorithms, as ﬁnite-memory strategies are of great practical interest.
