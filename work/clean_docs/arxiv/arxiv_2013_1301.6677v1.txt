31 
Relative Loss Bounds for On-line Density Estimation with the 
Exponential Family of Distributions 
Katy S. Azoury 
College of Business 
San Francisco State University 
San Francisco, CA 94132 
U.S.A 
kazouryJsfsu.edu 
Abstract 
We consider on-line density estimation with 
a parameterized density from the exponential 
family. The on-line algorithm receives one 
example at a time and maintains a parame­
ter that is essentially an average of the past 
examples. After receiving an example the al­
gorithm incurs a loss which is the negative 
log-likelihood of the example w.r.t. the past 
parameter of the algorithm. An off-line algo­
rithm can choose the best parameter based 
on all the examples. We prove bounds on the 
additional total loss of the on-line algorithm 
over the total loss of the off-line algorithm. 
These relative loss bounds hold for an arbi­
trary sequence of examples. The goal is to 
design algorithms with the best possible rel­
ative loss bounds. We use a certain diver­
gence to derive and analyze the algorithms. 
This divergence is a relative entropy between 
two exponential distributions. 
1 
Introduction 
Consider density estimation with a Gaussian of fixed 
variance. The off-line algorithm is given a batch of 
examples {xl>xz, ... ,xT} all at once and it chooses 
its mean parameter based on all T examples. The 
choice of the mean which maximizes the likelihood of 
the examples is clearly the average of the examples. 
The on-line algorithm sees the examples one at a time. 
At trial t it has to choose or predict with a mean 1-'t 
based on the past t - 1 examples. It then receives the 
example Xt and incurrs a loss (I-tt - x1)2 f2. Finally, 
at the end of trial t, the on-line algorithm updates its 
parameter (hypothesis) from 1-'t to 1-'tH. 
The performance of the on-line algorithm is measured 
by the total loss on the whole sequence of examples. 
The goal is to design on-line learning algorithms with 
M. K. Warmuth 
Computer Science Department 
University of California, Santa Cruz 
Santa Cruz, CA 95064 
U.S.A. 
manfredJcse.ucsc.edu 
good bounds on their total loss. Even though the moti­
vation of the parameter updates may have a probabilis­
tic interpretation, the bounds we prove hold for an ar­
bitrary (or worst-case) sequence of examples. Clearly, 
there cannot be meaningful bounds on the total loss 
of an algorithm that stand alone as well as hold for 
an arbitrary sequences of examples. However, in on­
line learning a certain type of "relative" loss bound 
is desirable and has been used successfully. The term 
"relative" means that there is a comparison to the best 
off-line model from a comparison class of models. The 
relative loss bounds quantify the additional total loss 
of the on-line algorithm over the total loss of the best 
off-line model. Since the on-line learner does not see 
the sequence of examples in advance, the relative loss 
bounds estimate the price of hiding the future exam­
ples from the learner. 
The simplest on-line algorithm in the Gaussian exam­
ple predicts at trial t as the off-line algorithm would 
on the batch {x1,x2, ... ,x1_1}. That is, 1-'t is the 
average of the past t - 1 examples. We call this the 
incremental off-line algorithm. The purpose of this pa­
per is to prove relative loss bounds for the incremental 
off-line algorithm and a variant of this algorithm that 
averages by t instead of t
- 1 when computing 1-'t· We 
call the latter algorithm the forward algorithm 
The bounds are developed for density estimation with 
a parameterized distribution from the exponential 
family. The Gaussian density is just a special case. 
In the general case the mean parameters remain aver­
ages or modified averages of the past instances. 
The forward algorithm was inspired by the work of 
Vovk [Vov97] on linear regression which can also be in­
terpreted as a density estimation problem. For linear 
regression the incremental off-line algorithm becomes 
the on-line linear least squares algorithm or ridge re­
gression. Dividing by t - 1 for computing 1-'t corre­
sponds to multiplying with the inverse covariance ma­
trix of the first t- 1 examples. The forward algorithm 
for linear regression multiplies with the inverse covari-

32 
Azoury and Warmuth 
ance matrix of the first t examples instead. 
Overview of previous work 
This work is rooted in the study of relative loss 
bounds for on-line learning algorithms of the com­
putational learning theory community. Even though 
these bounds may underestimate the performance on 
natural data we have been using them as a power­
ful yardstick for analyzing and comparing algorithms. 
This line of research was initiated by Littlestone with 
the discovery of the Winnow algorithm [Lit88]. He 
also pioneered a style of amortized analysis for prov­
ing relative loss bounds which use certain divergence 
functions as potential functions. Winnow is designed 
for disjunctions as the comparison class and the total 
number of mistakes is used as the loss. The next wave 
of on-line algorithms was designed for a finite set of 
experts as a comparison class and a wide range of loss 
functions [LW94, Vov90, CBFH+97, HKW98]. After 
that, algorithms were developed for the on-line linear 
least squares regression, i.e. when the comparison class 
consists of linear neurons (linear combination of ex­
perts) [LLW95, CBLW96, KW97]. This work has been 
generalized to the case when sigmoided linear neurons 
are the comparison class [HKW95, KW98]. General 
frameworks of on-line learning algorithms were devel­
oped in [GLS97, KW97, KW98]. We follow the phi­
losophy of Kivinen and Warmuth [KW97] of starting 
with a divergence function. From the divergence func­
tion we derive the on-line update and we then use the 
same divergence as a potential in the amortized anal­
ysis. A similar method was developed in [GLS97] for 
the case when the comparison class consists of linear 
threshold functions. They start with an update and 
construct the appropriate divergence that is used in 
the analysis. 
Recently we have been discovering that the diver­
gences that were used in on-line learning have been 
employed extensively in convex optimization and are 
called Bregman-distances [Bre67, CL81, Csi91, JB90]. 
We use the term "divergence" instead of "distance" 
since the triangular inequality does not hold for such 
divergences. Bregman divergences can also be seen as 
relative entropies between distributions from the ex­
ponential family of distributions [Ama85]. In partic­
ular a notion of duality was explored by Amari that 
is inherent in these divergences. In a later section we 
bring out the connection to the exponential family of 
distributions and also discuss briefly how the Bregman 
divergences are used in convex optimization. 
We use the divergence to prove a different type of rel­
ative loss bounds that grow logarithmically with the 
number of trials T. These types of bounds have been 
investigated in a Bayesian context by [Fre96, XB97, 
Yam98) where a generalized posterior is maintained 
on all parameters. Such bounds have also been proven 
for on-line linear regression [Fos91, Vov97]. An im­
portant insight we gained from this research is that 
log(T)-style bounds can be obtained by using variable 
learning rates. This is a key feature of this work and 
distinguishes it from most previous work on relative 
loss bounds which typically uses the same learning rate 
in each trial. In this paper the learning rate applied 
in trial t is proportional to 1/ t. 
Our updates are simple and operate directly on the 
sufficient statistic of the parametric exponential fam­
ily. Our methodology applies to any member of the 
exponential family of distributions. In summary, a key 
goal of this paper is to stress the importance of the di­
vergences as a tool for deriving and analyzing on-line 
learning algorithms and to bring out the advantage of 
variable learning rates. 
Outline 
In the next section we present an overview of impor­
tant properties of the exponential family and introduce 
the notion of divergences and their duality properties. 
In Section 3 we treat density estimation for the expo­
nential family. We give a number of examples of how 
our methods can be applied to particular members of 
the exponential family. We then outline in Section 4 
how the relative loss bounds for linear regression that 
where proven by Vovk [Vov97] can be obtained with 
our methods. In Section 5 we briefly discuss Vovk's 
alternate method for proving relative loss bounds that 
uses integration over a generalized posterior. Our tech­
niques are more concise. Finally we conclude (Section 
6) with a discussion of future work. 
2 
Exponential Families of 
Distributions 
In this section we review some important features of 
the exponential family of distributions that will be 
used in this paper. The features of interest that are 
used throughout this paper include measures of diver­
gence between two members of the family and an in­
trinsic duality relationship. See [BN78, Ama85] for a 
more comprehensive treatment. 
A multivariate parametric family Fa of distributions 
is said to be an exponential family, when its members 
have a density function of the form 
Pa(x[6) = exp(6 · x- G(6))Po(x) 
(2.1) 
where 6 and x are vectors in Rd, 6 · x denotes the 

Relative Loss Bounds for On-line Density Estimation 
33 
dot product, and Po ( x) represents any factor of the 
density which does not depend on 9. 
The d-dimensional parameter 9 is usually called the 
natural (or canonical ) parameter. 
Many common 
parametric distributions are members of this family, 
including the Gaussian. The function G(9) is a nor­
malization factor defined by 
G(9) =In j (exp(fJ · x))Po(x)dx 
The space E> f Rd for which the integral above is finite 
is called the natural parameter space. The exponential 
family is called regular if E> is an open subset of Rd. 
It is well known that E> is a convex set, and that G(9) 
is a strictly convex function on e. Moreover, G(9), 
is called the cumulant function of (2.1), and it plays 
a fundamental role in characterizing members of this 
family of distributions. 
We use g(9) to denote the gradient vector \7 9G(6), 
and G;i(9) to denote the matrix of second partials of 
the cumulant function G ( 9). Let 
>..a(9; x) =In Pa(xl9) 
represent the log-likelihood which is viewed as a func­
tion of 9. 
Under some standard regularity condi­
tions, log-likelihood functions, in general, satisfy some 
well known moment identities [MN89]. Applying these 
identities to the exponential family reveals the special 
role played by the cumulant function G(9). 
Let E9(-) be the expectation with respect to the dis­
tribution Pa(xl9). The first moment identity of log­
likelihood functions is 
(2.2) 
For the exponential family, this identity implies that 
E9(x) = \7 9G(9) = g(9) 
thus, showing that the gradient g( 9) is equal to the 
mean of x. We let g(9) = p,, and call p, the expectation 
parameter. 
Since the cumulant function G is convex, the map 
g(9) = p, has an inverse. We denote the image of 
E> under the map g(.) by M and the inverse map from 
M toE> by g-1(p,) = 9. The set M is called the expec­
tation space, which may not necessarily be a convex 
set. 
The second moment identity for log-likelihood func­
tions is 
E9 ('V 9>..a(6; x)(\7 9>..a(6; x))') 
= E9 ((x- p,)(x -p,)') 
= -E9 (:>..a(9;x)) . 
Here ' denotes the transpose operation. 
For the exponential family we have that 
82 
82 
-89>..a(9;x) = 89G(fJ) = G;i(9). 
Thus the Hessian of G(9) (the matrix G;j(9)) is the 
variance-covariance matrix Var(xl9) for x. This ma­
trix is also called the Fisher Information Matrix. 
2.1 
Duality Between the Natural Parameters 
and the Expectation Parameters 
Sometimes, it is more convenient to parametrize a dis­
tribution in the exponential family by using its expec­
tation parameter p, instead of its natural parameter 9. 
This pair of parametrizations have a dual relationship. 
We provide the aspects of the duality that are relevant 
to this paper. Define a second function on the range 
M of g as follows: 
F(p,) := 9 · g(9)- G(9) 
(2.3) 
Let f(p,) = \7 p,F(p,) denote the gradient of F(p,). 
Note that by taking the gradient of F(p,) in (2.3) with 
respect to p, and treating 9 as a function of p,, we get 
89 
89 
\7 p,F(p,) = J(p,) = 9 
+ 8/.t p,- 8/.t g(9). 
(2.4) 
The last two terms cancel, and f (p,) is exactly the 
inverse map g-1(p,). Since g(9) has a positive definite 
Jacobian for all 9 E E>, g-1(p,) has a positive definite 
Jacobian for all p, E M. Thus the second function 
F(p,) is strictly convex as well and the functions G(9) 
and F(p,) form a pair of dual convex functions. 
The two parametrizations 9 and p, are related by the 
Legendre Transformations 
'V 9G(9) = 9(9) = p,, 
and \7 p,F(p,) = f(p,) = 9 
(2.5) 
Let F;i (p,) denote the Hessian of F (p,). It follows from 
(2.5) that F;i (p,) is equal to the inverse of the Fisher 
Information Matrix, since an immediate consequence 
of (2.5) is 
8p, 
G;i(9) 
89 = 
89 
F;i(Jt) = Gi/(9). 
8p, 
Now consider the function V (p,) = G ii (f (p,)), which 
is the Fisher Information Matrix expressed in terms 
of the expectation parameter. This function is defined 
on the expectation space M, takes values in the space 

34 
Azoury and Warmuth 
of symmetric d x d matrices, and is called the variance 
function. The matrix V (p.) is positive definite for all 
IL E M, and V (p.) = F; .. / (p.). Thus in the context 
of exponential families the functions F and G are not 
arbitrary strictly convex functions but must have posi­
tive definite symmetric Hessians. However all the dual 
relationships hold in general for the case when G(9) 
is an arbitrary differentiable convex function. In that 
case the dual function F(p.) is also called the convex 
conjugate [Roc70]. 
2.2 
Divergence Between two Exponential 
Distributions 
Consider two distributions P(xl9) with an old param­
eter setting 9, and P(xiB) with a new parameter set­
ting B. Following Amari [Ama85] one may see the ex­
ponential family :Fa as a manifold. The parameters 9 
and B represent two points on this manifold. Several 
measures of distance (divergence) between these two 
points have been proposed in the literature. Amari 
introduced a divergences [Ama85], and other related 
"distances" were introduced by Csiszar [Csi91] known 
as f divergences (we use the letter h below). Also 
Chernoff distances and Renyi's a information are re­
lated [Ama85]. These divergences all have the follow­
ing general form: 
E h (Pa(xiB)) 
9 
Pa(xl9) ' 
where h(.) is some continuous convex function. In 
the context of the exponential families the natural 
parameters B and 9 are names for two distributions. 
"Good" measures of closeness or divergence between 
them should remain invariant when the two distribu­
tions are named by their expectation parameters jl 
and p., respectively. 
We limit our discussion to two choices of h. For h(z) = 
-ln z we get the relative entropy 
E ln P(xl2) 
(-) 
( ) 
(-
) 
9 
-
= G9 -G9- 9-9 ·p. 
P(xl9) 
= F(tt) -F(jl) -(J.t- jl) ·B. 
The second equality follows from the duality (2.3). 
Choosing h(z) = z ln z gives the "opposite" relative 
entropy 
P(xiB) 
EBln P(xi9) = G(9)- G(B)-(9-B) . jl 
= F(jl)- F(p.)- (jl-p.) · 9. 
These two entropies are called -1 and +1 divergences 
by Amari [Ama85], respectively. 
2.3 
Bregman Divergence and its Properties 
We just defined the notion of divergence between two 
exponential distributions using the cumulant function 
G whose Hessian is positive definite. We now give 
a slightly more general definition of divergence due 
to Bregman (Bre67]: For any arbitrary differentiable 
strictly convex function G, 
Ll.a(ii, 9) := G(B)-G(9)-(ii-9) ·p., 
where as before IL = 'V 9G(6) == g(9) 
(2.6) 
Note that Ll.a(ii, 9) equals G(ii) minus the first two 
terms of the Taylor expansion of G(B) around 9. In 
other words, Ll.a(B, 9) is the tail of the Taylor expan­
sion of G(B) beyond the linear term. 
Bregman divergences (or distances) have been used ex­
tensively in convex optimization. An important tool 
is a notion of projections w .r.t. the divergences. Breg­
man's essential method is to pick from a convex set of 
allowable parameters the one of minimal distance to 
the current parameter. In other words the current hy­
potheses is projected onto the set of allowable param­
eters. Some mild additional assumption on G(9) as­
sures the uniqueness of the projections. With this as­
sumption a generalized Pythagorean Theorem can be 
proven for Bregman divergences [Bre67, CL81, Csi91, 
JB90, HW98]. The latter theorem contradicts the tri­
angular inequality which is the reason why we use the 
term "divergence" instead of "distance". 
In the context of learning these divergences were re­
discovered in [KW97, HKW95, KW98]. Related diver­
gences are used in [GLS97]. Projections have recently 
been applied in [HW98] for the case when the under­
lying model shifts over time and the projections w.r.t. 
the divergences are used to keep the parameters of the 
algorithm in reasonable convex regions. This aids the 
recovery process when the underlying model shifts. 
Properties of Bregman divergences 
Using our notation IL = g(9) == 'V 9G(6) 
1. Ll.a(B, 9) <:: 0 and equality holds iff B = 9. 
2. Often Ll.a(B, 9) -::/: Ll.a(9, B), when 9-::/: 9. 
3. V9Ll.a(B, 9) = g(B)-g(9) = il-p.. 
4. Ll.a(B, 9) is strictly convex in the first argument 
and not necessarily convex in its second argument. 
5. If H(9) = G(9) +a· 9 + b, for a E Rd and bE R, 
then Ll.H(B, 9) = Ll.a(B, 9). 

Relative Loss Bounds for On-line Density Estimation 
35 
6. Lla(O, 8) = b.F(t£, ji), 
where F(p,) := 8 · p,- G(8). 
The first five properties are immediate and the last 
follows from the definition of the dual function F. 
Note that the order of the arguments in Lla(ii, 8) and 
LlF (p,, ji) is switched as we move from 8 to the dual 
parameter p, and that b.F(t£, ji) is again convex in its 
first argument. 
3 
Density Estimation with the 
Exponential Families 
In this section we will analyze the performance of on­
line algorithms for density estimation when the un­
derlying distribution belongs to the exponential family 
with cumulant function G(8). We start with the mo­
tivation of the update of the algorithms and then de­
velop techniques for proving relative loss bounds. We 
make the most obvious choices. The loss of parameter 
8 on the example x1 is 
-In P(xtl8) = G(8)- 8 · Xt -In Po(x1). 
We denote this loss as Lt(8) and use Lu(8) 
= 
l:ôQ1 Lq ( 8) as shorthand for the loss in the first t tri­
als. We now motivate the algorithms with the (Breg­
man) divergence w.r.t. the cumulant function G. 
The off-line algorithm has at hand all T examples 
{ x1, X2, .
.
•
 , xr} and minimizes 
1181 Lla(8, 81) + L1..r(8). 
(3.1) 
Note that this maximizes the posterior when the prior 
is chosen to be the conjugate prior and 1} i/ > 0 is 
a measure of the importance of the prior. Alterna­
tively one can think of 81 as an initial parameter esti­
mate based on some hypothetical examples seen before 
the first real example x1 and 17131 2: 0 as the number 
of those examples. The parameter 1/B measures the 
trade-off between staying close to the initial parameter 
(h and minimizing the loss on the T real examples. In 
the example of Gaussian density estimation discussed 
in the introduction we have zero initial examples and 
thus 1181 = 0. 
The gradient of (3.1) is 
T 
1181(1-£-1-'!) + L(tt-Xt) 
tQl 
Setting it to zero gives the best expectation parameter 
of the off-line algorithm: 
T 
11-B = (1181 +T)-1(11811-'! + LXt)· 
(3.2) 
tõ1 
The on-line algorithm starts with the same parameter 
setting p,1 = g( (h) and with a learning rate 171. At 
the end of trial t the examples { x1, x2, ... , xt} are 
available and the on-line algorithm minimizes: 
vt+l (8) = 1711 Lla(8, 81) + Lu(8), 
(3.3) 
with 1111 2: 0. Thus the expectation parameter of the 
on-line algorithm at end of trial t is 
t 
1-'t+! = (1711 +t)-1(17111-'1 + .L:xq)· 
(3.4) 
qQl 
We can also get a recursive version of the on-line up­
date by minimizing 
(3.5) 
at the end of trial t. This gives 
1-'t+l = 1-'t -rJt(l-'t+l -Xt), 
(3.6) 
We always choose the learning rate IJt at trial t as 
(3.7) 
With this choice the above update coincides with the 
expanded version (3.4). Thus this makes the update 
permutation invariant, i.e. independent of the order of 
the past instances. The following alternate recursive 
forms are useful: 
1-'t+! 
= 
1/t+l (rJt1 1-'t + Xt) 
1-'t+l 
1-'t - IJt+l (J.tt -Xt) 
(3.8) 
(3.9) 
The recursive forms (3.6) and (3.9) show that the on­
line update may be seen as gradient descent with dif­
ferent learning rates. The update (3.6) uses the gradi­
ent of the loss at ù-'t+l, while (3.9) uses the gradient of 
the loss evaluated at 1-'t. Finally, (3.8) expresses the 
new expectation parameter p,t+1 as a convex combina­
tion of the current expectation parameter and exam­
ple. 
The hypothesis 81 at trial t represents a summary of 
the information gained from seeing the previous t -1 
examples. Minimizing (3.5) may be seen as striking 
a tradeoff between two different goals: one is to stay 
close to the current hypothesis and the other is to stay 
close to (or, equivalently, minimize the loss on) the last 
example seen. The first wants to preserve the learn­
ing gained from the past as it is encapsulated by the 
current hypothesis, while the second wants to learn 
from the last example in case it sees it again. The bal­
ance between the two goals is governed by the learning 
rate. Closeness to the current hypothesis is measured 
by the divergence boa. This way of motivating on-line 
updates was used extensively in [KW97]. Here we let 
the trade-off parameter 1/t change with time. 

36 
Azoury and Warmuth 
We distinguish between two initializations of 171 for our 
on-line algorithm (given in (3. 4) or (3.6-3.9)): 
Incremental off-line algorithm: 
1711 
= 17[/ and 17t = (17£/ + t -1)-1. 
Forward algorithm: 
1711 
= 17'81 + 1 and 17t = (171/ + t)-1. 
Note that the hypothesis ILt+J obtained by the incre­
mental off-line algorithms at the end of trial t coincides 
with the hypothesis of the batch algorithm using the 
initial t examples. In the forward algorithm the inverse 
of the learning rate is shifted forward by one. This al­
gorithm is inspired by the work of Vovk [Vov97) who 
uses a similar forward algorithm for linear regression. 
Both algorithms start with the same initial expec­
tation parameter p.1. However, in the second trial 
the incremental off-line algorithm predicts with p.2 = 
(17'81 + 1)-1(17'8111-1 + x1) while the forward algorithm 
predicts with p.2 = (17'81 + 2)-1((17.81 + 1)p.1 + XJ). 
Since the algorithms already have different predictions 
in the second trial their relative loss bounds are in gen­
eral incomparable. 
Recall that relative loss bounds are upper bounds on 
the difference between the total loss of the on-line al­
gorithm and the total loss of the off-line algorithm, 
that is 
T 
L Lt(Bt) - if (171/ a(B, 81) + L÷..r(O)). (3.10) 
!:1 
We first develop concise forms for the two terms of the 
above difference. 
Lemma 3.1 
T 
LLt(Bt) 
= 
!=1 
T 
L 17N\ F(ILt+l• P.t) 
!=1 
+1711 F(p.l) -17rö1 F(ILT+J) 
T 
-:EinPo(xt). 
t=l 
Proof: 
We essentially rewrite the loss of the algo­
rithm using the dual function F and the update (3.9): 
Lt(Bt) + In Po(xt) 
= 
G(Ot)-XtBt 
= 
p.1Bt - F(p.t) -XtBt 
= -17;J1 (J.l.t+J -P.t)Ot -F(P.t) 
= 
17t_J1 (F(P.t+!)-F(p.t) - (ILt+J -p.t)Ot) 
-17!-J1F(p.1+1) + (ry!-J1 -1)F(P.t) 
= 
17;11 F(ILt+l, P.t) -17t_J1 F(P.t+l) + 7Jt1 F(p.t). 
Finally we sum over t. 
0 
Recall that 7J.B1a(O,BJ) + L1..T(O) is minimized at 
the natural parameter (J B and expectation parameter 
ILB· 
Lemma 3.2 
T 
= 
17[/ F (p.1)- (17.81 + T)-1 F(p.s)-LIn Po(xt)· 
t=l 
Proof: 
We rewrite the argument of the inf: 
T 
17.81 a(O, 81) + Lø..r(B) + L lnPo(xt) 
t:l 
T 
= 
17s1F(ILl•IL) + L(G(O)-.xt · O)) 
t=l 
= 
7J.81 {F(p.1)-F(p.)-(p.1 -p.) · 0)} 
T 
+ L (p. . e- F (p.)- xt . e) 
t=l 
= 
7Ji/ F(p.l)- (ry81 + T)F(p.) 
T 
+(1781 + T)p. · e- (17.811-'1 +:Ext) · e. 
t:l 
Setting p. = ILB, which is the argmin of the inf (see 
(3.2)), the last two terms above cancel and the lemma 
follows. 
0 
The following lemma expresses the difference of the 
losses in one trial i.t.o. divergences. 
Lemma 3.3 For trial t = 1, ... , T and 8 E 9, 
Lt(Bt)- Lt(8) 
= 
1Jt1a(O,Ot)) -ry;J1a(O, Ot+I) 
+1Jt:J1 a(Bt. 8t+l) 
Proof: Since vt+1 (0) - 1);11 G(8) is linear (3.3), for 
t = 0, . . .  , T, we have by Property (5) that 
Thus the three divergences used in the lemma become: 
We now simply expand the three divergences: 
vt(O) -vt(8t) - (0-Bt) · Vvt(Bt) 
Vt+l (0) + Vt+l (9t+I) + (9- Bt+J) · Vvt+l (Bt+J) 
+ 
vt+J(Bt)- Vi+J(Bt+l)- (Bt-Bt+!) · Y'Vt+l(Ot+J). 

Relative Loss Bounds for On-line Density Estimation 
37 
Since 61 = argmin9Vt(6), fort= 1, . . .  , T + 1, all gra­
dients in the above are zero and the three divergences 
simplify to 
Vt(6) -vt+d6) + Vf+! (6t) -Vt(6t)· 
Since Vt(9) - vt+t(9) = -Lt(9) and Vt+d6t) 
Vt(9t) = Lt(6t), the lemma follows. 
0 
We now sum the equality of the lemma. Note that the 
second and third term on the r.h.s. telescope. 
T 
L Lt(6t)- (L1 .. r(6) + IJ11 a(6, 61)) 
t=l 
T 
= -17rō1a(6, 6r+t) + L17H\a(6t, 6t+!) 
t=l 
By plugging 6 = 9 B into the above equality we get the 
following relative loss bound (This bound can also be 
proven by subtracting the expressions of the lemmas 
(3.1) and (3.2)). 
Theorem 3.4 For both algorithms 
T 
LLt(6t) -(h.r(6s) +1781a(6s, 6d) 
t=l 
= (1711 -1781 )a(6s, 61) -17rɃ1 a(6s, 6T+1l 
T 
+ L 17tila(6t, 6t+1l 
t=l 
Note that for the incremental off-line algorithm 1711 = 
1781 and 6r+! = 6s. Thus the first two terms on the 
r.h.s. of the above equality are zero. 
3.1 
Density Estimation with a Bernoulli 
Now the examples are coin flips in {0, 1} and the 
cumulant function is G(6) = ln(l + e6). The pa-
9 
rameter transformations are IL = g(9) =  and 
l+e 
6 = f(IL) = In  and the dual to G(6) is F(IL) = 
IL lniL + (1- IL) ln(1- IL)· The loss (negative log­
likelihood) i.t.o. the expectation parameter is Lt(6) = 
-x1 lniL -(1 -x1) ln(1 -IL). It is easy to see that 
for the Bernoulli distribution the total loss of our on­
line algorithms is permutation invariant (independant 
of the order of the instances). Consider the forward 
algorithm with 1781 = 0, 1711 = 1 and IL1 = 1/2. 
In this case the batch algorithm uses the fraction of 
ones ILs = L:;TI x, and the forward algorithm uses 
1/2+L;:_, x. 
B 
. 
I . d t' 
ILt+l = 
t+l 
. 
y a s1mp e m uc wn one can 
show that the total loss is 
T 
TIL8 
T-TIL8 
'L L1(91) = ln T! -ln II (t -1/2)-ln II (t -1/2). 
t=l 
!=1 
!=1 
Thus 
T 
T 
L Lt(6t) -L Lt(6s) =In r(T + 1) 
t=l 
!=1 
In r(TILs + 1/2) -lnf(T -TILe + 1/2) -ln(7r) 
+ 
T(ILs lniLs + (1 -ILs) ln(1 -ILs)). 
This expression was first derived in [Fre96] using inte­
gration. With standard approximations of the r func­
tion the above difference between the losses can be 
upper bounded [Fre96] by t ln(T + 1) + 1. 
3.2 
Density Estimation with a Gaussian 
For a vector x E Rd, x2 is shorthand for the real x'x. 
For Gaussian density estimation the cumulant function 
is G(9) = 92/2, and the parameter transformation 
g( 9) = IL is the identity function, i.e. 6 = IL· The loss 
and the distance are quadratic, i.e. Lt ( 6t) = (ILt -
Xt)2/2 +cand a(6t,6t+l) = (ILt+l -IL1)2/2. Here c 
is a constant and we choose c = 0. We use the on-line 
updates (e.g. (3.6), (3.9)) to rewrite the divergence 
between 61 and 6t+l: 
217ti1 a(6t, 6t+l) = 17til (ILt+l -1Ltl2 
(ILt+i - ILt)(xt- ILt) 
= 
(ILt+t- ILt)(xt + ILt+d- (ILt+t-ILt)CILt+t + ILt) 
(J_3) 
17t(Xt -ILt+t)(xt + ILt+t) + IL -IL+l 
= 
17t(X- IL+l) + IL- IL+i· 
In this case the difference between the total losses of 
the on-line algorithm and the off-line algorithm (see 
Theorem 3.4) becomes: 
(1711 -1781)(1Ll-1Ls)2 /2 -17rō1 (ILT+l-1Ls)2 /2 
T 
+ 
L IJt(X - IL+l)/2 + 1LU2 -IL}+l/2 
t=l 
T 
T-i 
= L 17tXU2 - L 17tiL+l /2 
t=l 
t=l 
(1 + 17r)1Lhl/2 + 1LU2 
+ (1711 -IJ81)(1Ll-ILB)2 /2 -17T;1(ILT+l-ILB)2 /2. 
We now assume IL1 = 0 for the rest of this section. 
For the incremental off-line algorithm (when 171 = 17B 
and ILT+! = ILs) the last three terms of the above 
are zero and the fourth last is negative. Also for the 
forward update (when 1711 = 1181 + 1 and ILT+l = 
-'+T 
# 
¥ T 
IL 8), the last four terms can be shown to equal 
#B + +1 
zero. Thus the difference of the total losses can be 
bounded by 
T 
T-1 
L IJtXU2 -L l)t#L+!/2. 
!=1 
!=1 
(3. 1 1) 

38 
Azoury and Warmuth 
This bound holds for both algorithms. For the forward 
algorithm the difference is equal to the above. 
It is easy find two examples for which the above differ­
ence depends on the order in which the examples are 
presented. By dropping the last sum in (3.11) we ob­
tain an upper bound on the difference which is is now 
independent of the order (i.e. permutation invariant). 
The remaining sum can be crudely bounded by using 
T 
"L:TJt 
t=1 
T 
1è-'+T-1 
t; 1/(TJ11 + t- 1) ::; 
éê
'
,
_1 
(1/x) d x 
= ln((1J11 + T -1)/(1J11 - 1)) 
and X2 = max[=1 xr. Thus the difference of the losses 
for both algorithms is at most 
X2 
T 
-2 ln(1 + 
1 
). 
1)1 - 1 
3.3 
Density Estimation with a Gamma 
(3.12) 
The Gamma density with shape parameter a and in­
verse scale parameter (3 is 
e-Xf3xa.-1 (3"' 
P(xja,/3)= 
r(a) 
, for x, a,/3 > 0. 
This is a member of the exponential family with nat­
ural parameter 9 = -a/ (3. So the density above i.t.o. 
9 can be written as 
P(xj9, a) = eo.(X9+tn( -9)) 
r(a) . 
We assume a is known and fixed. Note that a-1 is 
called the dispersion parameter. The parameter a 
scales the Joss and the divergences. So for the sake 
of simplicity we drop a just as we ignored the fixed 
variance in the case of Gaussian density estimation. 
The cumulant function for the Gamma density is 
G(9) = - In( -9) and the parameter transformations 
are g(9) = -1/9 = p, and f(p,) = -1/p, = 9. The 
dual convex function F(p,) becomes -1 -In p,. In this 
section we only bound the divergence between 9t and 
9t+l· This essentially leads to a relative loss bound for 
the incremental off-line algorithm (See Theorem 3.4): 
1Jt;1t.a(9t,9t+t) = TJt;1t.F(I-'t+1•1-'t) 
= 
-1 (I ...!:L + 1-'t+t - 1) 
1Jt+t n 
. 
1-'t+l 
1-'t 
We now use the update (3.9) and the notation Tt = 
xtf 1-'t > 0 to rewrite the divergence: 
TJt-11 t.p (1-'t+l' 1-'t) 
-1 (1 ( 
1-'t 
_ 1Jt+1 (1-'t - Xt)) 
1Jt+l 
n 
( 
) 
1-'t - 1Jt+l 1-'t - Xt 
1-'t 
-1 
1 - Tt 
1Jt+1 ln(1 + 
1 
) + Tt - 1 
1Jt 
+ Tt 
-1 1- Tt 
< 
1Jt+l 
1 
+ Tt - 1 
1Jt 
+ Tt 
< 
(1 - Tt)2 
< 
(1 
)2 
1 + 
_ 1Jt 
- Tt 
1Jt 
Tt 
Ifx1::; p,1, then (1-r)2::; 1; if xt > p,1, then (1-r)2::; 
(
)2• Thus if X= maxf=l Xt and Z = min({xt: 1::; 
t ::; T} U {p,t} ), then (1- r)2 ::; X2 fZ2 and the sum 
of the last terms can be bounded by 
x2 T 
x2 
Z2 L: 1Jt == Z2 O(log T). 
t=1 
3.4 
The General Case 
We rewrite the divergence between 91 and 91+1: 
1Jt;1t.a(9t,9t+1) == 1Jt;1t.F(I-'t+1•1-'t) 
= 1Jt;1 (F(I-'t+t)- F(p,t)- (l-'t+1 - 1-'t) · f(p,t)). 
After doing a second order Taylor expansion of 
F(p,1+1) at 1-'t, this last term equals 
1Jt;1 (1-'t+t - 1-'t)' F;i (iit )(l-'t+1 - 1-'t) /2 
= 1Jt+1(1-'1- xt)'F;j(iitHI-'t- Xt)/2, 
where Ji,1 is a convex combination of 1-'t and 1-'t+l. If 
F;i (p,) is constant then we have a Gaussian. The gen­
eral case may be seen as a local Gaussian with the 
time-varying curvature F;j(p,). Any reasonable meth­
ods have to proceed on a case-by-case basis using the 
form of F;j (p,) (which is the inverse of the variance 
function V(p,)). Recall that 1Jt+l == 0(1/t). So sum­
ming the last term should give log(T)-style bounds. 
4 
Linear Regression 
In on-line regression the learning algorithms receive 
a sequence of labeled examples (x1,y1), (x2,y2), ... , 
( XT, YT) in R a x R. In linear regression the Joss is 
L1(9) =(x1 · 9 - y1)2 /2. The incremental off-line al­
gorithm predicts with Xt · 9t right after receiving the 
instance x1 but before receiving the label y1• It chooses 
its parameter vector in Rd by minimizing 
t-1 
(8- 8t)'rf£/(8- 81)/2 + L Lq(9), 
q=1 
where 17£/ is now some symmetric strictly positive def­
inite matrix. Thus when 61 = 0 
t 
9t+1 == 11t L Xqyq, 
q=1 
( 4.1) 

Relative Loss Bounds for On-line Density Estimation 
39 
t 
where 11t = (71"81 + L Xqxë)-1. 
q=1 
This algorithm is also known as on-line linear least 
squares or ridge regression. Vovk's forward algorithm 
[Vov97] uses the same update (4.1) but now 
t+1 
( 
-I +""' 
I )-1 
11t = 11B 
L.. XqXq 
. 
q=1 
In the full paper we will show how linear regression 
can be viewed as a generalization of Gaussian density 
estimation. The proof of the relative loss bounds are 
similar except that the learning rates are now covari­
ance matrices. In the corresponding learning rates for 
density estimation each product XqXì is one. When 
fh = 0 the difference between the total loss of both 
on-line algoritms can be shown to be at most 
T 
T-1 
LYix;11tXt/2- L (8t · Xt)2x;111xtf2. 
t=1 
t=1 
In the case of the forward algorithm the difference is 
exactly the above (See (3.11) for the corresponding ex­
pression in Gaussian density estimation). After drop­
ping the second sum, the resulting bound on the dif­
ference of the losses becomes permutation invariant. If 
Yt E [-Y, Y], Xt,i E [-X, X], and TJ"81 = al for some 
a > 0, then the remaining first sum can be roughly 
upper bounded by dr• ln(1 + T-;• ) (See (3.12) for the 
corresponding expression). 
5 
Relationship to expanded updates 
Bayes 
There is an alternate method pioneered by Vovk for 
proving relative loss bounds. A distribution is main­
tained on the set of models (or experts) e. The ini­
tial work only considered the case when e is finite 
[Vov90, LW94]. However the method can also be ap­
plied when E> is infinite (Fre96, Vov97, Yam98]. The 
distribution at the end of trial t has the form 
( ) 
P (8) -è E' 
Lq(8) 
Pt+l 8 
 
1 
e 
•=• 
, 
where P1 is a prior and TJ a learning rate. When TJ = 1 
and Lt(Xt) = -ln(P(xtl8) then Pt+l is the posterior 
after seeing the first t examples. The following type of 
inequality is the main part of the method: 
< 
= 
-ĂIn { Pt(8)e-èL,(8) 
TJ 
18 
-ĂIn r pl (8)e-èLu(8) 
TJ 
18 
+ĂIn r p1 (8)e-èLu-t(8) 
TJ 
18 
Here LA(t) denotes the loss of the algorithm at trial 
t. The predictions of the algorithm are chosen so that 
the inequality holds for all sequences of examples. Also 
the larger the learning rate TJ, the better the resulting 
relative loss bounds. The same learning rate is used 
in all trials. The inequality is often tight when the 
examples lie on the boundary of the set of possible 
examples. By summing the inequality over all trials 
one gets the bound 
(5.1) 
The integral is now bounded using Laplace's method 
of integration using the best off-line model as a saddle 
point [Fre96]. 
Freund applied Vovk's method to the Bernoulli dis­
tribution (Fre96] and obtained the same bound that is 
sketched in Section 3.1. For the Bernoulli the total loss 
of algorithms is permutation invariant. The examples 
are binary and thus always lie on the boundary and so 
the bounds are exact. For Gaussian density estimation 
the boundary of the example space is { Xt : :z: = X2}, 
for some positive X. Now the total loss of the algo­
rithms is not permutation invariant. 
Note that the r.h.s. of inequality (5.1) is permutation 
invariant. So any method that is based on inequality 
(5.1) can only lead to permutation invariant bounds. 
However Vovk's method does not seem to lead to 
the best possible permutation invariant bounds. Our 
method gives better permutation invariant bounds on 
the total loss of the forward algorithm after dropping 
some terms from an exact reformulation of the total 
loss. Only when all examples lie on the boundary then 
both methods agree. We also avoid the use of involved 
integration methods needed for (5.1) (Vov97, Fre96]. 
6 
Conclusion 
In this paper we presented techniques for proving rel­
ative loss bound for density estimation with the expo­
nential family. We gave a number of examples of how 
to apply our methods including the case of linear re­
gression. More examples will be given in the full paper. 
For an exponential density with cumulant function G 
we use the divergence tla(B,8) as the measure of dis­
tances between the parameters 8 and 8. Thus the loss 
L1(8) = -ln Pa(xío8) and divergence are based on 
the same function G. However Lemma 3.3 and the 
methodology for proving relative loss bounds can be 
generalized to the case when the loss and the diver­
gence use different strictly convex functions. 
The parameters maintained by our algorithms are in­
variant to permuting the past examples. However the 

40 
Azoury and Warmuth 
total on-line loss of the algorithms may not be permu­
tation invariant. So an adversary could use this fact 
and present the examples in an order disadvantageous 
to the learning algorithm. We are currently exploring 
methods for making the total loss of the algorithms 
permutation invariant.
