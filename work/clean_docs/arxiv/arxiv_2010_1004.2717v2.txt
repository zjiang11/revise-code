arXiv:1004.2717v2  [cs.LO]  9 Jun 2016
Completeness of Flat Coalgebraic Fixpoint Logics
LUTZ SCHR ¨ODER, Friedrich-Alexander-Universit¨at Erlangen-N¨urnberg
YDE VENEMA, ILLC, Universiteit van Amsterdam
Modal ﬁxpoint logics traditionally play a central role in computer science, in particular in artiﬁcial intelli-
gence and concurrency. The µ-calculus and its relatives are among the most expressive logics of this type.
However, popular ﬁxpoint logics tend to trade expressivity for simplicity and readability, and in fact often
live within the single variable fragment of the µ-calculus. The family of such ﬂat ﬁxpoint logics includes, e.g.,
LTL, CTL, and the logic of common knowledge. Extending this notion to the generic semantic framework of
coalgebraic logic enables covering a wide range of logics beyond the standard µ-calculus including, e.g., ﬂat
fragments of the graded µ-calculus and the alternating-time µ-calculus (such as alternating-time temporal
logic ATL), as well as probabilistic and monotone ﬁxpoint logics. We give a generic proof of completeness of
the Kozen-Park axiomatization for such ﬂat coalgebraic ﬁxpoint logics.
CCS Concepts: •Theory of computation →Modal and temporal logics; Proof theory; Description logics;
Additional Key Words and Phrases: Completeness, Kozen/Park axioms, branching-time temporal logics, coal-
gebraic logic, alternating-time temporal logic, graded µ-calculus, algebraic semantics
1. INTRODUCTION
Many of the most well-known logics in program veriﬁcation, concurrency, and other
areas of computer science and artiﬁcial intelligence can be cast as modal ﬁxpoint
logics, that is, embedded into some variant of the µ-calculus. Typical examples are
PDL [Pratt 1976] where, say, the formula ⟨a∗⟩p (‘p can be reached by ﬁnite iteration of
a’) can be expressed as the least ﬁxpoint
µX. p ∨⟨a⟩X;
CTL [Emerson and Clarke 1982], whose formula AFp (‘p eventually holds on all paths’)
is just the ﬁxpoint
µX. p ∨2X;
and the common knowledge operator C of epistemic logic [Lewis 1969], where Cp (‘it
is common knowledge that p’) can be expressed as the ﬁxpoint
νX.
n
^
i=1
Ki(p ∧X)
with n the number of agents and Ki read as ‘agent i knows that’. A common feature
of these examples is that they trade off expressivity for simplicity of expression in
comparison to the full µ-calculus.
One of the reasons why the full µ-calculus is hard for both end users and logi-
cians is that it requires keeping track of bound variables. Indeed we note that the
simpler logics listed above (in the case of PDL, the ∗-nesting-free fragment) live in
the single-variable fragment of the µ-calculus (a subfragment of the alternation-free
fragment [Emerson and Lei 1986]), which is precisely what enables one to abandon
variables altogether in favour of variable-free ﬁxpoint operators such as AF or C.
We refer to logics that embed into a single-variable µ-calculus as ﬂat ﬁxpoint log-
ics [Santocanale and Venema 2010].
Here, we study ﬂat ﬁxpoint logics in the more general setting of coalge-
braic logic. Coalgebra has emerged as the right level of generality for a uni-
ﬁed treatment of a wide range of modalities with seemingly disparate seman-
tics beyond the realm of pure relational structures. Examples include mono-
tone modalities [Chellas 1980], probabilistic
modalities [Larsen and Skou 1991],

graded modalities [Fine 1972; D’Agostino and Visser 2002], coalitional/alternating-
time modalities [Alur et al. 2002; Pauly 2002], and various non-monotonic condition-
als [Friedman and Halpern 1994; Olivetti et al. 2007]. The semantics of coalgebraic
logic is parametrized over the choice of an endofunctor on the category of sets,
whose coalgebras play the role of frames. Besides standard Kripke frames, the no-
tion of coalgebra encompasses, e.g., Markov chains, weighted automata, multigraphs,
neighbourhood frames, selection function frames [Chellas 1980], and concurrent game
structures [Alur et al. 2002]. Generic completeness results in coalgebraic logic are
parametrized over sets of rules or axioms that satisfy a local form of completeness
called one-step completeness. That is, they require completeness of a restricted logic
without ﬁxpoints and nesting of modalities that is interpreted over mere elements
of the functor rather than full-blown frames or models (e.g. in the relational base
case, over a single subset of the base set thought of as a local view on a set of suc-
cessors), a condition that is typically quite easy to establish. In fact, suitable one-step
complete axiomatizations for many examples can already be found in the literature
on coalgebraic logic [Pattinson 2003; Schr¨oder 2007; Schr¨oder and Pattinson 2009;
Kupke and Pattinson 2010].
In our ﬂat coalgebraic ﬁxpoint logics one thus can express statements such as ‘the
coalition C of agents can maintain p forever’, ‘the present state is the root of a binary
tree all whose leaves satisfy p’, or ‘p is commonly believed with reasonable certainty’.
In particular, we cover ﬂat fragments of the graded µ-calculus [Kupferman et al. 2002]
and the alternating-time µ-calculus (AMC) [Alur et al. 2002]; one such ﬂat fragment is
alternating-time temporal logic (ATL).
Our main result on ﬂat coalgebraic ﬁxpoint logics is completeness of the natu-
ral axiomatization that makes the ﬁxpoint deﬁnitions explicit, generalizing the well-
known Kozen-Park axiomatization. The axiomatization is parametric both w.r.t. the
coalgebraic branching type and the choice of ﬂat fragment, under mild restrictions
on the form of ﬁxpoint operators. This result generalizes results by Santocanale
and Venema [2010] to the level of coalgebraic logic, and relies on the notion of O-
adjointness [Santocanale 2008] to prove that ﬁxpoints in the Lindenbaum algebra are
constructive, i.e. approximable in ω steps. The crucial ingredient here are one-step cut-
free complete rule sets [Schr¨oder and Pattinson 2009; Pattinson and Schr¨oder 2010].
These enable generalizations of both the key rigidity lemma and the O-adjointness
theorem of [Santocanale and Venema 2010], the latter to the effect that all uniform-
depth modal operators are O-adjoint.
Our
completeness
result
follows
a
long
tradition
of
non-trivial
com-
pleteness
proofs
for
ﬁxpoint
logics,
e.g.
PDL
[Kozen and Parikh 1981;
Segerberg 1982],
CTL
[Emerson and Halpern 1985],
LTL
[Gabbay et al. 1980;
Lichtenstein and Pnueli 2000], the aconjunctive µ-calculus [Kozen 1983], and the
full µ-calculus [Walukiewicz 2000]. Note that all these results are independent,
as completeness is not in general inherited by sublogics, and in fact employ quite
different methods. Instantiating our generic results to concrete logics yields new
results in nearly all cases that go beyond the classical relational µ-calculus, noting
that neither Kupferman et al. [2002] nor Cirstea et al. [2011] cover axiomatizations.
In particular, we obtain for the ﬁrst time a completeness result for graded ﬁxpoint
logics, i.e. fragments of the graded µ-calculus, and we generalize the completeness of
ATL [Goranko and van Drimmelen 2006] to arbitrary ﬂat fragments of the AMC.
Further Related Work. The present paper is an extended and revised version of a
previous conference paper [Schr¨oder and Venema 2010]. The technical treatment dif-
fers from the conference version in that we weaken the assumptions of the generic
completeness theorem to require only a one-step complete rule set instead of a one-
2

step cutfree complete one; moreover, we opt for a propositional basis with unre-
stricted negation, converting to negation normal form only for purposes of the model
construction in Section 5. Flat coalgebraic ﬁxpoint logics are fragments of coalge-
braic µ-calculi, and as such known to be decidable in EXPTIME under reasonable
assumptions [Cˆırstea et al. 2011]. A tableau-based global caching algorithm for ﬂat
ﬁxpoint logics has recently been developed by Hausmann and one of the authors
(Schr¨oder) [2015], so we omit discussion of the (less practical) tableau algorithm given
in the conference version.
Organization. We recall the requisite background in coalgebraic logic and introduce
the syntax of semantics of ﬂat coalgebraic ﬁxpoint logics in Section 2. We proceed to
discuss the generic axiomatization in Section 3. In Section 4, we prove the central O-
adjointness theorem, and then present the ensuing model construction in Section 5.
Section 6 concludes.
2. FLAT COALGEBRAIC FIXPOINT LOGICS
We brieﬂy recall the generic framework of coalgebraic modal logic [Pattinson 2004;
Schr¨oder 2007], and deﬁne its extension with ﬂat ﬁxpoint operators, a fragment of
the coalgebraic µ-calculus [Cˆırstea et al. 2011]. We present the calculus in a form that
includes negation, and therefore need to pay attention to positive and negative occur-
rences of variables and subformulas; to avoid excessive repetition, we ﬁx these notions
for all notions of formula that include negation and possibly a notion of (propositional)
variable:
Deﬁnition 2.1. In any logic with negation ¬, an occurrence of a subformula in a
formula is positive if it is in the scope of an even number of negations ¬, and otherwise
negative. In logics featuring a notion of (propositional) variable, a formula φ is positive
(negative) in a variable x if all occurrences of x in φ are positive (negative).
For the rest of the paper, we ﬁx a countably inﬁnite set V of variables, with a single
distinguished recursion variable that is always called x; all other variables are param-
eter variables, typically called p or p1, p2, . . . . The ﬁrst parameter of the syntax of a ﬂat
coalgebraic ﬁxpoint logic is a (modal) similarity type Λ, i.e. an at most countable set of
modal operators with associated ﬁnite arities. The set of modal ﬁxpoint schemes γ, δ is
given by the grammar
γ, δ ::= ⊥| v | ¬γ | γ ∧δ | ♥(γ1, . . . , γn)
where ♥∈Λ is n-ary and v ∈V ; we require additionally that ﬁxpoint schemes are
positive in all variables (this is essential in case of the recursion variable x to ensure
existence of ﬁxpoints; for parameter variables, it is a mere technical convenience, as
negative occurrences of a parameter variable can be replaced with positive occurrences
of a fresh variable, with negation moved into the parameter formula). Further Boolean
operations ⊤, ∨, →, ↔are deﬁned as usual. Moreover, we abbreviate
♥(φ1, . . . , φn) = ¬♥(¬φ1, . . . , ¬φn)
for ♥∈Λ, and refer to ♥as the dual of ♥. We intend variables as place holders for
arguments and parameters of formulas deﬁning ﬁxpoint operators; as such, they serve
only technical purposes and will not form part of the actual ﬁxpoint language deﬁned
below (other than as part of modal ﬁxpoint schemes indexing ﬂat ﬁxpoint operators).
In particular, propositional variables should not be confused with propositional atoms,
which can appear also in actual formulas. Propositional atoms are incorporated into
the modal similarity type Λ as nullary operators when needed; this approach not only
3

simpliﬁes the technical presentation but it also enhances generality in that it allows
covering logics that do not have propositional atoms, such as Hennessy-Milner logic.
The second syntactic parameter is a set Γ of modal ﬁxpoint schemes γ determin-
ing the choice of ﬁxpoint operators. We require that all γ ∈Γ are guarded, i.e. that
all occurrences of the recursion variable x are under the scope of at least one modal
operator; this is not an essential restriction as every µ-calculus formula is provably
equivalent to a guarded formula [Walukiewicz 2000]. We denote substituted formu-
las γ[φ1/p1; . . . ; φn/pn; ψ/x] as γ(φ1, . . . , φn, ψ). The set F♯(Λ, Γ) or just F♯of (ﬁxpoint)
formulas φ, ψ is then deﬁned by the grammar
φ, ψ ::= ⊥| ¬φ | φ ∧ψ | ♥(φ1, . . . , φn) | ♯γ(φ1, . . . , φn)
where ♥∈Λ is n-ary and γ ∈Γ. The operator ♯γ takes least ﬁxpoints
♯γ(φ1, . . . , φn) = µx.γ(φ1, . . . , φn, x).
The name ﬂat for the ﬁxpoint operators ♯γ relates to the fact that modal ﬁxpoint
schemes γ do not contain ﬁxpoint operators. Note however that nesting of ﬂat ﬁxpoint
operators is unrestricted, i.e. the φi can be arbitrary ﬁxpoint formulas in ♯γ(φ1, . . . , φn).
We introduce greatest ﬁxpoint operators as duals of least ﬁxpoint operators: for a
modal ﬁxpoint scheme γ we denote by γ its dual, i.e. the modal ﬁxpoint scheme ¬γσ
where σ(v) = ¬v for all variables v. We then deﬁne the greatest ﬁxpoint operator ♭γ by
♭γ(φ1, . . . , φn) = ¬♯γ(¬φ1, . . . , ¬φn)
so that ♭γ(φ1, . . . , φn) = νx. γ(φ1, . . . , φn, x).
A standard example of a ﬂat ﬁxpoint logic is CTL, whose operators AU, EG can be
coded as
A[φ U ψ] = ♯(p2∨(p1∧2x))(φ, ψ)
and
EG φ = ♭p∧3xφ.
Note here that p ∧3x is equivalent to the dual ¬(¬p ∨2¬x) of p ∨2x.
Syntactically, ♯γ is regarded as an atomic operator; in particular, occurrences of vari-
ables in γ do not count as occurrences in formulas ♯γφ. For the sake of readability,
we restrict the further technical development to unary modalities ♥∈Λ and unary
ﬁxpoint operators, i.e. we assume that every γ ∈Γ has only one parameter variable,
denoted by p throughout; the extension to higher arities is a mere notational issue, and
in fact we continue to use higher-arity modalities and ﬁxpoint operators in the exam-
ples. Note that we have not included variables in the deﬁnition of ﬁxpoint formulas. A
(ﬁxpoint) formula with variables is an expression of the form γσ, where γ is a modal
ﬁxpoint scheme and σ is a substitution of some of the variables in γ with ﬁxpoint for-
mulas (i.e. variables never appear under ﬁxpoint operators). In the following, the term
formula will refer to ﬁxpoint formulas without variables unless variables are explic-
itly mentioned. We sometimes indicate the occurrence of a variable v in a formula ψ by
writing ψ(v), and then write ψ(ρ) for the formula obtained by substituting a formula ρ
for v. For a modal ﬁxpoint scheme γ(p, x), we denote the function taking a formula ψ
to γ(φ, ψ) by γ(φ), and by γ(φ)k its k-fold iteration.
The logic is further parametrized semantically over the underlying class of systems
and the interpretation of the modal operators. The former is determined by the choice
of a functor
T : Set →Set,
i.e. an operation T that maps sets X to sets T X and functions f : X →Y to functions
T f : T X →T Y , preserving identities and composition. The role of models is then
played by T -coalgebras, i.e. pairs (X, ξ) where X is a set of states and
ξ : X →T X
4

is the structure map. Thinking of T X informally as a parametrized datatype over X,
we regard ξ as associating with each state x a structured collection ξ(x) of successor
states and observations. E.g. for T X = PX × PU, with U a ﬁxed set of propositional
atoms and P denoting the covariant powerset functor (with Pf(A) = f[A] for f : X →Y
and A ∈P(X)), we obtain that T -coalgebras are Kripke models, as they associate with
each state a set of successor states and a set of valid propositional atoms. Our main
interest here is in examples beyond Kripke semantics, see Example 2.3.
Given T , the interpretation of the modalities is determined by associating with each
♥∈Λ a predicate lifting [[♥]] for T . Here, a predicate lifting (for T ) is a family of maps
λX : PX →PT X, where X ranges over all sets, satisfying the naturality condition
λX(f −1[A]) = (T f)−1[λY (A)]
for all f : X →Y , A ∈PY . In other words, a predicate lifting is a natural transforma-
tion
λ : Q →Q ◦T op
where Q : Setop →Set denotes the contravariant powerset functor, given by QX being
the powerset of X and Qf(A) = f −1[A] for f : X →Y and A ⊆Y . The idea is that a
predicate lifting λX converts a predicate on the set X of states into a predicate on the
set T X of structured collections over X. The basic example, for T X = P(X) × P(U) as
above, is
[[2]]X(A) = {(B, P) | B ⊆A},
which induces precisely the usual semantics of the box when composed with taking
preimages under the structure map, as in the clause for the semantics of modalities
given below. Given a predicate lifting [[♥]], we also have a predicate lifting [[♥]] for the
dual modality ♥, given by
[[♥]]X(A) = X −[[♥]]X(X −A).
E.g. for 2 as above and 3 = 2, we have [[3]]X(A) = {(B, P) | B ∩A ̸= ∅}.
As we work with ﬁxpoints, we insist that all modal operators are monotone, i.e.
[[♥]]X : P(X) →P(T X) is monotone w.r.t. set inclusion for each ♥∈Λ. The semantics
of a formula φ with recursion variable x (no other variables will ever be evaluated in
unsubstituted form) is a subset [[φ]](X,ξ)(B) ⊆X, depending on a T -coalgebra (X, ξ) and
a set B ⊆X serving as the interpretation of x. The semantics of formulas φ without
variables (in particular of ♯-formulas) will not depend on B and hence will be denoted
just by [[φ]](X,ξ); we write x |=(X,ξ) φ for x ∈[[φ]](X,ξ). The set [[φ]](X,ξ)(B) is deﬁned by
recursion over φ:
[[x]](X,ξ)(B) = B
[[¬φ]](X,ξ)(B) = X \ [[φ]](X,ξ)(B)
[[φ ∧ψ]](X,ξ)(B) = [[φ]](X,ξ)(B) ∩[[ψ]](X,ξ)(B)
[[♥φ]](X,ξ)(B) = ξ−1[[♥]]X([[φ]](X,ξ)(B))
[[♯γφ]](X,ξ) =
\
{B ⊆X | [[γ(φ)]](X,ξ)(B) ⊆B}
The clause for ♯γφ just says that [[♯γφ]](X,ξ) is the least ﬁxpoint of the map [[γ(φ)]](X,ξ) :
P(X) →P(X), which is monotone because all modalities are monotone and modal
ﬁxpoint schemes are positive in the recursion variable. We ﬁx the data T , Λ, Γ etc.
throughout.
5

We recall that given T -coalgebras (X, ξ) and (Y, ζ), a T -coalgebra morphism f :
(X, ξ) →(Y, ζ) is a map f : X →Y making the diagram
X
ξ

f
/ Y
ζ

T X
T f / T Y
commute. Flat ﬁxpoint formulas are invariant under coalgebra morphisms:
LEMMA 2.2.
Let f : (X, ξ) →(Y, ζ) be a T -coalgebra morphism, let B ⊆Y , and let
φ be a ﬂat ﬁxpoint formula. Then
[[φ]](X,ξ)(f −1[B]) = f −1[[[φ]](Y,ζ)(B)].
(1)
(The lemma holds more generally for the full coalgebraic µ-calculus, with essentially
the same proof; we refrain from stating it in more generality here to avoid introducing
additional notation.)
PROOF. Induction over φ; the Boolean cases are trivial, and the modal cases are by
naturality of predicate liftings (cf. [Pattinson 2004]). We are left with the ﬁxpoint case;
we work with µ-calculus notation, i.e. our remaining case is of the form µx. φ, a closed
formula because there is only one recursion variable. That is, we are to show that
[[µx. φ]](X,ξ) = f −1[[[µx. φ]](Y,ζ)].
(2)
It is well-known that we can approximate least ﬁxpoints of monotone functions
from below using ordinal-indexed chains. Speciﬁcally, [[µx. φ]](X,ξ) is the union of the
sets [[φ]]α
(X,ξ)(∅) indexed over ordinals α, deﬁned by [[φ]]0
(X,ξ)(∅) = ∅, by [[φ]]α+1
(X,ξ)(∅) =
[[φ]](X,ξ)([[φ]]α
(X,ξ)(∅)) in the successor step, and by [[φ]]α
(X,ξ)(∅) = S
β<α [[φ]]β
(X,ξ)(∅) in the
limit step; analogously, [[µx. φ]](Y,ζ) is approximated from below by sets [[φ]]α
(Y,ζ)(∅). Since
taking preimages under f commutes with [[φ]] by the inductive hypothesis, and gener-
ally commutes with (inﬁnite) unions and preserves ∅, an easy transﬁnite induction
shows that
[[φ]]α
(X,ξ)(∅) = f −1[[[φ]]α
(Y,ζ)(∅)]
for all α. The inductive claim follows by forming the union over all α on both sides,
again using commutation of preimages with unions.
Example 2.3.
We
discuss
select
examples covered by
the
coalgebraic ap-
proach,
starting
with
a
more
detailed
exposition
of
the
basic
example
of
Kripke semantics and then moving on beyond. More examples are found, e.g.,
in [Schr¨oder and Pattinson 2009; Pattinson and Schr¨oder 2010]. For the sake of read-
ability, we elide the treatment of propositional atoms in all examples except Kripke
semantics, as the technicalities are the same in all cases.
(1) Kripke semantics: In mild generalization of the basic example discussed above,
ﬁxpoint extensions of the modal logics Km have modal operators 2i for i = 1, . . . , m,
interpreted over the functor T given on sets by T X = (PX)m × PU using the predicate
liftings J2iKX(A) = {(B1, . . . , Bm, P) ∈(PX)m × PU | Bi ⊆A}. Coalgebras for T
are in 1-1 correspondence with m-relation Kripke models, and [[2i]] captures the usual
semantics of the box operators. Atomic propositions are modelled as nullary modalities
a ∈Λ, interpreted by nullary predicate liftings
[[a]]X = {(B1, . . . , Bm, P) ∈(PX)m × PU | a ∈P}.
6

CTL, ∗-nesting-free PDL, and the logic of common knowledge all are ﬂat ﬁxpoint logics
in this setting. The CTL operators have been exempliﬁed above; the operator [a∗] of
PDL is the greatest ﬁxpoint operator
[a∗] = ♭p∧[a]x;
and the common knowledge operator C is the greatest ﬁxpoint operator ♭γ for
γ = p ∧
m
^
i=1
2ix.
Although our main result does not support completely arbitrary ﬁxpoint operators,
it does (like already [Santocanale and Venema 2010]) cover operators that go beyond
CTL. E.g. the operator ‘on every path, every even state satisﬁes . . . ’, which is not ex-
pressible in CTL [Emerson 1990; Wolper 1983], is the greatest ﬁxpoint operator
♭p∧22x.
Strictly speaking one should note that so far, we have not covered CTL in the standard
sense, according to which models are assumed to be serial, i.e. every state is required
to have at least one successor. We model this requirement coalgebraically by replacing
T as above with the functor T ′X = P∗X × PU where P∗is the non-empty powerset
functor P∗, i.e. P∗X is the set of non-empty subsets of X. (Since CTL standardly uses
only one next-step modality AX, we omit the exponent m.) We distinguish the arising
ﬁxpoint logics by the adjective serial, and refer to absence of the seriality requirement
by the adjective non-serial.
Further restricting the semantics to require that every state has exactly one successor
in fact produces the usual discrete-linear-order semantics of LTL (where 2 is typically
denoted #). Coalgebraically, this is reﬂected in using the functor T ′′X = X × PU; we
refer to T ′′-coalgebras as deterministic Kripke models. (For purposes of model check-
ing, LTL is more generally interpreted over labelled transition systems, but the more
restrictive discrete-linear-order semantics is equivalent for purposes of satisﬁability.)
(2) Graded
ﬁxpoint
logics
are
sublogics
of
the
graded
µ-
calculus [Kupferman et al. 2002]. They have modal operators 3k ‘in more than k
successors’, with duals □k = 3k ‘in all but k successors’. We interpret them over the
functor B that takes a set X to the set
BX = X →N ∪{∞}
of multisets over X (with possibly inﬁnite multiplicities) by
[[3k]]X(A) = {µ ∈BX | µ(A) > k}
where we use µ
∈
BX as an N ∪{∞}-valued measure, i.e. write µ(A)
=
P
x∈A µ(x).
This
captures
the
semantics
of
graded
modalities
over
multi-
graphs [D’Agostino and Visser 2002], which is equivalent to the more customary
Kripke semantics [Fine 1972] w.r.t. satisﬁability of ﬁxpoint formulas (Lemma 2.4).
In
description
logic,
graded
operators
are
called
qualiﬁed
number
restric-
tions [Baader et al. 2003]. The example mentioned in [Kupferman et al. 2002], a
graded ﬁxpoint formula expressing that the current state is the root of a ﬁnite binary
tree all whose leaves satisfy p, can be expressed by the ♯-operator for
p ∨31x.
Similarly, the ♯-operator for
p ∨□kx
7

expresses that p holds somewhere on every inﬁnite k+1-ary tree starting at the current
state. To add an example where the recursion variable x appears under more than one
modality, the ♯-operator for
p ∨3131x
expresses that the current state is the root of a ﬁnite binary tree all whose leaves are
at even distance from the root and satisfy p.
(3) Probabilistic ﬁxpoint logics, i.e. ﬁxpoint extensions of probabilistic modal
logic
[Larsen and Skou 1991;
Fagin and Halpern 1994;
Heifetz and Mongin 2001],
have modal operators Lr ‘in the next step, it holds with probability at least r that’,
for r ∈[0, 1] ∩Q. They are interpreted over the functor D that maps a set X to the set
of discrete probability distributions on X by putting
[[Lr]]X(A) = {P ∈DX | PA ≥r}.
Coalgebras for D are Markov chains. Flat probabilistic ﬁxpoint logics in this
sense are fragments of the probabilistic µ-calculus in the sense introduced by
Cˆırstea et al. [2011] (to be distinguished from the [0, 1]-valued logic of the same
name [Morgan and McIver 1997; Huth and Kwiatkowska 1997]) as an instance of the
coalgebraic µ-calculus. The probabilistic µ-calculus was subsequently reinvented by
Liu et al. [2015] (Liu et al. prove that model checking the probabilistic µ-calculus is
in UP∩COUP and satisﬁability is in 2EXPTIME while Cˆırstea et al. already show that
satisﬁability is EXPTIME-complete). More generally, one can admit linear inequalities
between probabilities, as, e.g., in work by Fagin and Halpern [1994], as long as one
pays attention to monotonicity; this allows, e.g., for statements of the form ‘more prob-
ably than not’.
In a view of probabilistic logic as a logic of reactive systems, we can use the ♭-operator
AGr = ♭p∧Lrx
to express formulas like AGr ¬fail, stating that the system will, at any point during its
run time, fail with probability at most 1 −r; a sensible speciﬁcation for systems that
may sometimes fail but should not fail excessively often (as announced, we silently
include propositional atoms such as fail in the syntax).
Alternatively, we may interpret the operators Lp epistemically. We extend the logic to
multiple agents in the same way as for K in Item 1, obtaining a logic with probabilistic
operators Li
r read ‘agent i believes with conﬁdence r that’. We then have an uncertain
variant Cr of the common knowledge operator, namely the ♭-operator for
Vm
i=1 Li
r(p ∧x).
Thus, Crφ is read ‘everyone believes with conﬁdence r that φ holds and that everyone
believes with conﬁdence r that φ holds etc.’, in short ‘φ is commonly believed with
conﬁdence r’.
A variant is the operator Cr,q which, when applied to a formula φ, separates belief in φ
from beliefs about other agents: Cr,q is the ♭-operator for
Vn
i=1(Li
rp ∧Li
qx),
and thus Cr,qφ states that everyone believes φ with conﬁdence r, and believes with
conﬁdence q that all agents believe the same, etc. (Note that Cr,r is not the same as Cr!)
(4) Conditional ﬁxpoint logics have a single binary modal operator ⇒, written
in inﬁx notation. The intended reading of a ⇒b is ‘if a, then normally b’. Con-
ditional logics come with a wide variety of axiomatizations and semantics (see,
e.g., [Pattinson and Schr¨oder 2010] for an overview). E.g., the minimal conditional
logic CK is interpreted over the functor Cf that maps a set X to the set PX →PX
8

(more precisely QX →PX, where Q is contravariant powerset), whose coalgebras are
selection function models [Chellas 1980], by putting
[[⇒]]X(A, B) = {f ∈Cf X | f(A) ⊆B}.
(Thus, ⇒is monotone in the second but not antimonotone in the ﬁrst argument, which
is precisely the point of inventing it. Technically, this is ﬁne as long as we form ﬁxpoints
only over the second argument, i.e. let the recursion variable x appear in γ ∈Γ only to
the right of ⇒as in the example below.)
We
can
combine
coalgebraic
logics
freely
using
results
of [Schr¨oder and Pattinson 2007]. E.g., combining conditional logic with multi-agent K
in its description logic incarnation ALC [Baader et al. 2003], we can deﬁne an abstract
concept of animal taxa with two-gender descendancy as ♭γ(FirstOfItsKind, Male, Female)
with γ given as
(¬p1) ⇒((∃hasParent. p2 ⊓x) ⊓(∃hasParent. p3 ⊓x)).
A taxon is a ﬁxpoint of γ(FirstOfItsKind, Male, Female) if all individuals that do not belong
to some assumed ﬁrst ancestor generation of the species normally have two parents of
the same species, and the greatest ﬁxpoint subsumes all animals belonging to such
taxa. The use of the conditional ⇒instead of standard material implication →takes
into account that these days, a given exceptional sheep might, e.g., be a clone and thus
not have parents in the strict sense.
(5) The alternating-time µ-calculus (AMC) [Alur et al. 2002] has modal operators
⟨⟨C⟩⟩⃝read ‘coalition C of agents can enforce . . . in one step’, where a coalition C is a
subset C ⊆N of a ﬁxed set N = {1, . . . , n} of agents; we shall also write [C] in place
of ⟨⟨C⟩⟩⃝as in coalition logic [Pauly 2002]. The semantics of the coalitional modali-
ties is deﬁned over concurrent game structures (or game frames), and can be captured
coalgebraically [Schr¨oder and Pattinson 2009]: We deﬁne a functor G by
GX = {(f, (ki)i∈N) | f :
  Q
i∈N

[ki] →X}
where ki ∈N and [ki] = {1, . . ., ki}, and by Gg(f, (ki)i∈N) = (g ◦f, (ki)i∈N) for g : X →Y .
This captures a form of concurrent game where each agent i ∈N chooses a move
ji ∈[ki] and the joint choice determines an outcome f(j1, . . . , jn) ∈X. (The semantics
given by Pauly [2002] differs slightly in that the agents can have unrestricted sets of
available moves rather than only ﬁnite ones.) Coalgebras (X, ξ) for G are concurrent
game structures [Alur et al. 2002]; they associate to each state x ∈X a concurrent
game ξ(x) ∈GX whose outcomes are states, and thus allow for plays with multiple
successive moves. The semantics of the modalities [C] is given by the liftings
J[C]KX(A) = {(f, (ki)i∈N) ∈GX | ∃(ji ∈[ki])i∈C. ∀(ji ∈[ki])i∈N\C. f((ji)i∈N) ∈A}.
That is, a state x in a concurrent game structure satisﬁes [C]φ if the agents in C have
a joint choice of moves such that regardless of the choice of moves by the other agents,
the outcome satisﬁes φ.
One of the ﬂat fragments of the AMC is Alternating-Time Temporal Logic
(ATL) [Alur et al. 2002]. E.g., the ATL-operator ⟨⟨C⟩⟩p1Up2, read ‘coalition C can even-
tually force p2 and meanwhile maintain p1’, is the ♯-operator for
p2 ∨(p1 ∧[C]x).
As already in the case of CTL, ﬂat ﬁxpoints in the AMC go considerably beyond ATL;
e.g. the ♭-operator for p ∧[∅][∅]x (‘p holds in all even states along any path’) is not even
in the more expressive logic ATL∗[Alur et al. 2002; Dam 1994]. A similar ﬂat operator,
the ♭-operator for [C](p ∧[D](q ∧x)), expresses that coalitions C and D can forever play
ping-pong between p and q.
9

(6) Monotone ﬁxpoint logics have a modal operator 2, interpreted over the monotone
neighbourhood functor deﬁned by
MX = {A ∈P(PX) | A upwards closed}
by means of the predicate lifting
[[2]]X(A) = {A ∈MX | A ∈A}.
(The functor M acts on maps f : X →Y by Mf(A) = {B ∈PY | f −1[B] ∈A}, and
hence is a subfunctor of the double contravariant powerset functor.) Often, the axioms
2⊤and 3⊤are imposed where 3 = 2 denotes the dual of 2. This amounts to using
the subfunctor Ms of M given by
MsX = {A ∈MX | ∅/∈A ∋X},
the serial monotone neighbourhood functor, whose coalgebras are serial monotone
neighbourhood frames. In particular, these form the semantic setting of concurrent
PDL [Peleg 1987] and Parikh’s game logic [Parikh 1985], where additionally opera-
tors are indexed over atomic programs or games, respectively; this is modelled coalge-
braically in the same way as multi-modal K (Item 1). The ∗-nesting-free fragments of
concurrent PDL and game logic are ﬂat ﬁxpoint logics. E.g., game logic has operators
⟨γ⟩, indexed over composite games γ and read ‘Angel has a strategy to enforce . . . in
game γ’. Games are formed from atomic games using the usual constructs for regular
expressions as in PDL, and additionally the dualizing operator (−)d which swaps the
roles of the players in a game. E.g. one has a demonic iteration operator (−)× deﬁned
by γ× = ((γd)∗)d. The formula ⟨γ×⟩φ thus says that Angel has a strategy to enforce φ in
the game where γ is played repeatedly, with Demon choosing the number of rounds. If
γ is star-free (i.e. contains neither (−)∗nor (−)×), we can phrase ⟨γ×⟩as the ♭-operator
for p ∧⟨γ⟩x.
It remains to show the mentioned equivalence of the Kripke semantics and the multi-
graph semantics of the graded µ-calculus (Example 2.3.2), generalizing the equiva-
lence for the ﬁxpoint-free case [Schr¨oder 2007].
LEMMA 2.4.
A formula in the ﬂat graded µ-calculus is satisﬁable over (ﬁnite)
Kripke frames iff it is satisﬁable over (ﬁnite) multigraphs.
PROOF. For the sake of simplicity, we continue to elide propositional atoms. Since
every Kripke frame can be regarded as a multigraph, ‘only if’ is clear. To show ‘if’, let
φ be a ﬂat graded ﬁxpoint formula, and let x0 be a state in a B-coalgebra (X, ξ) such
that x0 |=(X,ξ) φ. Let k0 be maximal such that 3k0 occurs in φ. Observe that φ remains
satisﬁed if we replace ξ with ξ′ where ξ′(x)(y) = min(ξ(x)(y), k0 + 1) (formally, this is
proved by induction on φ), so we can assume that all ξ(x)(y) are ﬁnite (in fact, at most
k0 + 1). Now construct a Kripke model ( ¯X, R) by making sufﬁciently many copies of
states, as in [Schr¨oder 2007, Remark 6]: take as states in ¯X all pairs (y, j) ∈X × N
such that ξ(x)(y) > j for some x, and in this case put (x, i)R(y, j) for all i such that
(x, i) ∈
¯X. Note that ¯X is ﬁnite if X is ﬁnite. Like for any Kripke frame, we can
equivalently regard ( ¯X, R) as a multigraph ( ¯X, ¯ξ) where ¯ξ(x, i)(y, j) = 1 if (x, i)R(y, j),
and ¯ξ(x, i)(y, j) = 0 otherwise. Let π : ¯X →X denote the projection that maps (x, i) to
x. By Lemma 2.2, it sufﬁces to show that
π : ( ¯X, ¯ξ) →(X, ξ)
is a B-coalgebra morphism, i.e. that
Bπ(¯ξ(x, i)) = ξ(x).
10

Indeed the multiplicity of y ∈X in the multiset on the left hand side is the cardinality
of the set {(y, j) ∈¯X | (x, i)R(y, j)}, which equals ξ(x)(y) by construction of R.
3. THE GENERIC AXIOMATIZATION
The generic semantic and syntactic framework of the previous section comes with a
generic, parametrized deduction system, whose completeness will be our main result.
We begin with the ﬁxed part of the deduction system. We include full propositional
reasoning, i.e. introduction of substituted propositional tautologies and modus ponens.
Fixpoints are governed by the obvious Kozen-Park axiomatization: we have the unfold-
ing axiom
γ(φ, ♯γφ) →♯γφ
and the ﬁxpoint induction rule
γ(φ, χ) →χ
♯γφ →χ
,
for all formulas φ, χ; together, these axioms capture the fact that ♯γφ is the least pre-
ﬁxpoint of γ(φ).
LEMMA 3.1.
The monotonicity and congruence rules
φ →ψ
♯γφ →♯γψ
φ ↔ψ
♯γφ ↔♯γψ
are derivable.
PROOF. The congruence rule is derivable from the monotonicity rule. To derive the
latter, assume φ →ψ. Since γ is positive in the parameter variable, we can then derive
γ(φ, ♯γψ) →γ(ψ, ♯γψ). By unfolding, we derive γ(φ, ♯γψ) →♯γψ, and then ♯γφ →♯γψ by
ﬁxpoint induction.
The variable part of the proof system is the axiomatization of the modal operators,
which turns out to be completely orthogonal to the ﬁxpoint axiomatization. In fact, we
can just reuse complete rule sets for the purely modal part of the logic [Schr¨oder 2007;
Schr¨oder and Pattinson 2009]. First some notation.
Deﬁnition 3.2. We denote the set of propositional formulas over a set Z by Prop(Z),
and the set {♥a | ♥∈Λ, a ∈Z} by Λ(Z). A literal over Z is either an element of Z or the
negation of such an element, i.e. has the form ǫz where z ∈Z and ǫ ∈{·, ¬} is either
nothing or negation. A (disjunctive) clause is a ﬁnite (possibly empty) disjunction of
literals; a conjunctive clause is a ﬁnite conjunction of literals. A clause (disjunctive or
conjunctive) is contracted if it contains every literal at most once. For φ, ψ ∈Prop(Z),
we say that φ propositionally entails ψ, and write φ ⊢PL ψ, if φ →ψ is a propositional
tautology. Similarly, Φ ⊆Prop(Z) propositionally entails ψ (Φ ⊢PL ψ) if there exist
φ1, . . . , φn ∈Φ such that φ1 ∧· · · ∧φn ⊢PL ψ. We write 2 for the set {⊥, ⊤} of truth
values. For φ ∈Prop(Z), we denote the evaluation of φ in the Boolean algebra PX
under a valuation τ : Z →PX by [[φ]]X,τ, and write X, τ |= φ if [[φ]]X,τ = X. For
ψ ∈Prop(Λ(Prop(Z))), the interpretation JψKT X,τ of ψ in the Boolean algebra P(T X)
under τ is the inductive extension of the assignment J♥(z)KT X,τ = J♥KXτ(z). We write
T X, τ |= ψ if JψKT X,τ = T X. A propositional formula over Λ(V ) is injective if it mentions
every variable at most once.
We can now give the formal deﬁnition of the modal rule format.
Deﬁnition 3.3.
A one-step rule R = φ/χ consists of a premise φ ∈Prop(V ) and a
conclusion χ which is an injective (disjunctive) clause over Λ(V ) (recall that V is the
11

set of variables), where every variable in φ appears also in χ. We say that R is monotone
(a notion similar to one introduced by Cirstea et al. [2011] for rules phrased without
negation) if whenever χ is positive (negative) in a variable a ∈V then φ is positive
(negative) in a. The rule R is one-step sound if whenever X, τ |= φ for a valuation
τ : V →PX, then T X, τ |= χ. A set R of one-step rules is one-step complete if, whenever
T X, τ |= ψ for a set X, an injective clause ψ over Λ(V ), and a PX-valuation τ, then ψ
is provable over X, τ, i.e. propositionally entailed by clauses χσ where φ/χ ∈R and σ
is a Prop(V )-substitution such that X, τ |= φσ. Moreover, R is one-step cutfree complete
if, whenever T X, τ |= ψ for X, τ, ψ as above, then ψ is cutfree provable over X, τ, i.e.
χ ⊢PL ψ for some φ/χ ∈R such that X, τ |= φ.
Remark 3.4. In the terminology of [Schr¨oder and Pattinson 2009], one-step cutfree
complete rule sets correspond to one-step complete rule sets which are closed under
contraction, resolution, and injective renamings of the propositional variables. Notice
in particular that χ as in the deﬁnition of cutfree provability is injective, being the
conclusion of a one-step rule, and hence contracted.
As the last parameter of the framework, we
ﬁx from now on a one-step complete set R of one-step sound one-step rules,
and denote the arising logic by L♯.
Rules φ/ψ ∈R are applied in substituted form, i.e for every substitution σ, we may
conclude ψσ from φσ. In summary, the proof system consists of propositional reasoning,
the unfolding axiom, the ﬁxpoint induction rule, and the rules in R. It is easy to see
that this system is sound. As usual, we write ⊢φ if a formula φ is provable. We say
that φ is consistent if ¬φ is not provable. One should note that the system without the
ﬁxpoint rules (i.e. comprising only propositional reasoning and a one-step complete set
of modal rules) is complete for the ﬁxpoint-free language [Schr¨oder 2007].
We conclude the technical part of the section with some facts on the relationship
between the two notions of one-step completeness.
Deﬁnition 3.5. A one-step rule φ/χ is R-derivable if it is propositionally entailed by
conclusions ψσ of rules ρ/ψ ∈R where σ is a Prop(V )-substitution and φ ⊢PL ρσ.
LEMMA 3.6.
All one-step sound one-step rules are R-derivable.
PROOF. Let φ/χ be one-step sound. Let V0 ⊆V be the set of propositional variables
that occur in χ, and put
X = {κ : V0 →2 | κ |= φ}
τ(a) = {κ ∈X | κ(a) = ⊤}.
Then X, κ |= φ, so that T X, κ |= χ by one-step soundness. By one-step completeness
of R, χ is propositionally entailed by clauses ψσ where ρ/ψ ∈R and σ is a Prop(V )-
substitution such that X, τ |= ρσ. It sufﬁces to show that φ ⊢PL ρσ, which however is
clear by construction of X, τ.
LEMMA 3.7. The set of R-derivable monotone one-step rules is one-step cutfree com-
plete.
In other words, a clause over Λ(V ) is provable over X, τ iff it is cutfree provable over X, τ
using an R-derivable monotone one-step rule. We thus say that a conjunctive clause ρ
over Λ(V ) is one-step τ-consistent for τ : V →PX if ¬ρ is not provable over X, τ,
equivalently not cutfree provable over X, τ using an R-derivable monotone one-step
rule.
PROOF. By Lemma 3.6, this follows once we show that the set of one-step sound
monotone one-step rules is one-step cutfree complete. This is proved by Cirstea et
12

al. [2011, Proposition 4.7] for a formally even more restrictive class of rules (also called
monotone1).
Example 3.8.
One-step complete rule systems have been exhibited for all log-
ics of Example 2.3 and many more [Pattinson 2003; Cˆırstea and Pattinson 2007;
Schr¨oder and Pattinson 2009;
Pattinson and Schr¨oder 2010;
Kupke and Pattinson 2010].
In
some
cases
[Cˆırstea and Pattinson 2007;
Pattinson 2003], axiomatizations have been phrased in terms of one-step axioms,
i.e. formulas in φ ∈Prop(Λ(Prop(V ))) that can be introduced in substituted form φσ
with σ a substitution of propositional variables by formulas; it has been shown that
this format is interconvertible with one-step rules [Schr¨oder 2007]. We recall some
examples in more detail, converting to one-step rules where necessary.
(1) Kripke semantics: The standard axiomatization of the modal logic K1, with 21
written as just 2, can be phrased in terms of one-step rules for necessitation, mono-
tonicity, and normality as
a
2a
a →b
2a →2b
a ∧b →c
2a ∧2b →2c
where here and in the following, we write clauses ¬a1 ∨· · · ∨¬an ∨b1 ∨. . . bm as im-
plications a1 ∧· · · ∧an →b1 ∨· · · ∨bm. Given this axiomatization, a one-step rule
φ/ψ is R-derivable iff ψ contains disjuncts ¬2a1, . . . , ¬2an, 2b (with n ≥0) such that
φ ⊢PL a1 ∧· · · ∧an →b.
A more restricted semantics over serial Kripke models as for serial CTL (Exam-
ple 2.3.1) is reﬂected in the additional rule
¬a
¬2a.
Restricting additionally to deterministic models as used for LTL (and switching from
2 to # as customary) logically corresponds to adding the rule
a ∨b
#a ∨#b
(which, given the other rules, is mutually interderivable with the better-known axiom
¬ # a →#¬a).
(2) Graded ﬁxpoint logics: Rephrasing a known complete axiomatization of graded
modal logic [De Caro 1988], we obtain the rules
(RG1)
a →b
3n+1a →3nb
(A1)
c →a ∨b
3n1+n2c →3n1a ∨3n2b
(A2)
a ∨b →c
a ∧b →d
3n1a ∧3n2b →3n1+n2+1c ∨30d
(RN)
¬a
¬30a.
These rules are clearly one-step sound. They have previously been shown to be one-
step complete [Schr¨oder and Pattinson 2009] by reference to the previous complete-
ness proof for graded modal logic; we give a simple stand-alone proof in Lemma 3.10.
(3) Probabilistic ﬁxpoint logics: We can reuse the one-step complete rule set for
probabilistic modal logic [Heifetz and Mongin 2001; Cˆırstea and Pattinson 2007]. For
1Speciﬁcally, a rule is monotone in the sense of Cirstea at al. if its premise is a positive formula and its
conclusion is a disjunction of atoms of the form ♥a or ♥a; in fact, monotone rules in our sense can be
transformed into this format by taking negation normal forms and substituting away negated variables.
13

the extended language with linear inequalities on probabilities, one has the one-
step cutfree complete rule set given by Kupke and Pattinson [2010], noting that one-
step cutfree complete rule sets can be restricted to any subset of the modal opera-
tors [Schr¨oder and Pattinson 2009], in particular to monotone linear inequalities.
(4) Conditional ﬁxpoint logics: one-step complete rule sets are known for various
ﬂavours of conditional logic [Pattinson and Schr¨oder 2010; Schr¨oder et al. 2010].
(5) Alternating-time µ-calculus: The following one-step complete set of rules is im-
plicit in [Pauly 2002] (see also [Schr¨oder and Pattinson 2009]):
¬a
¬[C]a
a
[C]a
a ∨b
[0]a ∨[N]b
a ∧b →c
[C]a ∧[D]b →[C ∪D]c
where C and D are disjoint in the last rule. In words: no coalition can enforce the logi-
cally impossible; every coalition can enforce logical tautologies; either a is unavoidable
or ¬a can be enforced by all agents in collaboration; and disjoint coalitions can combine
their abilities.
(6) Monotone ﬁxpoint logics: When we interpret 2 over the monotone neighbourhood
functor, we have only the rule
a →b
2a →2b.
Seriality is captured by the additional rules
a
2a
¬a
¬2a.
Remark 3.9.
We point out that in all examples with ﬁnite modal similarity type Λ,
the rule sets given above are ﬁnite, so that our completeness result will establish ﬁ-
nite axiomatizability; this holds in particular for alternating-time logics. When Λ is
inﬁnite, we cannot reasonably expect a ﬁnite axiomatization. The rules for graded
modalities are locally ﬁnite in the sense that every modality is mentioned only
in ﬁnitely many axioms. The rules for probabilistic logic [Heifetz and Mongin 2001;
Cˆırstea and Pattinson 2007] do not have this property, and it remains open whether a
locally ﬁnite axiomatization is possible in this case.
We conclude the section with the announced stand-alone proof of one-step complete-
ness of the rules for graded modal logic:
LEMMA 3.10.
The rules (RG1), (A1), (A2), and (RN) (Example 3.8.2) are one-step
complete for graded modal logic.
PROOF. Let τ : V →PX. We extend the notion of one-step τ-consistency to inﬁnite
conjunctive clauses, i.e. inﬁnite sets of literals, over Λ(V ) in the evident way: a set Φ of
literals over Λ(V ) is one-step τ-consistent if for all ρ1, . . . , ρn ∈Φ, ρ1 ∧· · ·∧ρn is one-step
τ-consistent. As usual, one-step completeness dualizes to showing that a given one-
step τ-consistent clause φ is one-step τ-satisﬁable, i.e. [[φ]]BX,τ ̸= ∅. We can assume that
X is ﬁnite (see [Schr¨oder 2007, Proposition 23], [Myers et al. 2009, Lemma 30]), and
then that τ : V →PX is surjective. By a standard argument, there exists a maximal
inﬁnite conjunctive clause Φ over Λ(V ) that is one-step τ-consistent and contains φ.
(Note that the argument is entirely generic up to this point.) We construct a multiset
µ ∈BX that satisﬁes Φ over τ, i.e. µ ∈T
φ∈Φ [[φ]]T X,τ. Speciﬁcally, we deﬁne µ as an
N ∪{∞}-valued measure on X by
µ(A) = max{k + 1 | 3ka ∈Φ, τ(a) = A}
for A ⊆X, where by convention max ∅= 0. To see that µ is really a measure, note ﬁrst
that µ(∅) = 0 by (RN), applied to a such that τ(a) = ∅. Moreover, µ is additive, i.e.
14

µ(A ∪B) = µ(A) + µ(B) for disjoint A, B. (Since X is ﬁnite, there is no issue about µ
being σ-additive.) Here, ≥is by (A2) and (RN), applied to a, b, c, d such that τ(a) = A,
τ(b) = B, τ(d) = ∅, and τ(c) = A ∪B. Similarly, ≤is by (A1) applied to a, b, c such
that τ(a) = A, τ(b) = B, and τ(c) = A ∪B. It remains to show that µ ∈[[3ka]]T X,τ iff
3ka ∈Φ. ‘If’ is immediate by the deﬁnition of µ, and ‘only if’ is by (RG1).
4. CONSTRUCTIVE FIXPOINTS
Our next aim is to prove that the Lindenbaum algebra of L♯is constructive, i.e. its ﬁx-
points can be iteratively approximated in ω steps. In terms of consistency of formulas,
this means that whenever a formula of the form ♯γφ ∧ψ is consistent, then already
γi(φ)(⊥) ∧ψ is consistent for some i < ω; this fact will play a pivotal role in the model
construction in Section 5. We begin by introducing the requisite algebraic tools.
Deﬁnition 4.1. A Λ-algebra is a Boolean algebra A with a monotone operation ♥A :
A →A for each ♥∈Λ. In a Λ-algebra A, a modal ﬁxpoint scheme φ with n variables
is interpreted as an operation φA : An →A in the evident way. A ♯-algebra is a Λ-
algebra A that is endowed with operations ♯A
γ for each γ ∈Γ such that for each a ∈A,
♯A
γ (a) is the least ﬁxpoint of the map γA(a, −) : A →A (in particular, these ﬁxpoints
exist in a ♯-algebra). In a ♯-algebra A, every ﬁxpoint formula with variables φ(v1, . . . , vn)
is interpreted as a function φA : An →A. We say that A validates a rule R = φ/ψ if
ψA(a1, . . . , an) = ⊤whenever φA(a1, . . . , an) = ⊤, for a1, . . . , an ∈A. An L♯-algebra is a
♯-algebra A that validates every rule in our ﬁxed set R of one-step rules.
In the tradition of algebraic logic, L♯-algebras provide an algebraic encoding of our
proof system. More speciﬁcally, we will be interested in the Lindenbaum algebra A(L♯)
of our logic. As usual, this algebra is deﬁned as the quotient of the set F♯of formulas
under the congruence relation ≡of provable equivalence (φ ≡ψ iff ⊢φ ↔ψ), equipped
with the algebra structure that just interprets every connective as itself. (The con-
gruence property of ≡follows from the fact that both the modalities and the ﬁxpoint
operators come with congruence rules, the former by one-step completeness and the
latter by Lemma 3.1.) An easy induction shows that every formula φ is interpreted as
the element φA(L♯) = [φ] of this algebra, where [φ] denotes the equivalence class of φ
under ≡; when there is no danger of confusion, we may write φ in place of [φ]. The
Kozen-Park axiomatization ensures that A(L♯) actually is an L♯-algebra:
LEMMA 4.2. The Lindenbaum algebra is the initial L♯-algebra.
PROOF. The unfolding axiom makes ♯γφ a preﬁxpoint of φA(L♯)(φ, −) in A(L♯), and
the ﬁxpoint induction rule ensures that ♯γφ is the least such, because every element of
A(L♯) is the denotation of a formula. This shows that A(L♯) is an L♯-algebra. Initiality
is then straightforward: if B is an L♯-algebra, then B validates the unfolding axiom and
the ﬁxpoint induction rule so that we have a well-deﬁned map f : A(L♯) →B given
by f([φ]) = φB. By construction of the algebra structure on A(L♯), f is homomorphic
w.r.t. all algebraic operators (Boolean, modal, and ﬁxpoint). Uniqueness of f is shown
by induction over formulas in the usual way, using the fact that the ﬁxpoint operators
are explicitly included in the algebra structure.
In these terms, our target property is phrased as follows.
Deﬁnition 4.3. We say that γ ∈Γ is constructive if
♯γφ =
_
i<ω
γ(φ)i(⊥)
15

in the Lindenbaum algebra A(L♯), i.e. if ⊢♯γφ →ψ whenever ⊢γ(φ)i(⊥) →ψ for all
i < ω. If all γ ∈Γ are constructive, then A(L♯) is constructive.
We explicitly state the dual formulation of this property:
LEMMA 4.4.
Let γ be constructive. If ♯γφ ∧ψ is consistent, then γ(φ)i(⊥) ∧ψ is
consistent for some i < ω.
The central tool for proving constructivity, introduced by Santocanale [2008] and fea-
turing prominently in subsequent work by Santocanale and Venema [2010], is the
notion of a ﬁnitary O-adjoint:
Deﬁnition 4.5. We say that a formula φ with a variable x is an O-adjoint if for all
ψ ∈F♯, there exists a ﬁnite set Gφ(ψ) of formulas such that for all ρ ∈F♯,
⊢φ(ρ) →ψ iff ⊢ρ →χ for some χ ∈Gφ(ψ),
i.e. φ(ρ) ≤ψ in A(L♯) iff ρ ≤χ for some χ ∈Gφ(ψ). Moreover, φ is a ﬁnitary O-adjoint if
Gφ can be chosen such that for every ψ, the closure of ψ under Gφ, i.e. the smallest set
A with ψ ∈A and χ ∈A ⇒Gφ(χ) ⊆A, is ﬁnite. We say that a modal ﬁxpoint scheme
γ ∈Γ is a (ﬁnitary) O-adjoint if γ(φ) is a (ﬁnitary) O-adjoint for all φ ∈F♯.
LEMMA 4.6.
[Santocanale 2008] Every ﬁnitary O-adjoint is constructive.
Deﬁnition 4.7.
Let φ be a formula (possibly with variables) such that φ contains
some modal operator and every occurrence of a variable in φ is in the scope of some
modal operator. Let φ′ be the equivalent formula that arises by unfolding all top-level
occurrences of ♯once (where an occurrence is top-level if it is not in the scope of a modal
or ﬁxpoint operator). By guardedness of ﬁxpoint operators, φ′ is of the form φ0σ, where
φ0 ∈Prop(Λ(V )) is injective and σ is a substitution. We refer to the equivalence φ ≡φ0σ
(or more precisely the pair (φ0, σ)) as the top-level decomposition of φ.
The ﬁrst step in the proof of O-adjointness for a large class of operators is a general-
ization of the rigidity lemma of [Santocanale 2008]:
LEMMA 4.8 (RIGIDITY). Let ψ be a clause over Λ(F♯), with top-level decomposition
ψ ≡ψ0σ0. Then ψ is provable iff there exists an R-derivable monotone one-step rule φ/χ
such that φσ0 is provable and χ ⊢PL ψ0.
The proof relies on the one-point extension of an algebra (so called because it mim-
ics the addition of a new root point in a coalgebraic model on the algebraic side), in
generalization of a similar construction by Santocanale and Venema [2010]:
Let A be a countable L♯-algebra, let S(A) be the set of ultraﬁlters of A, ﬁx a surjective
map σ : V →A, and let an injective conjunctive clause ρ over Λ(V ) be one-step θ-
consistent for θ : V →P(S(A)) given by θ = j ◦σ (applicative composition) where
j : A →P(S(A)) is the usual canonical map j(a) = {u ∈S(A) | a ∈u}. We construct
the one-point extension Aρ, an L♯-algebra emulating the addition of a new point whose
successor structure is described by ρ, as follows. To begin, we can ﬁnd a maximally
one-step cutfree θ-consistent set Φ ⊆Λ(V ) such that Φ ⊢PL ρ. As we emulate adding a
single point, the carrier of Aρ is A × 2 where 2 is the Boolean algebra {⊥, ⊤}; we thus
have projection maps π1 : Aρ →A and π2 : Aρ →2. We make Aρ into a Λ-algebra by
putting
♥Aρ(a, d) = (♥A(a), ♥ρ(a)),
where ♥ρ : A →2 is deﬁned by ♥ρ(a) = ⊤iff ♥a ∈Φσ. (Thus, ♥Aρ(a, d) is independent
of d, in agreement with the semantic fact that the interpretation of modal operators
16

depends only on the successor structure of the current state, not on the current state
itself.) We deﬁne a valuation ˆσ : V →Aρ by
ˆσ(v) =
(σ(v), ⊥)
if v occurs in a positive literal in ρ
(σ(v), ⊤)
otherwise.
We then have
ρˆσ > ⊥in Aρ :
(3)
by deﬁnition of Aρ, the second component of ρˆσ is ⊤because Φσ ⊢PL ρσ.
LEMMA 4.9.
The algebra Aρ is an L♯-algebra.
PROOF. The proof that Aρ is a ♯-algebra is a simple application of Bekic’s theorem,
as in [Santocanale and Venema 2010]: since Aρ has carrier A×2, the ﬁxpoint deﬁnition
of (ψ, c) = ♯γ(φ, b) in Aρ can be seen as a mutually recursive deﬁnition of two variables
ψ, c. By guardedness of ﬁxpoints and the interpretation of modalities in Aρ, the deﬁni-
tion of the ﬁrst variable ψ does not mention the second variable c, so we can calculate
the solution for ψ separately, using the fact that A is a ♯-algebra, and then replace ψ
with its solution ♯γ(ψ) in the recursive deﬁnition of the second variable c. We end up
with c being deﬁned as the least ﬁxpoint of a monotone function on 2, which exists
because 2 is a complete lattice.
It remains to prove that Aρ validates the one-step rules in R. The ﬁrst component of
Aρ behaves just like A, so that we have to verify the rules only on the second compo-
nent, 2. That is, whenever we have a one-step rule χ/ψ ∈R and a valuation τ : V →Aρ
such that χτ = ⊤in Aρ, we have to prove that π2ψτ = ⊤. Since ψτ depends only on π1τ,
we thus have to prove that whenever we have τ ′ : V →A such that χτ ′ = ⊤in A, the
interpretation of ψτ ′ in 2 is ⊤, where the interpretation of ψτ ′ is determined by means
of the ♥ρ and the Boolean algebra structure of 2. Since σ is surjective, we have τ ′ = σ◦ˆτ
(applicative composition) for some renaming ˆτ : V →V . It now follows that Φ ⊢PL ψˆτ:
Otherwise, by maximality of of Φ, we would have Φ ⊢PL ¬ψˆτ. Since Φ is one-step θ-
consistent, this implies that [[¬χˆτ]]S(A),θ ̸= ∅. But since θ◦ˆτ = j◦σ◦ˆτ = j◦τ ′, [[¬χˆτ]]S(A),θ
is the image of ¬χτ ′ ∈A under the Boolean homomorphism j : A →P(S(A)). This im-
plies ¬χτ ′ ̸= ⊥, in contradiction to χτ ′ = ⊤.
In consequence of the fact that A(L♯) is the initial L♯-algebra, we thus have
LEMMA 4.10.
Let σ : V →A(L♯) be surjective, and let ρ be a conjunctive clause
over Λ(V ). If ρ is one-step θ-consistent for θ(v) = {u ∈S(A(L♯)) | σ(v) ∈u}, then ρσ is
consistent, i.e. ρσ > ⊥in A(L♯).
PROOF. Let f be the unique L♯-algebra homomorphism A(L♯) →A(L♯)ρ, and take
ˆσ : V →A(L♯)ρ as above. Note that the ﬁrst projection π1 : A(L♯)ρ →A(L♯) is a
homomorphism of L♯-algebras by construction of A(L♯)ρ. By the uniqueness part of
initiality, it follows that π1 ◦f = id. For a ∈V , we therefore have π1(f(σ(a)) = σ(a) =
π1(ˆσ(a)); moreover, if a occurs in a positive literal in ρ, then π2(f(σ(a))) ≥⊥= π2(ˆσ(a)),
and if a occurs in a negative literal in ρ (hence does not occur in a positive literal,
since ρ is injective), then π2(f(σ(a))) ≤⊤= π2(ˆσ(a)). Since the order on A(L♯)ρ is
componentwise, this implies that f ◦σ(a) ≥ˆσ(a) if a occurs in a positive literal in ρ,
and f ◦σ(a) ≤ˆσ(a) if a occurs in a negative literal in ρ. Since f is homomorphic and
the modalities are monotone, it follows by (3) that f(ρσ) = ρ(f ◦σ) ≥ρˆσ > ⊥. Since f
preserves ⊥, this implies ρσ > ⊥.
From Lemma 4.10, one easily proves rigidity (Lemma 4.8) using the fact that every
consistent formula is contained in some ultraﬁlter of A(L♯):
17

PROOF (LEMMA 4.8). ‘If’ is clear. For ‘only if’, we prove the dual statement: Let ψ be
a conjunctive clause over Λ(F♯), with top-level decomposition ψ ≡ψ0σ0, such that ¬φσ0
is consistent for all R-derivable monotone one-step rules φ/χ and all substitutions σ
such that χ ⊢PL ¬ψ0; we show that ψ is consistent. W.l.o.g. σ0 is a surjection V →
F♯, which we prolong to a surjection ¯σ0 : V →A(L♯). By Lemma 4.10, it sufﬁces to
prove that ψ0 (a conjunctive clause over Λ(V )) is one-step θ-consistent for θ(v) = {u ∈
S(A(L♯)) | ¯σ0(v) ∈u}. Thus, let φ/χ be an R-derivable rule such that χ ⊢PL ¬ψ0. We
have to show that S(A(L♯)), θ ̸|= φ. By assumption and because χ ⊢PL ¬ψ0, we have
that ¬φσ0 is consistent, hence ¬φ¯σ0 > ⊥in A(L♯), so that there exists u ∈S(A(L♯))
with ¬φ¯σ0 ∈u. Now one shows by induction over ρ ∈Prop(V ) that u ∈[[ρ]]S(A(L♯)),θ
iff ρ¯σ0 ∈u: the cases for Boolean connectives are by the ultraﬁlter property of u, and
the base case ρ = a ∈V is by deﬁnition of θ. In particular, we have u ∈[[¬φ]]S(A(L♯)),θ,
showing that S(A(L♯)), θ ̸|= φ as required.
In a nutshell, rigidity enables us to prove O-adjointness of all (monotone) modal op-
erators, and even more generally all modal ﬁxpoint schemes in which the recursion
variable x occurs at uniform depth (such as 23x ∧32x). Formally:
Deﬁnition 4.11.
A formula φ with variables is uniform of depth k if every occur-
rence of the ﬁxed recursion variable x in φ is in the scope of exactly k modal operators
(including the case that x does not occur in φ; recall moreover that variables never
occur under ﬁxpoint operators). Moreover, φ is uniform if φ is uniform of depth k for
some k; the minimal such k is the depth of uniformity of φ. Formally, uniform formulas
are deﬁned inductively by following inductive clauses.
— x is uniform of depth 0;
— formulas not containing x are uniform of depth k, for any k;
— any Boolean combination of uniform formulas of depth k is uniform of depth k; and
— if ψ is uniform of depth k, then ♥ψ is uniform of depth k + 1, for ♥∈Λ.
Finitariness of O-adjoints will use the standard Fischer-Ladner closure:
Deﬁnition 4.12. A set Σ of formulas is Fischer-Ladner closed if Σ is closed under
subformulas and negation (where the negation of a negated formula ¬φ is taken to
be φ), and whenever ♯γφ ∈Σ, then γ(φ, ♯γφ) ∈Σ. We denote the Fischer-Ladner closure
of a formula φ, i.e. the smallest Fischer-Ladner closed set containing φ, by FL(φ).
LEMMA 4.13.
[Kozen 1983] The set FL(φ) is ﬁnite.
The further development revolves largely around derivable rules:
Deﬁnition 4.14. A rule R = φ/ψ consists of a premise φ and a conclusion ψ, both
being formulas with variables. The rule R is derivable if ψ can be derived from the as-
sumption φ using the rules of the system (propositional reasoning, unfolding, ﬁxpoint
induction, modal rules).
LEMMA 4.15.
If φ is positive in x and ⊢χ →ρ, then ⊢φ(χ) →φ(ρ).
PROOF. Induction over φ, simultaneously with a corresponding statement on for-
mulas that are negative in x. The Boolean cases are straightforward, and the cases for
ﬁxpoint operators are trivial because x never appears under ﬁxpoint operators. The
case for a modal operator ♥∈Λ is discharged by the fact that by monotonicity of ♥,
the monotonicity rule a →b/♥a →♥b is one-step sound and therefore, by Lemma 3.6,
R-derivable.
18

LEMMA 4.16.
Let ψ = ψ(x) be a uniform formula, and put
G = {φ ∈Prop(FL(ψ)) | φ/ψ derivable, φ uniform of depth 0}.
Then given a formula ρ, ψ(ρ) is provable iff φ(ρ) is provable for some φ(x) ∈G.
PROOF. ‘If’ holds by deﬁnition of admissibility. We prove ‘only if’ by induction over
the depth of uniformity, with trivial base case. Thus, let ψ be uniform of depth k > 0,
and let ψ(ρ) be provable. By unfolding (guarded) top-level ﬁxpoints (i.e. those not in the
scope of a modality) and then applying propositional reasoning to transform into CNF,
we reduce to the case that ψ is a clause over Λ(FL(ψ)); note here that these transfor-
mations remain within Prop(FL(ψ)). Let ψ ≡ψ0σ0 be the top-level decomposition of ψ;
then the top-level decomposition of ψ(ρ) has the form ψ0σρ
0 where σρ
0(a) = σ0(a)(ρ). By
rigidity (Lemma 4.8), we have an R-derivable monotone one-step rule χ/ψ′ such that
χσρ
0 is provable and ψ′ ⊢PL ψ0. Then the rule χσ0/ψ is derivable, χσ0 ∈Prop(FL(ψ))
is uniform of depth k −1, and (χσ0)(ρ) is provable (being equal to χσρ
0). By the induc-
tive assumption, applied to χσ0, there exists φ(x) ∈Prop(FL(χσ0)) ⊆Prop(FL(ψ)) such
that φ/χσ0 is derivable, φ is uniform of degree 0, and φ(ρ) is provable. Since φ/χσ0 and
χσ0/ψ are derivable, so is φ/ψ, hence φ ∈G, which proves the claim.
We recall a trick from propositional logic:
LEMMA 4.17.
Let φ be a formula with variables containing only top-level occur-
rences (Deﬁnition 4.7) of the variable y. Then there is a formula a such that
φ[⊤/y] ∨φ[⊥/y] ⊢PL φ[a/y].
PROOF. By standard Boolean expansion. Speciﬁcally, a = φ[⊤/y] does the job. This
is seen by case distinction over φ[⊤/y]: First, assume φ[⊤/y]; then φ[⊤/y] is equivalent
to ⊤, so φ[⊤/y] propositionally entails φ[φ[⊤/y]/y] because y has only top-level occur-
rences in φ. Second, assume ¬φ[⊤/y]. From φ[⊤/y] ∨φ[⊥/y] we then conclude φ[⊥/y].
Since by assumption, φ[⊤/y] is equivalent to ⊥, this, again, propositionally entails
φ[φ[⊤/y]/y].
We are now set to prove the main result of this section:
THEOREM 4.18 (FINITARY O-ADJOINTNESS). If the formula ψ with recursion vari-
able x is positive and uniform in x, then the operation ψA(L♯) : A(L♯) →A(L♯) induced
by ψ is a ﬁnitary O-adjoint.
PROOF. For readability, we phrase the arguments using formulas in F♯rather than
elements of A(L♯). Let φ ∈F♯. We have to construct a set Gψ(φ) of formulas such that
for all ρ ∈F♯, ⊢ψ(ρ) →φ iff ⊢ρ →χ for some χ ∈Gψ(φ); moreover we require Gψ(φ)
to be ﬁnite up to provable equivalence.
The formula ψ′ :≡ψ →φ is uniform (as φ does not contain x). Let G ⊆Prop(FL(ψ′))
be as in Lemma 4.16, applied to ψ′; notice that Prop(FL(ψ′)) is ﬁnite up to provable
equivalence. Then put
Gψ(φ) = {χ(⊤) | χ(x) ∈G, ⊢χ(⊤) ∨χ(⊥)}.
Now let ⊢ρ →χ(⊤) for some χ(x) ∈G such that ⊢χ(⊤)∨χ(⊥). To show that ⊢ψ(ρ) →φ,
it sufﬁces by construction of G and positivity of ψ to prove that ⊢χ(a) ∧(ρ →a) for
some formula a: then it follows that ⊢(ψ(a) →φ) ∧(ρ →a), and hence by Lemma 4.15
that ⊢ψ(ρ) →φ. Since χ ∧(ρ →x) is uniform of depth 0, existence of such an a follows
by Lemma 4.17 once we show that
⊢(χ(⊤) ∧(ρ →⊤)) ∨(χ(⊥) ∧(ρ →⊥)),
19

equivalently
⊢χ(⊤) ∨(¬ρ ∧χ(⊥)).
By distributing disjunction over conjunction, this last formula is equivalent to
(ρ →χ(⊤)) ∧(χ(⊥) ∨χ(⊤))
and hence provable by assumption.
Conversely, let ⊢ψ(ρ) →φ. By Lemma 4.16, there exists χ ∈G such that χ(ρ)
is provable. Since χ is uniform of depth 0, χ(ρ) is propositionally equivalent to its
Boolean expansion (ρ →χ(⊤)) ∧(¬ρ →χ(⊥)) and hence propositionally entails χ(⊥) ∨
χ(⊤), which is therefore provable. That is, we have χ(⊤) ∈Gψ(φ) and ⊢ρ →χ(⊤), as
required.
This proves that ψA(L♯) is O-adjoint. From the above description of Gψ, one sees im-
mediately that ψA(L♯) is in fact a ﬁnitary O-adjoint, as Prop(FL(ψ′)) is closed under Gψ
and ﬁnite up to provable equivalence.
Using uniform formulas as a base, we can now exploit some known closure properties
of ﬁnitary O-adjoints [Santocanale 2008].
Deﬁnition 4.19. The set of admissible modal ﬁxpoint schemes is the closure of the
set of uniform modal ﬁxpoint schemes under disjunction, conjunction with modal ﬁx-
point schemes not containing the recursion variable x, and substitution for x, the latter
in the sense that if γ(x) and δ are admissible, then γ(δ) is admissible.
COROLLARY 4.20.
If γ ∈Γ is admissible, then γ is a ﬁnitary O-adjoint, and hence
constructive.
PROOF. The set of ﬁnitary O-adjoints is closed under joins, meets with constants,
and composition [Santocanale 2008].
From now on, we require that every γ ∈Γ is admissible, and hence A(L♯) is construc-
tive; a ﬂat coalgebraic ﬁxpoint logic satisfying this requirement will be called admissi-
ble. All ﬁxpoint operators mentioned in Example 2.3 are based on admissible ﬁxpoint
schemes (in fact, on uniform ones).
Remark 4.21.
The sufﬁcient criterion for O-adjointness given by Santocanale and
Venema [2010] is that modal ﬁxpoint schemes be harmless, which in the single-
modality case means that modal ﬁxpoint schemes γ, δ are generated by the grammar
γ, δ ::= ⊤| x | γ ∨δ | χ ∧γ | Vk
i=1 3φi | 2φ
where χ is a modal formula not mentioning the recursion variable x (but possibly
mentioning argument variables). This notion is incomparable to admissibility in the
sense of Deﬁnition 4.19; e.g. 3x ∧33x is harmless but not admissible, and 32x ∧
23x is admissible (in fact, uniform) but not harmless. We leave it as an open problem
to ﬁnd a sufﬁcient criterion for O-adjointness that subsumes both admissibility and
harmlessness. The ﬁxpoint schemes generating the CTL operators are both harmless
and admissible.
5. THE MODEL CONSTRUCTION
We proceed to describe a model construction that uses sets of timed-out formulas as
states; a timed-out formula has some of the ﬁxpoints that appear in it annotated with
time-outs indicating how often they need to be unfolded. Our time-outs are related to
Kozen’s µ-counters [Kozen 1983] but, as indicated, are integrated into formulas rather
than maintained independently in a tableau construction. The use of time-outs is jus-
tiﬁed by constructivity of ﬁxpoint operators as proved in the previous section.
20

Since only some of the ﬁxpoint subformulas contained in a state will be annotated
with ﬁnite time-outs, only one implication of the truth lemma will hold (every state
satisﬁes the timed-out formulas it contains but not conversely – the model we construct
will be ﬁnite, so every ﬁxpoint will be satisﬁed with some time-out, which the state may
fail to specify). Consequently, in the inductive proof of the truth lemma, the step for
negation would fail. We therefore work with formulas in negation normal form, deﬁned
in detail as follows. A modal ﬁxpoint scheme is in negation normal form (NNF) if it can
be generated by the grammar
γ, δ ::= ⊥| ⊤| v | γ ∧δ | γ ∨δ | ♥γ | ♥γ,
(recall that ♥abbreviates ¬♥¬). We can clearly transform every modal ﬁxpoint scheme
into a provably equivalent one in NNF (recall that modal ﬁxpoint schemes are positive
in all variables), and therefore assume from now on that Γ consists of modal ﬁxpoint
schemes in NNF (this does not substantially affect the syntax as the modal ﬁxpoint
schemes just serve as indices of ﬁxpoint operators). A formula φ is in NNF if it can be
generated by the grammar
φ, ψ ::= ⊥| ⊤| φ ∧ψ | φ ∨ψ | ♥φ | ♥φ | ♯γφ | ♭γφ.
The dual γ of a modal ﬁxpoint scheme γ as deﬁned in Section 2 is clearly equivalent
to the modal ﬁxpoint scheme obtained from γ by swapping ∧with ∨, ⊤with ⊥, and ♥
with ♥, and we regard γ as being syntactically deﬁned in this way from now on; e.g.
if 3 = 2 then the dual of γ = p ∨2x is γ = p ∧3x. To show that we can transform
every formula into NNF, it sufﬁces as usual to show that we can implement negation
on NNFs; this is by the standard procedure of pushing negation inside through the
other connectives using provable equivalences. In particular, the equivalence
¬♯γφ ↔♭γ¬φ
is trivially provable, as the right hand side is just an abbreviation for ¬♯γ¬¬φ. Sum-
ming up, we have
LEMMA 5.1.
Every formula is provably equivalent to a formula in NNF.
It remains to adapt the notion of Fischer-Ladner closure; as no confusion is likely and
the changes are rather inessential, we use the same term:
Deﬁnition 5.2. A set Σ of formulas in NNF is Fischer-Ladner closed if Σ is closed
under subformulas and ﬁxpoint unfolding, i.e. whenever ⋆γφ ∈Σ for ⋆∈{♯, ♭} then
γ(φ, ⋆γφ) ∈Σ.
The analogue of Lemma 4.13 remains true for the modiﬁed deﬁnition; so from now on
we ﬁx a ﬁnite Fischer-Ladner closed set Σ of formulas in NNF. We proceed to introduce
the announced notion of time-out:
Deﬁnition 5.3. The set of timed-out formulas φ, ψ is generated by the grammar
φ, ψ ::= ⊥| ⊤| φ ∧ψ | φ ∨ψ | ♥φ | ♥φ | ♯κ
γρ | ♭γρ
(κ ∈ω + 1, ρ ∈L♯)
where γ ∈Γ, ♥∈Λ, subject to the restriction that φ is a timed-out formula only in
case φ has at most one subformula of the form ♯κ
γχ with κ < ω (which however may
occur any number of times), and for this ♯κ
γχ,
— ♯ω
γ χ is not a subformula of φ; and
— whenever ♯ω
δ ρ is a subformula of φ, then ♯δρ is a subformula of χ.
In this case, we deﬁne the time-out τ(φ) of φ to be κ, and τ(φ) = ω otherwise (i.e. if
φ does not contain any subformula of the form ♯κ
γχ with κ < ω). The time-out gives
21

the number of steps left until satisfaction of the eventuality ♯γχ, with time-out ω sig-
nifying an unspeciﬁed number of steps (note that time-outs are never associated with
♭-formulas).
We deﬁne two translations s and t of timed-out formulas into L♯, given by commuta-
tion with Boolean and modal operators, (♭γρ)s = (♭γρ)t = ♭γρ, and
(♯ω
γ ρ)s = ♯γρ
(♯i
γρ)s = γ(ρ)i(⊥)
(i < ω)
(♯κ
γρ)t = ♯γρ.
Thus, s unfolds ﬁxpoints as prescribed by their time-outs, and t just removes time-
outs. Both translations extend to sets of formulas. For timed-out formulas φ, ψ, we
put φ ⪯ψ iff φt = ψt and τ(φ) ≤τ(ψ). That is, φ ⪯ψ iff φ is the same as ψ up to
possible decrease of the time-out. Given a set Σ of formulas, a timed-out formula φ is a
timed-out Σ-formula if φt ∈Σ.
Notice that unlike formulas in NNF, timed-out formulas are not closed under nega-
tion because time-outs can only appear on least ﬁxpoints. The point of the deﬁnition of
timed-out formulas is that every standard formula φ has at most one candidate sub-
formula at which one can insert a time-out, namely the greatest element under the
subformula ordering among the subformulas of φ which are ♯-formulas, if such a great-
est element exists and is not in scope of a ♭-operator. This enables the simple deﬁnition
of ⪯, which by the preceding discussion has the following property.
LEMMA 5.4.
For every formula φ, the preimage of φ under the translation t is well-
ordered by ⪯.
At the same time, timed-out formulas are stable under unfolding:
LEMMA 5.5.
If ♯κ
γφ is a timed-out formula, then so is γ(φ, ♯κ
γφ).
PROOF. As φ is a (standard) formula, it is clear that γ(φ, ♯κ
γφ) cannot contain formu-
las of the form ♯λ
δ ρ with λ < ω other than ♯κ
γφ. By well-foundedness of the subformula
relation, γ(φ, ♯κ
γφ) cannot contain ♯ω
γ φ. Finally, the only way ♯-subformulas can arise in
γ(φ, ♯κ
γφ) is as subformulas of ♯κ
γφ.
We have the expected relationship between the translations regarding provable entail-
ment:
LEMMA 5.6.
(1) For every timed-out formula φ, ⊢φs →φt.
(2) If φ ⪯ψ for timed-out formulas φ, ψ, then ⊢φs →ψs.
PROOF. By iterated application of the unfolding axiom and monotonicity of modal
ﬁxpoint schemes.
States of the model will be sets of formulas satisfying a timed-out version of the usual
expandedness requirement.
Deﬁnition 5.7.
A timed-out Σ-atom is a maximal set A of timed-out Σ-formulas
such that
— the translation t is injective on A, and
— As is consistent.
Here, maximality is w.r.t. ⊑where A ⊑B iff for all φ ∈A, there exists a (necessarily
unique) φ′ ∈B such that φ′ ⪯φ; intuitively: B contains A up to possible decrease of
time-outs. We write A↑for the upwards closure of A under ⪯(i.e. if φ ∈A↑and φ ⪯φ′
then φ′ ∈A↑).
22

To prove ﬁniteness of the model we construct, we use the fact that ﬁnite product
orderings (ω + 1)k are well-quasi-orders, and in particular have only ﬁnite anti-
chains [Laver 1976].
LEMMA 5.8 (TIMED-OUT LINDENBAUM LEMMA).
(1) The set of timed-out Σ-atoms is ﬁnite.
(2) For every set A0 of timed-out Σ-formulas such that As
0 is consistent, there exists a
timed-out Σ-atom A such that A0 ⊑A.
(3) For every consistent subset C ⊆Σ, there exists a timed-out Σ-atom A such that
C ⊆At.
PROOF. Claim (3) follows from (2) by converting C into a set of of timed-out formulas
with time-out ω. We prove (1) and (2) in one go. Note ﬁrst that we can assume w.l.o.g.
that t is injective on A0. We deﬁne a Σ-atom to be a maximally consistent subset of
Σ. The set of Σ-atoms is ﬁnite, so for every set A0 as in the statement there exists a
Σ-atom C such that At
0 ⊆C. Let A be the set of sets A of timed-out Σ-formulas such
that As is consistent, t is injective on A, and At = C; the timed-out Σ-atoms whose
t-image is C are the ⊑-maximal elements of A. Now every element A ∈A is uniquely
determined by the C-tuple ι(A) of time-outs it induces (explicitly, for φ ∈C, the φ-
component of ι(A) is τ(φ′) where φ′ is the unique formula in A such that (φ′)t = φ),
and for A, B ∈A, A ⊑B iff ι(B) ≤ι(A) in the componentwise ordering. Thus, A is
order-isomorphic to a subset of the ﬁnite power (ω + 1)C of the well-ordering ω + 1. It
follows from the theory of well-quasi-orders that (ω + 1)C is a well-quasi-order, which
means that for every subset F of (ω + 1)C, every element of F is above one of ﬁnitely
many minimal elements of F [Laver 1976]; applying this to A proves (1) and (2) (for (1),
recall additionally that the set of Σ-atoms is ﬁnite).
As usual, the proof of the truth lemma will depend on a set of Hintikka-like properties:
LEMMA 5.9.
If A is a timed-out Σ-atom, then
(1) if φ ∧ψ ∈A then φ ∈A↑and ψ ∈A↑;
(2) if φ ∨ψ ∈A then φ ∈A↑or ψ ∈A↑;
(3) ⊥/∈A↑;
(4) if ♯κ
γφ ∈A, then κ < ω;
(5) ♯κ
γφ ∈A iff γ(φ, ♯κ−1
γ
φ) ∈A;
(6) ♭γφ ∈A iff γ(φ, ♭γφ) ∈A.
PROOF. 1: If φ ∧ψ ∈A, then (A′)s is consistent for A′ := A ∪{φ}. By maximality of
A, it follows that t is not injective on A′, i.e. there is φ′ ∈A such that (φ′)t = φt. Again
by maximality, φ  φ′, so φ′ ⪯φ by Lemma 5.4, and hence φ ∈A↑.
2: If φ∨ψ ∈A, then either (A∪{φ})s or (A∪{ψ})s is consistent; in both cases, proceed
as for 1.
3: Clear.
4: If ♯κ
γφ ∈A, then by Lemma 4.4 and by ﬁniteness of A, there is i < ω such that
(A ∪{♯i
γφ})s is consistent. By maximality of A, κ ≤i.
5: Both formulas have ♯γφ as the greatest ♯-subformula of their t-translation, and
their s-translations are syntactically equal. Therefore if, e.g., ♯κ
γφ ∈A, then (A′)s,
where A′ = A ∪{γ(φ, ♯κ−1
γ
φ)}, is consistent; moreover (A′)t ⊆Σ. By maximality
of A, it follows that the translation t does not remain injective on A′, i.e. we have
χt = (γ(φ, ♯κ−1
γ
φ))t for some χ ∈A. Again by maximality of A and Lemma 5.4, we must
have χ ⪯γ(φ, ♯κ−1
γ
φ), so that γ(φ, ♯κ−1
γ
φ) ∈A↑. Similarly, we show that γ(φ, ♯κ−1
γ
φ) ∈A
implies ♯κ
γφ ∈A↑. Together, these implications yield the same implications for A in
23

place of A↑: e.g. if ♯i
γφ ∈A for i < ω (where necessarily i > 0), then γ(φ, ♯i−1
γ
φ) ∈A↑, so
γ(φ, ♯j
γφ) ∈A for some j ≤i −1. Thus ♯j+1
γ
φ ∈A↑, so that i ≤j + 1, implying j = i −1.
6: Similar to (and easier than) 5.
We denote by TΣ the (by Lemma 5.8, ﬁnite) set of timed-out Σ-atoms, and proceed
to construct a model with carrier TΣ. We need to construct a coalgebra rather than
just a relational structure; this coalgebra should adequately implement the formulas
contained in the states. This requirement is encapsulated in the notion of coherence:
Deﬁnition 5.10.
A coalgebra structure ξ on TΣ is coherent if for every A ∈TΣ and
every timed-out Σ-formula ♠ρ where ♠is either a modal operator ♥∈Λ or a dual
modal operator ♥,
ξ(A) ∈[[♠]]TΣ(ˆρ) whenever ♠ρ ∈A,
where ˆρ = {B ∈TΣ | ρ ∈B}.
Existence of such a coherent coalgebra structure relies on one-step completeness.
LEMMA 5.11 (EXISTENCE LEMMA).
There exists a coherent coalgebra structure
on TΣ.
PROOF. The proof follows the same pattern as the one for the ﬁxpoint-free
case [Schr¨oder 2007]. We can construct the coalgebra structure ξ pointwise. So let
A ∈TΣ; in the notation of Deﬁnition 5.10, we have to show that there exists t ∈T TΣ
such that t ∈[[♠]]TΣ(ˆφ) whenever ♠φ ∈A. With a view to deriving a contradiction,
assume the contrary. Then T TΣ, τ |= ψ where
ψ = W
♥ρ∈A ¬♥a♥ρ ∨W
♥ρ∈A ♥b♥ρ
τ(a♥ρ) = ˆρ
τ(b♥ρ) = TΣ −ˆρ
for pairwise distinct propositional variables a♥ρ, b♥ρ. By one-step completeness, it fol-
lows that ψ is provable over TΣ, τ, so by Lemma 3.7 there is an R-derivable monotone
one-step rule φ/χ ∈R such that χ ⊢PL ψ and TΣ, τ |= φ. Now let θ be the substitu-
tion deﬁned by θ(a♥ρ) = ρs and θ(b♥ρ) = ¬(ρs). Then As propositionally entails ¬ψθ.
Since As is consistent, we are done once we show that ψθ is provable. To this end, it
sufﬁces to show that φθ is provable. Assume the contrary, i.e. ¬φθ is consistent. Then
there is a conjunctive clause φ0 in the disjunctive normal form of ¬φ such that φ0θ is
consistent. Since φ/χ is monotone and χ ⊢PL ψ just means that χ is contained in ψ, φ is
negative in the a♥ρ and positive in the b♥ρ; thus, φ0 is positive in the a♥ρ and negative
in the b♥ρ. Therefore, φ0θ is, after removing double negations, of the form V As for a
set A of timed-out Σ-formulas. By the timed-out Lindenbaum lemma (Lemma 5.8), A is
contained in a timed-out Σ-atom B ∈TΣ. Then B ∈[[φ0]]τ, so B /∈[[φ]]τ, in contradiction
to TΣ, τ |= φ.
Next, we establish that a coherent coalgebra does what we expect:
LEMMA 5.12 (TRUTH LEMMA).
Let ξ be a coherent coalgebra structure on TΣ. If
A ∈TΣ and φ ∈A, then A |=(TΣ,ξ) φs.
PROOF. Induction over timed-out Σ-formulas φ using the lexicographic product
of the subterm ordering on φt with ⪯as the induction measure. We note that by
Lemma 5.6 (and soundness), the inductive hypothesis can be strengthened to apply
also to φ ∈A↑. The case φ = ⊤is trivial. The steps for ⊥, ∧, and ∨are taken care of by
Lemma 5.9. The step for modal operators ♥or ♥is by coherence and monotonicity.
24

Next, we discharge the case φ = ♯κ
γψ. By Lemma 5.9, we have κ < ω and γ(ψ, ♯κ−1
γ
ψ) ∈
A. We prove by a further induction on modal ﬁxpoint schemes δ that δ(ψ, ♯κ−1
γ
ψ) ∈A↑
implies that A |=(TΣ,ξ) (δ(ψ, ♯κ−1
γ
ψ))s. The case for the parameter variable is discharged
by the inductive hypothesis applied to ψ, as ψt is a proper subterm of φt, while the case
for the recursion variable is discharged by the inductive hypothesis applied to ♯κ−1
γ
ψ
(which is strictly below ♯κ
γψ w.r.t. ⪯since κ < ω). The cases for Boolean operations and
modal operators are as in the outer induction. This ﬁnishes the inner induction, so
that A |=(TΣ,ξ) γ(ψ, ♯κ−1
γ
ψ) and hence A |=(TΣ,ξ) ♯κ
γψ.
Finally, the case φ = ♭γψ is discharged by coinduction. For timed-out Σ-formulas ρ,
we put TΣ(ρ) = {A ∈TΣ | ρ ∈A} (this is like ˆρ in Deﬁnition 5.10 but will be applied to
long expressions). As [[♭γψ]](TΣ,ξ) is a greatest ﬁxpoint, it sufﬁces to prove that TΣ(♭γψ)
is semantically a postﬁxpoint of γ(ψ), i.e.
TΣ(♭γψ) ⊆[[γ(ψ)]](TΣ(♭γψ)).
(4)
To begin, we prove by induction on modal ﬁxpoint schemes δ that for all timed-out
Σ-formulas χ,
TΣ(δ(ψ, χ)) ⊆[[δ(ψ)]](TΣ(χ)).
(5)
The cases for ⊥, ⊤, and the recursion variable x are clear. The case for the parameter
variable p is discharged by the outer inductive hypothesis applied to ψ. The cases for
∧and ∨are by Lemma 5.9; e.g., we have
TΣ((δ1 ∨δ2)(ψ, χ))
⊆TΣ(δ1(ψ, χ)) ∪TΣ(δ2(ψ, χ))
(Lemma 5.9)
⊆[[δ2(ψ)]](TΣ(χ)) ∪[[δ1(ψ)]](TΣ(χ))
(induction)
= [[(δ2 ∨δ1)(ψ)]](TΣ(χ)).
Finally, the case for modal operators is by coherence: for ♠being either a modal oper-
ator ♥∈Λ or a dual modal operator ♥, we have
TΣ(♠δ(ψ, χ))
⊆ξ−1[[[♠]]TΣ(TΣ(δ(ψ, χ)))]
(coherence)
⊆ξ−1[[[♠]]TΣ([[δ(ψ)]]TΣ(χ))]
(induction, monotonicity)
= [[♠δ(ψ)]](TΣ(χ)).
By (5), we reduce our goal (4) to
TΣ(♭γφ) ⊆TΣ(γ(ψ, ♭γψ)),
which holds by Lemma 5.9.
In summary, we have proved completeness of the Kozen-Park axiomatization:
THEOREM 5.13 (COMPLETENESS).
If Γ is admissible and R is one-step complete,
then the logic L♯is complete over ﬁnite models.
PROOF. We have to show that every consistent formula φ is satisﬁable in a ﬁnite T -
coalgebra, where by Lemma 5.1 we can assume that φ is in NNF. This follows from the
preceding lemmas by the usual pattern: Let Σ be the least Fischer-Ladner closed set
of formulas in NNF containing φ. Then φ is contained in the t-image of some timed-out
Σ-atom by the timed-out Lindenbaum lemma (Lemma 5.8), hence by the truth lemma
(Lemma 5.12) satisﬁed in a coherent coalgebra on the set TΣ of timed-out Σ-atoms,
which exists by Lemma 5.11; the set TΣ is ﬁnite by Lemma 5.8.
25

We enumerate a few concrete instances of this result:
Example 5.14. We obtain that the Kozen-Park axiomatization (in combination with
the modal rules and propositional reasoning) is complete for the following logics.
(1) All admissible ﬂat fragments of the standard relational µ-calculus, interpreted
over unrestricted, serial, or deterministic Kripke models. We thus recover the
known completeness results for LTL [Gabbay et al. 1980] and serial and non-serial
CTL [Emerson and Halpern 1985], but also for logics featuring operators outside
even CTL∗as discussed in Example 2.3.1. Non-serial CTL is already covered by
the generic results of Santocanale and Venema [2010].
(2) All admissible ﬂat fragments of the graded µ-calculus, including ones featuring the
operators ‘the current state is the root of a ﬁnite binary tree all whose leaves satisfy
. . . ’, ‘. . . holds somewhere on every inﬁnite k + 1-ary tree starting at the current
state’, and ‘the current state is the root of a ﬁnite binary tree all whose leaves are
at even distance from the root and satisfy . . . ’ discussed in Example 2.3.2.
(3) All admissible ﬂat probabilistic ﬁxpoint logics, including ones featuring linear in-
equalities on probabilities.
(4) All admissible ﬂat conditional ﬁxpoint logics.
(5) All admissible ﬂat fragments of the monotone µ-calculus and the serial monotone
µ-calculus, including the star-nesting-free fragments of CPDL and game logic.
(6) All admissible ﬂat fragments of the alternating-time µ-calculus, including
alternating-time temporal logic ATL but also logics going beyond ATL, e.g. ones
featuring the operator ‘. . . holds in all even states along any path’ discussed
in Example 2.3.5. In fact, ATL appears to be the only example outside the
relational world for which a completeness result of this type was previously
known [Goranko and van Drimmelen 2006].
In most of these examples except graded and probabilistic ﬁxpoint logics, we actually
obtain ﬁnite axiomatizability, and locally ﬁnite axiomatizability in the case of graded
ﬁxpoint logics (Remark 3.9).
6. CONCLUSIONS
We
have
lifted
the
completeness
theorem
for
ﬂat
modal
ﬁxpoint
log-
ics [Santocanale and Venema 2010] to the level of generality of coalgebraic logic.
Speciﬁcally, we have given a Kozen-Park style axiomatization for ﬁxpoint opera-
tors, and we have shown this axiomatization to be sound and complete under the
conditions that (i) the deﬁning formulas of the ﬁxpoint operators satisfy a mild
syntactic restriction, and (ii) the coalgebraic base logic is axiomatized by a one-
step complete rule set. This result covers, e.g., probabilistic ﬁxpoint logics and ﬂat
fragments of the monotone µ-calculus, the ambient ﬁxpoint logic of Parikh’s game
logic [Parikh 1985] and concurrent PDL [Peleg 1987]. Further instances include
completeness of ﬂat fragments of the graded µ-calculus [Kupferman et al. 2002],
to our knowledge the ﬁrst completeness result for any graded ﬁxpoint logic, and
completeness of ﬂat fragments of the alternating-time µ-calculus [Alur et al. 2002],
with alternating-time temporal logic (ATL) being apparently the only previously
known example [Goranko and van Drimmelen 2006]. In those examples that have
ﬁnite modal similarity type, in particular for alternating-time ﬁxpoint logics, the
axiomatization we obtain is ﬁnite.
A core technical point in the proof was to show that essentially all monotone modal
operators (including nested ones like 22, as long as the nesting depth is uniform) are
ﬁnitary O-adjoints in the sense of Santocanale [2008], and hence induce constructive
ﬁxpoint operators that can be approximated in ω steps in the Lindenbaum algebra.
26

This has enabled a model construction using explicit time-outs for least ﬁxpoint for-
mulas in the spirit of the completeness proof for the aconjunctive fragment of the µ-
calculus [Kozen 1983], which relies on a judicious deﬁnition of timed-out formula.
A remaining open problem is to extend the completeness result to larger fragments
of the coalgebraic µ-calculus beyond the single variable fragment covered here, ﬁrst
and foremost the alternation-free fragment, and eventually the full coalgebraic µ-
calculus.
