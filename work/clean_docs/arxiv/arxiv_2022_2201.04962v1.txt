Distributed Cooperative Multi-Agent Reinforcement Learning with
Directed Coordination Graph
Gangshan Jing, He Bai, Jemin George, Aranya Chakrabortty and Piyush. K. Sharma
Abstract— Existing distributed cooperative multi-agent re-
inforcement learning (MARL) frameworks usually assume
undirected coordination graphs and communication graphs
while estimating a global reward via consensus algorithms
for policy evaluation. Such a framework may induce expen-
sive communication costs and exhibit poor scalability due
to requirement of global consensus. In this work, we study
MARLs with directed coordination graphs, and propose a
distributed RL algorithm where the local policy evaluations
are based on local value functions. The local value function
of each agent is obtained by local communication with its
neighbors through a directed learning-induced communication
graph, without using any consensus algorithm. A zeroth-order
optimization (ZOO) approach based on parameter perturbation
is employed to achieve gradient estimation. By comparing
with existing ZOO-based RL algorithms, we show that our
proposed distributed RL algorithm guarantees high scalability.
A distributed resource allocation example is shown to illustrate
the effectiveness of our algorithm.
Index Terms— Reinforcement learning, multi-agent systems,
decomposition, distributed control
I. INTRODUCTION
Traditional Reinforcement Learning (RL) aims to ﬁnd an
optimal policy for a single agent to accomplish a speciﬁc
task by making the agent interact with the environment. The
problem, however, is much more complex for multi-agent
reinforcement learning (MARL) due to the non-stationary
environment for each agent and the curse of dimensionality.
MARL, therefore, has been attracting increasing attention
recently, and has been studied extensively in e.g., [1]–
[7]. Markov game [8] has been commonly employed as a
mathematical formulation for MARL, where each agent has
its own reward and different agents may have conﬂicts of
interest. In distributed cooperative MARL, e.g., [1]–[3], the
global reward is usually formulated as the average of the
sum of all agents’ local rewards. Accordingly, policy gradient
algorithms always require each agent to track the global
reward by interacting with its neighbors via a connected
undirected communication graph. Moreover, due to the non-
stationarity of the environment, the global state information
is assumed to be known for all agents in several references,
e.g., [2], [4]. In [7], the authors considered a more distributed
setting, where each agent has its local state space and
local action space, and different agents are coupled through
G. Jing and A. Chakrabortty are with North Carolina State University,

H. Bai is with Oklahoma State University, Stillwater, OK 74078, USA.

J.
George
and
P.
Sharma
are
with
the
DEVCOM
U.S.
Army
Research
Laboratory,
Adelphi,
MD
20783,
USA.

network-based interactions. This is similar to the setting in
the present work.
In this paper, we study distributed cooperative MARL with
partial observations and a directed coordination graph. We
consider a general case where the coordination graph and
the communication graph are different but related to each
other. The coordination graph is considered to be weakly
connected, which speciﬁes the coordination relationship be-
tween agents and determines the partial observation of each
agent. The communication graph is designed for each agent
to obtain the value of its local value-function, which is a
directed graph dictated by the coordination graph. We show
that under an appropriate design for local value-functions,
the local gradient of the global value-function with respect
to the policy of each agent is equivalent to the local gradient
of a local value-function (see Lemma 1). As a result, gradient
estimation can be achieved without knowing the global value.
The main contributions of our work in contrast to the
existing literature are summarized as follows:
1) We employ a weakly connected graph to characterize
the coordination relationships among agents, which also
determines the partial observation of each agent. The
notion “coordination graph” has been used in [9], [10] to
describe limited communication and observability in the
network and enable distributed Q-learning. However,
this graph was always considered bidirectional in those
works, while we use a general directed graph.
2) We establish a connection between the coordination
graph and the communication graph required for learn-
ing, based on which each agent can estimate its local
gradient via communications with its designated set of
agents. The agents are not required to reach consensus
on any global index. Such a setting is more scalable to
large-scale networks compared with the distributed RL
based on average consensus, e.g., [1]–[3].
3) Distributed MARL via value-functions involving only
partial agents has also been studied in [7], where the
local value-function of each agent is designed to approx-
imate the global value-function, by ignoring some other
agents. In our work, there is no error induced by the
use of local value-functions in the sense that the partial
gradient of each local value-function is equivalent to
that of the global value-function; see Lemma 1.
4) Our distributed RL algorithm is a policy gradient algo-
rithm based on zeroth-order optimization (ZOO) with
exploration in the parameter space. It is known that the
dimension of the parameter space is typically higher
than that of the action space [11], [12], which is the
arXiv:2201.04962v1  [cs.MA]  10 Jan 2022

main shortage of parameter-based ZOO compared with
action-based ZOO for RL. In this paper, the sample
space dimension for each agent is signiﬁcantly reduced
due to the use of local value-functions. Moreover,
under different zeroth-order oracles, we show that our
learning framework always exhibits a reduced variance
of the estimated gradient compared with global value-
based policy evaluation. Note that most of the existing
distributed MARL works (e.g., [2], [3], [5]) require
estimating the value of the global value-function.
The rest of this paper is organized as follows. Section
II describes the MARL formulation and the main goal of
this paper. Section III proposes the local value-function
design and speciﬁes the communication graph required for
learning. Section IV shows the distributed RL algorithm, and
provides convergence and variance analysis. Section V shows
the simulation result of an application example. Section VI
concludes the paper.
Notation: Throughout the paper, G = (V, E) is an un-
weighted directed graph, where V = {1, ..., N} is the set of
vertices, E ⊂V × V is the set of edges, (i, j) ∈E means
that there is a directional edge in G from i to j. A path from
i to j is a sequence of distinct edges of the form (i1, i2),
(i2, i3), ..., (ir−1, ir) where i1 = i and ir = j. We use
i
E
−→j to denote that there is a path from i to j in edge
set E. A subgraph G′ = (V′, E′) with V′ ⊆V and E′ ⊆E
is said to be a strongly connected component (SCC) if there
is a path between any two vertices in G′. One vertex is a
special SCC. The d×d identity matrix is denoted by Id, the
a×b zero matrix is denoted by 0a×b. Rd is the d-dimensional
Euclidean space. N is the set of non-negative integers.
II. PROBLEM FORMULATION
A. Cooperative MARL with Partial Observation
We
model
the
MARL
as
a
tuple
M
=
(V, EC, Πi∈VMi, Πi∈VOi, Πi∈VRi, γ)
with
Mi
=
(Si, Ai, Pi) denoting the process for agent i, where
• V = {1, ..., N} is the set of agent indices;
• EC
⊆V × V is the edge set of the coordination
graph GC = (V, EC), which speciﬁes the coordination
relationship between agents;
• Si and Ai are the state space and the action space of
agent i, respectively;
• Pi
: Πj∈IiSj × Πj∈IiAj × Si
→P(Si) is the
transition probability function specifying the probability
of transition to state s′
i ∈Si under states {sj}j∈Ii and
actions {aj}j∈Ii, here P(Si) is the set of probability
measures on Si, Ii = {j ∈V : (j, i) ∈EC} ∪{i};
• Ri : Si × Ai →R is the immediate reward returned to
agent i when agent i takes action ai ∈Ai at the current
state si ∈Si;
• Oi = Πj∈IiSj is the observation space1 of agent i,
which includes the states of all the agents in Ii;
1The proposed results in this work are also applicable to cases when the
observation space of agent i is only a subset of Πj∈IiSj, e.g., Si.
• γ ∈(0, 1] is the discount factor that trades off the
instantaneous and future rewards.
Let S = Πj∈VSj, A = Πj∈VAj, and P = Πj∈VPj denote
the state space, action space and state transition matrix of
the whole multi-agent system. Let π : S × A →P(A) and
πi : Oi × Ai →P(Ai) be the global policy function of the
MAS and the local policy function of agent i, respectively.
Here P(A) and P(Ai) are the sets of probability measures
on A and Ai, respectively. The global policy is the policy
of the whole MAS from the centralized perspective, thus is
based on the global state s. The local policy of agent i is
the policy from agent i’s point of view, which is based on oi
composed of partial states. Note that a global policy always
corresponds to a collection of local policies uniquely.
At each time step t of the MDP, each agent i ∈V executes
an action ai,t ∈Ai according to its policy and the local
observation oi,t ∈Oi, resulting in transition to a new state
si,t+1, then obtains a reward ri,t+1(si,t, ai,t). Note that such
a formulation is different from that in [2], [4], where the
transition and reward of each agent are associated with the
global state st.
The long-term accumulated discounted global reward is
deﬁned as
R(s, a) =
T −1
X
t=0
γtrt(st, at) =
N
X
i=1
Ri(si, ai),
(1)
where Ri(si, ai) = PT −1
t=0 γtri,t(si,t, ai,t), rt is the global
reward for the MAS at time t, Ri(s, a) is the long-term
individual reward for agent i, ri,t is the individual reward
for agent i at time t, and T is the number of iterations
we consider. Note that maximizing R(s, a) is equivalent to
maximizing its average 1
N R(s, a), which has been commonly
adopted as the learning objective in many MARL references,
e.g. [1]–[3], [7].
Based on the long term global reward, with a given policy
π, we are able to deﬁne the global state value-function
V π(s′) = E[R(s, π(s))|s0 = s′] and state-action value-
function Qπ(s′, a′) = E[R(s, a)|s0 = s′, a0 = a′], which
describe the expected long term global reward when agents
have initial state s′ and initial state-action pair (s′, a′), re-
spectively. Similarly, a local state value-function with initial
state s′ for each agent i can be deﬁned as V π
i (s′) =
E[Ri(si, ai)|s0 = s′].
The goal of the MARL problem in this paper is to ﬁnd a
solution to
max
π
J(π) := Es0∼DV π(s0),
(2)
where D denotes the distribution that the initial state follows.
Our approach is also applicable to other formulations for
J(π), for example, J(π) = Es0∼Ds,a0∼DaQπ(s0, a0), where
Ds and Da are the two distributions that s0 and a0 follow,
respectively. For convenience of analysis, we also deﬁne the
expected value to be maximized corresponding to individual
reward for each agent i as
Ji(π) = Es0∼DV π
i (s0), i ∈V.
(3)
To solve for an optimal policy such that J(π) can be maxi-

1
2
3
4
5
6
7
8
9
Fig. 1: A coordination graph with 9 agents and 4 independent
maximum SCCs.
mized, without loss of generality, we parameterize the global
policy π(s, a) using parameters θ = (θ⊤
1 , ..., θ⊤
N)⊤∈Rd
with θi ∈Rdi. The global policy and agent i’s local policy
are then rewritten as πθ(s, a) and πθi
i (oi, ai), respectively.
As a result, it sufﬁces to solve the following optimization
problem:
max
θ
J(θ) := Es0∼DV π(θ)(s0).
(4)
A typical method for solving (4) is implementing the
policy gradient algorithm for each agent i:
θk+1
i
= θk
i + η∇θiJ(θk), i ∈V,
(5)
where η > 0 is the step-size.
Existing distributed policy gradient methods such as actor-
critic [2] and zeroth-order optimization [3] can always be
employed to estimate ∇θiJ(θk) when there is a connected
undirected communication graph among the agents. How-
ever, these approaches are based on estimating the value
of the global value-function, which requires a large amount
of communications during each learning episode. Moreover,
policy evaluation based on the global value-function has a
signiﬁcant scalability issue due to the high dimension of the
state and action spaces for large-scale networks.
In the next section, by utilizing the structure of the directed
coordination graph GC, we will design a local value-function
for each agent, based on which the local gradient can be
derived with only partial agents involved.
III. LOCAL VALUE-FUNCTION AND COMMUNICATION
GRAPH DESIGN
In this section, we introduce a local value-function for
each agent and propose a graph condition for inter-agent
communications required for distributed learning.
A. Graph Clustering and Local Value-Function Design
We consider the coordination graph GC as a weakly
connected directed graph2. Note that SCCs can always be
found in GC. Suppose that we already have a clustering
V = ∪n
j=1Vj, where Vj is the vertex set of the j-th maximum
independent SCC in GC, and Vj ∩Vk = ∅for any distinct
j, k ∈{1, ..., n}. An example for the coordination graph is
shown in Fig. 1, where there are 4 clusters corresponding to 4
maximum independent SCC, i.e., V1 = {1, 2}, V2 = {3, 4},
V3 = {5, 6}, and V4 = {7, 8, 9}.
Let
N L
i = {j ∈V : i
E
−→j}
2When GC is not weakly connected, the problem can be naturally
decomposed to independent smaller-sized MARL problems.
be the set of vertices in graph G that are reachable from agent
i. Denote IL
i = N L
i ∪{i}. It is observed that IL
i = IL
j if i
and j belong to the same cluster.
Deﬁne the following composite reward for agent i:
ˆri,t =
X
j∈IL
i
rj,t(sj,t, aj,t).
(6)
Accordingly, we deﬁne a local state value-function for agent
i:
ˆV π
i (s′) = E
h
ˆRi(s, a)|s0 = s′i
,
(7)
where ˆRi(s, a) = PT −1
t=0 γtˆri,t.
Now we are able to obtain a local value-function for each
agent i to maximize:
ˆJi(θ) = Es0∼D ˆV π(θ)
i
(s0) =
X
j∈IL
i
Jj(θ).
(8)
Given a function f(θ) : Rd →R and a positive δ, we
deﬁne
f δ(θ) = E[f(θ + δu)],
u ∼N(0, Id).
(9)
The following lemma builds a connection between the local
value-function ˆJδ
i (θ) and the global value-function Jδ(θ).
Lemma 1. The following statements are true:
(i) ∇θiJδ(θ) = ∇θi ˆJδ
i (θ) for any r > 0, i ∈V.
(ii) If Ji(θ), i ∈V are differentiable, then ∇θiJ(θ) =
∇θi ˆJi(θ), i ∈V.
Proof. Deﬁne
¯Ji(θ) = J(θ) −ˆJi(θ) =
X
j∈V\IL
i
Es0∼DV π(θ)
j
(s0).
(10)
Next we show that ¯Ji(θ) is independent of θi.
Let N L−
i
be the set of vertices in graph GC that can reach
i. Note that for each agent j, its action at time t, i.e., aj,t,
is only affected by the partial observation oj,t, the current
state sj,t, and policy θj. Therefore, there exists a function
fj : Oj × Rdj →P(Aj) such that
aj,t ∼fj(oj,t, θj) = fj({sk,t}k∈Ij, θj).
(11)
Similarly, according to the deﬁnition of Pi, there exists
another function hj : Πk∈IjSk × Πk∈IjAk →P(Sj) such
that
sj,t ∼hj({sk,t−1}k∈Ij, {ak,t−1}k∈Ij).
(12)
According to (11) and (12), we conclude that sj,t and
aj,t are essentially only affected by {sk,0}k∈N L−
j
∪{j},
{θk}k∈N L−
j
∪{j}, and the transition probability function of
each agent k ∈N L−
j
.
Note also that if j /∈IL
i , then i /∈N L−
j
∪{j}, which
implies that θi never inﬂuences E[Rj(s, a)|s0 = s] for j /∈
N L
i . Therefore, ¯Ji(θ) is independent of θi.
Proof for (i): it has been shown in [13] that
∇θJδ(θ) = 1
δκ
Z
Rd J(θ + δu)e−1
2 ∥u∥2udu,
(13)
where
κ =
Z
Rd e−1
2 ∥u∥2du.
(14)

Deﬁne
Φi = (0di×d1, ..., Idi, ..., 0di×dN ) ∈Rdi×d,
(15)
then ui = Φiu. It follows that
∇θiJδ(θ) = Φi∇θJδ(θ)
= 1
δκ
Z
Rd J(θ + δu)e−1
2 ∥u∥2uidu
= 1
δκ
Z
Rd
ˆJi(θ + δu)e−1
2 ∥u∥2uidu
+ 1
δκ
Z
Rd
¯Ji(θ + δu)e−1
2 ∥u∥2uidu.
(16)
Let ¯θi = (..., θ⊤
j , ...)⊤
j̸=i ∈Rd−di, ¯ui = (..., u⊤
j , ...)⊤
j̸=i ∈
Rd−di. Since we have proved that ¯Ji(θ +δu) is independent
of ui, the following holds:
Z
Rd
¯Ji(θ + δu)e−1
2 ∥u∥2uidu
=
Z
Rd−di
¯Ji(¯θi + δ¯ui)e−1
2 ∥¯ui∥2d¯ui
Z
Rdi
e−1
2 ∥ui∥2uidui
= 0.
Therefore,
∇θiJδ(θ) = 1
δκ
Z
Rd
ˆJi(θ + δu)e−1
2 ∥u∥2uidu = ∇θi ˆJδ
i (θ).
(17)
Proof for (ii): differentiability of Ji(θ) for all i ∈V
implies that ˆJi(θ) for all i ∈V and J(θ) are differentiable
as well. Since ¯Ji(θ) is independent of θi, we have
∇θiJ(θ) = ∇θi( ˆJi(θ) + ¯Ji(θ)) = ∇θi ˆJi(θ).
(18)
This completes the proof.
It is important to note that statement (i) in Lemma 1
does not require Ji(θ), i ∈V to be differentiable because
Jδ(θ) = E[J(θ + δu)] is always differentiable. When J(θ)
is not differentiable, we choose to ﬁnd the stationary point of
Jδ(θ). The gap between J(θ) and Jδ(θ) can be bounded if
J(θ) is Lipschitz continuous and δ > 0 is sufﬁciently small.
We make the following assumption for functions Ji(θ):
Assumption 1. Ji(θ), i ∈V are Li-Lipschitz continuous in
Rd. That is, |Ji(θ)−Ji(θ′)| ≤Li∥θ−θ′∥for any θ, θ′ ∈Rd.
Assumption 1 implies that J(θ) is L-Lipschitz continuous
in Rd, where L ≜P
i∈V Li, due to the following fact:
|J(θ)−J(θ′)| ≤
X
i∈V
|Ji(θ)−Ji(θ′)| ≤
X
i∈V
Li∥θ−θ′∥. (19)
B. Learning Graph Design
Lemma 1 has shown that having the local gradient of a
local value-function is sufﬁcient for each agent to update its
policy according to gradient ascent. In this paper, we will
estimate the local gradient based on the function value. To
ensure that each agent i has access to the value of ˆJi(θ), we
derive a learning graph (the communication graph required
for learning) GL = (V, EL), where
EL = {(j, i) ∈V × V : i
EC
−→j}.
(20)
Based on this deﬁnition, we have the following observations:
1
2
3
4
5
6
7
8
9
Fig. 2: The learning graph corresponding to the coordination graph
in Fig. 1.
Cluster 1
Cluster 2
Cluster 3
Cluster 4
Fig. 3: The cluster-wise learning graph.
• The subgraph corresponding to each cluster Vi in GL is
a clique.
• Once there is one communication link from one cluster
to another cluster, then any pair of agents in these two
clusters have a communication link.
• The agents in the same cluster share the same local
value-function.
To demonstrate the edge set deﬁnition (20), the learning
graph corresponding to the coordination graph in Fig. 1 is
shown in Fig. 2. According to the above-mentioned obser-
vations, the graph in Fig. 2 can be interpreted from a cluster
perspective, as shown in Fig. 3. In real applications, one
coordinator can be chosen in each cluster, based on which
different clusters are able to exchange information with each
other, so that a large amount of communication links in Fig.
2 can be avoided.
The following assumption ensures that agent i is able
to communicate with those agents whose rewards inﬂuence
ˆJi(θ).
Assumption 2. During the learning process, agents are able
to communicate with each other via the communication
graph GL.
We assume that given the global policy π(θ) and initial
state s0, each agent is able to compute Ri(s, a) and ˆRi(s, a)
based on the received rewards rj,t, j ∈IL
i
at each time
step t = 0, ..., T −1. By further considering random effects
in the transition, we rewrite the obtained values of agent
i by implementing policy π(θ) as Wi(θ, ξ) = Ji(θ) + ξi
and
ˆWi(θ, ξ) =
ˆJi(θ) + ˆξi with ˆξi = P
j∈IL
i ξj, where
ξ ∈RN and ξ ∼H, E[ξi] = 0, E[ξ2
i ] = σ2
i , i ∈V, H
is determined by randomness of both s0 and the transition
probability. Therefore,
Ji(θ) = E[Wi(θ, ξ)], ˆJi(θ) = E[ ˆWi(θ, ξ)],
∀i ∈V. (21)
Note that different from [1]–[3], where the agents need to
communicate with each other frequently during each learning
episode, in our work, only one-time communication for
transmitting Wi(θk, ξk) among neighbors in GL is required

in each learning episode k.
IV. DISTRIBUTED RL VIA ZOO
In this section, we propose a distributed RL algorithm
based on ZOO with policy search in the parameter space.
Since the parameter space usually has a higher dimension
than the action space, ZOO with policy search in the action
space was proposed in [12]. However, their approach requires
the action space to be continuous and leverages the Jacobian
of the policy π w.r.t. θ. Our distributed RL algorithm is
applicable to both continuous and discrete action spaces and
even does not require π to be differentiable. In addition,
our distributed learning framework based on local value-
functions is actually also compatible with the method in [12].
A. Distributed RL Based on Local Value Evaluation
Let θk = (θk⊤
1 , ..., θk⊤
N )⊤∈Rd be the global policy of
the MAS at episode k. For agent i, its policy is updated at
the k-th episode according to gradient ascent:
θk+1
i
= θk
i + ηgi(θk, uk, ξk),
(22)
where we adopt the following one-point zeroth-order oracle
gi(θk, uk, ξk) =
ˆWi(θk + δuk, ξk)
δ
uk
i ,
(23)
ξk
∈
Rp is the random variable induced by random
initial state sk
0 and possible noises in agents’ transitions
at episode k, δ
>
0 is the smoothing radius, uk
∼
N(0, Id) is the randomly sampled perturbation direction,
uk = (uk⊤
1 , ..., uk⊤
N )⊤, and uk
i ∈Rdi.
The following lemma shows that gi(θk, uk, ξk) is an
unbiased estimation of ∇θiJδ(θk).
Lemma 2. Given policy θk, it holds that ∇θiJδ(θk) =
E[gi(θk, uk, ξk)].
Proof. According to (21),
E[gi(θk, uk, ξk)] = E[
ˆJi(θk + δuk)
δ
uk
i ]
= ΦiE[
ˆJi(θk + δuk)
δ
uk]
= Φi∇θ ˆJδ
i (θk)
= ∇θi ˆJδ
i (θk),
(24)
where Φi is deﬁned in (15) and the third equality has been
proved in [13]. Recall Lemma 1, the proof is completed.
Remark 1. Besides the one-point feedback gradient esti-
mation (23), there are other efﬁcient unbiased estimation
approaches in the literature such as the two-point feedback
oracle [13] and residual feedback oracle [3], via which
the second moment of the estimated gradient has an upper
bound independent of the value of Wi. The distributed RL
framework in this work is also compatible with those different
gradient estimates, and the variance may be reduced by
utilizing different zeroth-order orcacles. However, in real
applications, it may be costly to implement one more policy
to compute the long term accumulated reward Wi, or to
store information at each time step. Therefore, we adopt the
simplest conventional one-point zeroth-order oracle (23).
Based on the zeroth-order oracle (23), the distributed RL
algorithm is proposed in Algorithm 1.
Algorithm 1 Distributed RL Algorithm
Input: Step-size η, initial state distribution D, number of learning
epochs K, number of iterations T (for policy evaluation), initial
policy parameter θ0, smoothing radius δ > 0.
Output: θK.
1. for k = 0, 1, ..., K −1 do
2.
Sample sk
0 ∼D.
3.
for all i ∈V do (Simultaneous Implementation)
4.
Agent i samples uk
i ∼N(0, Idi), implements policy
πi(θk
i +δuk
i ) for t = 0, ..., T −1, observes Wi(θk +δuk, ξk),
computes gi(θk, uk, ξk) in (23), and then obtains θk+1
i
ac-
cording to (22).
5.
end
6. end
Remark 2. Note that when the coordination graph GC
is strongly connected, we have IL
i
= V for all i ∈V,
then Algorithm 1 becomes a centralized RL algorithm. In
practice, when the multi-agent network is of a large scale,
it is reasonable to consider that the whole coordination
graph is weakly connected due to limited energy, selﬁshness,
or physical constraints of partial agents. In such cases
Algorithm 1 is more efﬁcient than centralized approaches
in terms of gradient variance and convergence rate.
B. Convergence Result
We further make the following assumption to ensure
solvability of (4).
Assumption 3. There exists J∗
i > 0 such that |Ji(θ)| ≤J∗
i
for any θ ∈Rd, ξ ∈Rp, i ∈V.
Assumption 3 implicitly implies that there exists an op-
timal policy for the RL problem (4), which is the premise
of solving problem (4). Based on Assumption 3, we can
bound | ˆJi| and |J| = | P
i∈V Ji| as P
j∈IL
i J∗
j ≜ˆJ∗
i and
P
i∈V J∗
i ≜J∗, respectively. Note that boundedness of Ji
does not lead to boundedness of Wi. Therefore, the observed
Wi may be unbounded due to unbounded noises such as
Gaussian noise during transition.
The following lemma bounds the variance of the zeroth-
order oracle (23).
Lemma 3. Under Assumption 3, for any θk
∈
Rd,
E[∥gi(θk, uk, ξk)∥2] ≤
( ˆ
J∗2
i +ˆσ2
i )di
δ2
, i ∈V, here ˆσ2
i
=
P
j∈IL
i σ2
j .
Proof. According to (23), we have
E[∥gi(θk, uk, ξk)∥2] = E[ ˆW 2
i (θk + δuk, ξk)∥uk
i ∥2]
δ2
(25)
=
1
δ2κ
Z
Rd E
h
( ˆJi(θk + δuk) + ˆξk
i )2i
∥uk
i ∥2e−1
2 ∥uk∥2duk
(26)

≤
1
δ2κ
Z
Rd( ˆJ∗2
i
+ E[ˆξk2
i ])∥uk
i ∥2e−1
2 ∥uk∥2duk
(27)
≤
ˆJ∗2
i
+ ˆσ2
i
δ2κ
Z
Rdi
∥uk
i ∥2e−1
2 ∥uk
i ∥2duk
i
Z
Rd−di
e−1
2 ∥v∥2dv
(28)
≤
ˆJ∗2
i
+ ˆσ2
i
δ2κ
di
Z
Rdi
e−1
2 ∥uk
i ∥2duk
i
Z
Rd−di
e−1
2 ∥v∥2dv (29)
= ( ˆJ∗2
i
+ ˆσ2
i )di
δ2
,
(30)
where κ is in (14), the ﬁrst inequality used the inde-
pendence between different ui, the second inequality used
R
Rdi ∥uk
i ∥2e−1
2 ∥uk
i ∥2duk
i ≤di
R
Rdi e−1
2 ∥uk
i ∥2duk
i , which has
been proved in [13, Lemma 1].
Lemma 3 shows that the variance of each local zeroth-
order oracle is only associated with the bound of the local
value-function ˆJ∗
i , and the variances of observations noises
for agents in IL
i . Note that if the policy evaluation is based on
the global reward, the bound of E[∥gi(θk, uk, ξk)∥2] would
be (J∗2+PN
i=1 σ2
i )di
δ2
. When the network is of a large scale,
J∗may be much larger than ˆJ∗
i , which means that our
algorithm has a signiﬁcantly improved scalability to large-
scale networks.
Theorem 1. Under Assumptions 1-3, let δ =
ϵ
L
√
d, η =
ϵ1.5
d1.5√
K . Then
(i). |Jδ(θ) −J(θ)| ≤ϵ for any θ ∈Rd.
(ii). By implementing Algorithm 1, if K ≥d3B2
ϵ5 , then
1
K
K−1
X
k=0
E[∥∇θJδ(θk)∥2] ≤ϵ,
(31)
where B = J∗−Jδ(θ0)+L4(J2
0 +σ2
0)/2, J0 = maxi∈V ˆJ∗
i ,
σ0 = maxi∈V ˆσi.
Proof. Statement (i) is obtained by using the Lipschitz
continuity of J(θ), which has been proved in [13].
Now
we
prove
statement
(ii).
According
to
Assumption
1
and
[13,
Lemma
2],
the
gradient
of
Jδ(θ)
is
√
dL/δ-Lipschitz
continuous.
Let
gk
=
(g⊤
1 (θk, uk, ξk), ..., g⊤
N(θk, uk, ξk))⊤
∈
Rd, the
following holds:
|Jδ(θk+1)−Jδ(θk)−⟨∇θJδ(θk), ηg(θk)⟩| ≤
√
dL
2δ η2∥gk∥2,
(32)
which implies that
⟨∇θJδ(θk), ηg(θk)⟩≤Jδ(θk+1) −Jδ(θk) +
√
dL
2δ η2∥gk∥2.
(33)
Lemma 3 implies that
E[∥g(θk, uk, ξk)∥2] =
N
X
i=1
E[∥gi(θk, uk, ξk)∥2]
≤
N
X
i=1
(2J2
0 + 2σ2
0)di/δ2 = (2J2
0 + 2σ2
0)d/δ2.
(34)
Taking expectation on both sides of (33), we obtain
ηE[∥∇θJδ(θk)∥2]
≤E[Jδ(θk+1) −Jδ(θk)] +
√
dL
2δ η2E[∥g(θk, uk, ξk)∥2]
≤E[Jδ(θk+1) −Jδ(θk)] + Lη2(J2
0 + σ2
0)d1.5
2δ3
.
It follows that
1
K
K−1
X
k=0
E[∥∇θJδ(θk)∥2]
(35)
≤1
η
 1
K
 E[Jδ(θK)] −Jδ(θ0)

+ Lη2(J2
0 + σ2
0)d1.5
2δ3

(36)
≤
d1.5
ϵ1.5√
K

(J∗−Jδ(θ0)) + L4(J2
0 + σ2
0)/2

.
(37)
The proof is completed.
Advantage Analysis. If the policy evaluation is based
on global value-function J(θ), then J2
0 and σ2
0 in (37) will
be replaced by J∗2 and PN
i=1 σ2
i , which may dominate the
complexity and be signiﬁcantly larger than J2
0 and σ2
0 when
there are a large number of agents. This implies that our
algorithm has a higher scalability to large-scale network
problems than the algorithms with policy evaluation based
on the global value-function.
According to (37), the convergence result can also be writ-
ten as
1
K
PK−1
k=0 E[∥∇θJδ(θk)∥2] ≤O(Bd1.5ϵ−1.5K−0.5),
which is similar to the result in [3, Theorem 3.2]. However,
they require a consensus algorithm to be run for a sufﬁciently
long time during each learning episode, which is unnecessary
in our algorithm. Therefore, our algorithm has a lower
sample complexity. However, the residual feedback used in
[3] may exhibit a lower gradient variance than the one-point
feedback (23) when ˆJ∗
i is very large for each agent i. In the
next subsection, we show that if the two-point feedback or
the residual feedback is utilized in our work, our local value-
function design can further reduce the gradient estimation
variance for large-scale networks.
C. Discussions on Distributed RL with Two-Point Feedback
We have proposed a distributed RL algorithm based on
the one-point zeroth-order oracle (23). We observe that
Algorithm 1 is always efﬁcient as long as g(θk, uk, ξk) is an
unbiased estimation of ∇θJδ(θk) and E[∥g(θk, uk, ξk)∥2] is
bounded. Therefore, the two-point feedback oracles proposed
in [13] and the residual feedback oracle in [14] can also be
employed in Algorithm 1. Based on the local value-function
design in our work, the two-point feedback oracle and the
residual feedback oracle can be obtained by
¯gi(θk, uk, ξk) =
ˆWi(θk + δuk, ξk) −ˆWi(θk, ξk)
δ
uk
i , (38)
and
˜gi(θk, uk, ξk) =
ˆ
Wi(θk + δuk, ξk) −ˆ
Wi(θk−1 + δuk−1, ξk−1)
δ
uk
i ,
(39)

respectively, where only local value-function is needed, in
contrast to [13], [14].
Deﬁne ˆLi = P
j∈IL
i Li, which is actually a Lipschitz
constant of ˆWi. Then we are able to show
E[∥¯gi(θk, uk, ξk)∥2] ≤E[(ˆL2
i + ˆσ2
i )∥uk∥2∥uk
i ∥2]
(40)
≤(ˆL2
i + ˆσ2
i )

E[∥ui∥4] + E[
X
j∈V\{i}
∥uj∥2]E[∥ui∥2]


(41)
≤(ˆL2
i + ˆσ2
i )
 (di + 4)2 + (d −di)di

(42)
= (ˆL2
i + ˆσ2
i )(did + 8di + 16).
(43)
As a result,
E[∥¯g(θk, uk, ξk)∥2] ≤(L2
0 + σ2
0)(d + 4)2,
(44)
where L0 = maxi∈V ˆLi.
Advantage Analysis. If the two-point feedback oracle
(38) is based on the global observation W(θ, ξ) = J(θ) +
PN
i=1 ξi, which is the case in [13], the bound in (44) will
be increased by replacing L2
0 and σ2
0 with L2 and PN
i=1 σ2.
Similarly, for the residual feedback, we can show that the
bound of E[∥˜gi(θk, uk, ξk)∥2] can be reduced by using local
value-functions due to the reduction of the Lipschitz constant
from L to L0. In conclusion, a local value-function always
exhibits a lower variance of the zeroth-order oracle than
the global value-function, thereby inducing a lower sample
complexity.
V. A DISTRIBUTED RESOURCE ALLOCATION EXAMPLE
We adopt a generalized version of the example in [3],
where the coordination graph is directed and the partial
observation of each agent includes states of its collaborative
neighbors and itself instead of only itself. Nonetheless,
our algorithm is applicable to the case when the partial
observation of each agent is itself only.
Consider a distributed resource allocation problem with
a coordination graph GC = (V, EC). There are N agents
representing N warehouses. At time step t, each agent i
stores resources with the amount mi(t) ∈R, receives a
local demand di(t) ∈R, sends partial of its resources to and
receives resources from its neighbors j ∈Ni. Then agent i
has the following dynamics
mi(t + 1) = mi(t) −
X
j∈N out
i
aij(t)mi(t)
+
X
j∈Ni
aji(t)mj(t) −di(t),
di(t) = Ai(1 −sin(wi,tt)) + wi,t,
(45)
where aij(t) ∈[0, 1] denotes the fraction of resources agent
i sends to its neighbor j at time t, 0 < Ai < mi(0) is
a constant, wi,t is a bounded noise with zero mean, Ni =
{j ∈V : (j, i) ∈EC} and N out
i
= {j ∈V : (i, j) ∈EC}
are the in-neighbor set and the out-neighbor set of agent i,
respectively.
Under Assumption 2 and a learning graph GL = (V, EL)
with EL deﬁned in (20), agent i receives the following
information at time step t during the learning process:
Partial observation: oi,t = [m⊤
Ii(t), d⊤
i (t)]⊤∈R|Ii|+1.
Individual reward: ri,t = 0 if mi(t) ≥0, and ri,t =
−m2
i (t) otherwise.
Individual rewards of neighbors in GL: rj,t, j ∈N L
i .
Deﬁne ai,t = (..., aij(t), ...)⊤
j∈N out
i
∈R|N out
i
| as the
action agent i takes. The local policy πi : R|Ii|+1 →R|Ni|
maps the observation oi,t of agent i to its action ai,t. The
global policy π : R18 →R
P
i∈V |Ni| maps all the agents’
states to all the local policies.
The cooperative control objective is to ﬁnd an optimal pol-
icy π∗such that the following global reward is maximized:
J =
N
X
i=1
T −1
X
t=0
γtri,t.
(46)
To seek the optimal policy πi(oi) for agent i to determine
its action {aij}j∈Ni, we parameterize the policy function by
deﬁning
aij =
exp(−zij)
P
j∈Ii exp(−zij),
(47)
where zij is approximated by radial basis functions:
zij =
nc
X
l=1
∥oi −cil∥2θij(l),
(48)
cil = (ˆc⊤
il, ¯cil) ∈R|Ii|+1 is the center of the l-th feature
for agent i, here ˆcil ∈R|Ii| and ¯cil are set according to the
ranges of mIi and di, respectively, such that cil, l = 1, ..., nc
are approximately evenly distributed in the range of oi.
Example 1. Consider 9 warehouses with the coordination
graph shown in Fig. 1. Fig. 2 shows the corresponding
learning graph. Set mi(0) = 1 + wi for all i = 1, ..., 9,
wi is a random variable with zero mean and is bounded by
0.01, iteration number T = 8, number of learning epochs
K = 600, di = 0.2(1−sin(wt))+wt, w ∼N(0, 0.01). Fig.
4 (left) depicts the evolution of the observed values of the
global value-function by implementing 4 different algorithms
repeatedly for 10 times. Here ξ∗corresponds to a ﬁxed initial
state mi(0) = 1, i = 1, ..., 9. In each time of implementation,
one perturbation vector uk is sampled and used for all the 4
algorithms during each learning episode k. The centralized
algorithm is the zeroth-order optimization algorithm based
on global value evaluation, while the distributed algorithm
is based on local value evaluation (Algorithm 1). The dis-
tributed two-point feedback algorithm is Algorithm 1 with
gi(θk, uk, ξk) replaced by ¯gi(θk, uk, ξk) in (38). We observe
that the distributed algorithms are always faster than the
centralized algorithms. Fig. 4 (middle) and Fig. 4 (right)
show the comparison of centralized and distributed one-point
feedback algorithms, and the comparison of centralized and
distributed two-point feedback algorithms, respectively. From
these two ﬁgures, it is clear that the distributed algorithms
always exhibit lower variances in contrast to the centralized
algorithms. This implies that the policy evaluation based on
local value-functions is more robust than policy evaluation
based on the global value-function.

0
100
200
300
400
500
600
Learning Episode
-2.5
-2
-1.5
-1
-0.5
0
Centralized, one-point
Distributed, one-point
Centralized, two-point
Distributed, two-point
W( k, *)
0
100
200
300
400
500
600
Learning Episode
-2.5
-2
-1.5
-1
-0.5
0
Centralized, one-point
Distributed, one-point
W( k, *)
0
100
200
300
400
500
600
Learning Episode
-2
-1.5
-1
-0.5
0
Centralized, two-point
Distributed, two-point
W( k, *)
Fig. 4: Comparison of (Left) different algorithms for 9 warehouses; (Middle) centralized and distributed algorithms under zeroth-order
oracles with one-point feedback; (Right) centralized and distributed algorithms under zeroth-order oracles with two-point feedback.
0
100
200
300
400
500
600
Learning Episode
-50
-40
-30
-20
-10
0
Centralized, one-point
Distributed, one-point
Centralized, two-point
Distributed, two-point
W( k, *)
Fig. 5: Comparison of different algorithms for 100 warehouses.
Example 2. We further consider a larger-size case where
there are N = 100 warehouses. The coordination graph
GC = (V, EC) has the following edge set:
EC = {(i, j) ∈V2 : |i−j| = 1, i = 2k−1, k ∈N}∪{(1, N)}.
Accordingly, graph GL = (V, EL) has the edge set
EL = {(i, j) ∈V2 : |i −j| = 1, i = 2k, k ∈N} ∪{(N, 1)}.
The parameter settings are the same as those in Example
1 Fig. 5 shows the evolutions of the observed values of the
global value-function by implementing 4 different algorithms
repeatedly for 10 times. Observe that when the network is of
a large scale, policy evaluation based on the global value-
function becomes very unstable due to the high variance
of the gradient estimates. On the contrary, our distributed
algorithms based on observing local value-functions are
more robust and exhibit signiﬁcantly lower variances. This
is consistent with our analysis in Subsections IV-B and IV-C.
VI. CONCLUSIONS
We studied the MARL problem with a directed coordi-
nation graph via a MDP formulation. Based on the weakly
connected coordination graph, we showed that a local value-
function can be designed for each agent, which is sufﬁcient
for estimating the local gradient of the global value-function.
To ensure that each agent has access to the value of the local
value-function, we further speciﬁed how the learning graph
can be designed. By employing zeroth-order oracles, we
proposed a distributed RL algorithm that has guaranteed con-
vergence, and exhibits reduced variance of the gradient. The
algorithm was applied to a distributed resource allocation
problem. For our future work we would like to investigate
how to reduce the number of required communication links
using graph-theoretic reduction techniques.
